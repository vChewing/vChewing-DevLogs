# vChewing & LibVanguard 工作區 - 格物致知專用文件

- 本文檔供 AI Agent 在每次開工前迅速了解專案全貌。
- **FeatureRequest（FR）目錄註記**：`vChewing-DevLogs/PendingFeatureReqs/` 存放 FeatureRequest 類 PreResearch 文件（現含 `FR608-PreResearch.md`）。該目錄**僅在被要求處理時才處理**——除非事主明確指示，否則不得主動動工、不納入任何 Phase 排程、不預先分析其內容。
- **Reqs4LLM 分卷歸檔註記**：`vChewing-DevLogs/Reqs4LLM/` 之下，分卷以「至多 10 個 Phase」為單位；現行卷為 `vChewing-DevLogs/Reqs4LLM/Archive_P201-P300/Reqs_0211-0220.md`（收 Phase 211~220，現載 Phase 211~216；同目錄的 `Reqs_0201-0210.md` 已滿、收 Phase 201~210），`vChewing-DevLogs/Reqs4LLM/Reqs_Other_Pending_Phases.md` 為不隨分卷歸檔的待定事項暫存。一卷寫滿、或經事主裁定結案後，即依其 Phase 區間移入 `vChewing-DevLogs/Reqs4LLM/Archive_P{首 Phase 所屬百位段}/`（既有：`Archive_P001-P100/`〔Phase 01~100〕、`Archive_P101-P200/`〔Phase 101~200〕、`Archive_P201-P300/`〔Phase 201~216〕）。**（archive 桶的「凍結」僅指該百位段的 100 個 Phase 已收齊；未滿 100 個 Phase 者只稱「已歸檔」、仍會續收後續結案的分卷——現下的 `Archive_P201-P300/` 即屬此態。）已滿或已結案的歸檔卷不再增改、新增記錄一律寫入現行卷、且牽涉的前述歸檔記錄需要補記（簡述）其在之後什麼時候的哪個 Phase 做了修改。** 本文件群內所有路徑一律以**工作區根**為錨（`vChewing-DevLogs/…`、`vChewing-LibVanguard/…`、`vChewing-macOS/…` 等）。
- **授權註記**：本倉庫自身內容以 **LGPL-3.0-or-later** 授權，詳見 `COPYING` 與 `LICENSES/preferred/LGPL-3.0-or-later`。倉內**引用**的程式碼／資料仍依其來源授權、不受本倉庫授權影響（`vChewing-LibVanguard` LGPL-3.0-or-later〔附 Swift 靜態連結例外〕、`vChewing-macOS`／`vChewing-OSX-Legacy`（自 2026-09-13 起，**閉包以外之一切自研內容已換軌為 `MulanPSL-2.0`**，兩倉自此不再有 MIT-NTL；**以 `LibVanguard`（原 `OSNeutralAssembly`）為終末點之 8 個 SPM package 則為 LGPL-3.0-or-later**——`LibVanguard`／`LexiconAssembly`〔內含 `TrieKit`；`TrieKit` 係套件與 target 名，與本倉 `Sources/` 的 `LX_` 檔名前綴無關〕／`Homa`／`Shared`／`Tekkon`／`BrailleSputnik`／`BPMFVS`／`SwiftExtension`〔惟 `SwiftExtension` 為 `MulanPSL-2.0`〕；其遞移相依閉包以外者一律為 `MulanPSL-2.0`）、`vChewing-VanguardLexicon` `MulanPSL-2.0`、`vChewing-Homebrew` AGPL-3.0，另有 Megrez／ButKo BPMFVS／KeyKey／McBopomofo 等第三方片段）。清單見 `README.md`。
- 最後更新：2026-09-16 // 這一行只寫日期，不用贅述 DevReqHistory 裡面的那種記載格式。
- AI Agent 得特別注意本文所提到的「Response Pattern」。
- **命名沿革（2026-09-13，Phase 201~204 ＋ 同日第二輪 ＋ Phase 205）**：本文檔在此日期之前的所有條目，所出現的 `Typewriter`／`LangModelAssembly`／`LMAssembly`／`LMInstantiator`／`LMI`／`LMMgr`／`LookupHub`／`lmi`／`lookupHub`／`lm*`（小寫家族）／`LMPlainBPMF`／`LangModel`／`langModel`／`TestLM` 等名稱，係各該階段當時的實際名稱，**依歷史記載原則原樣保留、不予改寫**。更名結果：兩倉（`vChewing-macOS`／`vChewing-OSX-legacy`）的 `OSNeutralAssembly`／`LexiconAssembly`／`LXAssembly`／`LXFacade`／`LXMgr`／`LXQuerier`／`lxQuerier`／`lx*`，對應目錄亦已同步（`Packages/vChewing_OSNeutralAssembly`、`Packages/vChewing_LexiconAssembly`、`Shared/vChewingComponents/OSNeutralAssembly`、`Shared/vChewingComponents/LXAssembly`、`LangModelManager/` → `LXManager/`）；另 `Shared.InputMode.langModel` → `.lexicon`、`LangModelCache` → `LexiconCache`、`resetLangModelCache` → `resetLexiconCache`、`initUserLangModels()` → `initUserLexicons()`、`targetLangModels` → `targetLexicons`，測試靶的 `TestLM` 族 → `TestLX` 族（`mockLM` → `mockLX`、`strLMSampleData*` → `strLXSampleData*` 等）。**本倉**的 `Lexicon.LMPlainBPMF` → `Lexicon.LXPlainBPMF`（檔名 `LX_LXPlainBPMF.swift`）、`lmPlainBPMFData` → `lxPlainBPMFData`、`HomaTests_Basic.swift` 的測試局部變數 `langModel` → `lexicon`。本倉程式碼除此與 3 處文字引用外零改動。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P201-P300/Reqs_0201-0210.md`。
- **命名沿革（2026-09-14 第二輪，Phase 214）**：`OSNeutralAssembly` → `LibVanguard`（**套件名、module 名、target 名、測試靶名、其目錄名，以及 macOS 的套件目錄名**）。範圍含兩倉（`vChewing-LibVanguard` 與 `vChewing-macOS`）之 import／`@testable import`／`@_exported import`、註解與 doc comment、`UserDefaults` suite 名、macOS 11 個 consumer manifest 的 product 名（改 `Vanguard`）、`plugin.swift` 的 bundle 名排除集、CI 檔名與 job 名、`Makefile` 目標名、以及兩倉的 README／EVOLUTION_MEMO／AGENTS／CLAUDE／algorithm／copilot-instructions／DevLab 散文。**macOS 套件目錄亦一併正名**（事主同日補佈置）：`Packages/vChewing_OSNeutralAssembly` → `Packages/vChewing_OSNeutral_LibVanguard`，故 11 個 consumer manifest 的 `.package(path:)` 與 `.product(…, package: "vChewing_OSNeutral_LibVanguard")`（SwiftPM 對本地路徑相依之 identity 取自目錄名）、3 處 CI `--package-path`、`Makefile` 2 處、`project.pbxproj` 2 處與全部文件路徑同步；但該目錄**內部**的 `Sources/OSNeutralAssembly/`、`Tests/OSNeutralAssemblyTests/` 已改名為 `LibVanguard/`、`LibVanguardTests/`。兩倉 `Package.swift` 自此**逐位元組相同**（並改用 `@resultBuilder` 的 ArrayBuilder DSL），兩倉 `Sources/` 內容**全等**、`Tests/` 僅差本倉獨有的 2 個搶救測檔。另含一項檔名殘端：聚合靶的 umbrella 檔 `OSNeutralAssemblySPM.swift` → `LibVanguardSPM.swift`（兩倉）——檔名不入 manifest，byte-identity 檢查與編譯都不會發現它，須靠「殘留的 `OSNeutralAssembly*` 建置產物」反查。
- **刻意保留、勿於後續命名清剿中更動（事主 2026-09-13 裁定）**：`ButKo_BPMFVS` 這個 **SwiftPM 套件**已更名為 `vChewing_BPMFVS`（macOS `Packages/vChewing_BPMFVS`），但以下一律**不動**——① `ButKo_BPMFVS_Accessors.swift` 檔名（macOS `Sources/BPMFVS/` 與 legacy `Shared/3rdParty/` 各一份；事主明示故意保留，legacy `project.pbxproj` 有 4 處引用）；② legacy `vChewing/Resources/ButKoBPMFVS_Assets/` 目錄（內含 ButKo 上游資產 `phonic_table_Z.txt`／`LICENSE_BPMFVS.txt`，只放第三方資產故不隨套件改名）；③ 使用者可見的**功能名**「ButKo BPMFVS」——含 4 語系 `Localizable.strings` 的 `i18n:UserDef.kReflectBPMFVSInCompositionBuffer.*`／`i18n:UserDef.kSpecifyCmdOptCtrlEnterBehavior.option.4` 文案與官網 `ReleaseNotes.md`／`Downloads.md`／`manual/preferences.md`；④ Swift 端的 `CommitableMarkupType.bpmfvsAnnotationButKo` 與測試函式名 `test_IH103*_ButKoBPMFVS*`。另：`build_darwin_SPMTestsAndPackage.yml` 的 `packages=(...)` **刻意不收** `./Packages/vChewing_BPMFVS`（該 package 有 `Tests/BPMFVSTests`，實測 6/6 通過；事主對該套件另有後續打算）。

---

## 一、專案概述

**LibVanguard**（先鋒引擎）是 vChewing Project 下一代輸入法的引擎的核心套件庫，以 Swift Package 形式提供。專案採用 LGPL v3.0 授權（`SwiftExtension` 模組例外：自 2026-09-13 起採 `MulanPSL-2.0`，見 `LICENSES/preferred/MulanPSL-2.0` 與 `COPYING`），涵蓋從注音符號解析、辭典樹查詢、語言模型聚合、到組字引擎的完整輸入法管線（pipeline）。

- **Swift 工具版本**：**Swift 6.4+**（6.2／6.3 為封堵對象——各套件之 `Package@swift-6.2.swift`／`6.3.swift` 以 `#error` 明文拒絕該兩版，理由見 §10.5 末段）；另備 **Swift 5.10.1**（open-source toolchain）供 macOS 10.9 legacy 側建置——自 Phase 216 起本倉具雙 toolchain 建置能力、Phase 217 起**推及 `vChewing-macOS` 全倉**，建置行為隨 toolchain 版本而異（見 §十）。
- **最低平台支援**：現行側 macOS 12（**.macOS(.v12)**）——自 Phase 213 起承襲 `vChewing-macOS` 的 `LibVanguard` 設定（原為 macOS 15 / macCatalyst 18 / iOS 18 / visionOS 2，事主裁定現階段一律承襲 macOS 側）；**5.10（legacy）側之 manifest 刻意不宣告平台（`platforms: nil`）、部署目標為 `x86_64-apple-macosx10.9`**（PackageDescription 能宣告的 macOS 最低值只有 10.10，一宣告即被明文寫入產物，索性不宣告；地板由 makefile 之 `-Xswiftc -target` 與 legacy 連結端決定。早前記為「`Data` 標為 macOS 10.10 起、故 `10.9` 不存在」——該說已經重測推翻——詳見 §十）。
- **跨平台目標**：保持對 Linux 與 Windows 的可建置性（透過 `#if canImport(Darwin)` 條件編譯平台限制）。
- **出貨產物**：**（6.4+ 側）單一動態庫 `libVanguard.dylib`**（manifest 之 `package.name = "LibVanguard"` 而 product 名為 `Vanguard`）。**聚合靶與其測試靶自 2026-09-14 第二輪起亦更名**為 `LibVanguard` / `LibVanguardTests`（原 `OSNeutralAssembly` / `OSNeutralAssemblyTests`），故 `import LibVanguard`；其餘模組名（`Homa`、`TrieKit`、`LexiconAssembly` 等）不變。SwiftPM 的資源 bundle 名取**套件名**，故實為 `LibVanguard_*.bundle`。**兩倉（本倉與 `vChewing-macOS`）自此 `Package.swift` 逐位元組相同、`Sources/` 全等**。**（5.10 側，自 Phase 216 起）兩顆均為 static 之產物**：`libVanguard.a`（聚合體，14 MB）與 `libVanguardSwiftExtension.a`（子套件，540 KB）；`.a` 係純 `ar` 封存、不帶任何 load command，故 minOS 不進產物。
- **角色（事主 2026-09-14 定調）**：本倉現為**實驗田**；`vChewing-macOS` 仍消費自己的 `Packages/vChewing_OSNeutral_LibVanguard`，兩者自此是「逐位元組同源、各自獨立」的兩份副本。LibVanguard 對 macOS 的**向前建置能力**屬後續 phase（**自 Phase 216 起開始落地**：5.10／legacy 側之雙 toolchain 建置已可行，見 §10.5）。

> 該專案本身不應視為 vChewing 唯音輸入法的一部分，除非今後刻意有此安排。

---

## 二、專案架構

### 2.1 目錄結構

```
vChewing-LibVanguard/
├── Package.swift                    # SPM 套件定義（package name: LibVanguard, 出貨 product: Vanguard）
├── makefile                         # lint / format / test / dockertest 指令
├── EVOLUTION_MEMO.md                # 各模組研發備忘錄
├── README.md                        # 專案說明（含〈What Is In This Repository〉）
├── COPYING / LICENSES/              # 授權（LGPL-3.0-or-later；SwiftExtension 與 ResourceLocator 例外為 MulanPSL-2.0）
├── .github/workflows/               # CI：test_ubuntu / test_winnt / test_darwin_LibVanguard.yml
├── Sources/
│   ├── LibVanguard/                 # 作業系統中立層：輸入控制器與狀態機（動態產品 Vanguard 之聚合靶）
│   ├── LexiconAssembly/             # 辭典聚合與洞察（LXFacade / LXQuerier / LXPerceptor）
│   ├── LXAssemblyMaterials4Tests/   # 測試專用辭典素材靶（獨立於出貨動態庫之外）
│   ├── Homa/                        # 護摩組字引擎
│   ├── TrieKit/                     # 辭典樹（RAM Trie + TextMap Trie）
│   ├── Tekkon/                      # 注拼引擎（聲韻並擊）
│   ├── BrailleSputnik/              # 盲文點字轉換
│   ├── BPMFVS/                      # 注音資產與存取器（ButKo BPMFVS）
│   ├── Shared/                      # 全體共用型別與常數
│   ├── SwiftExtension/              # Swift 語言擴展（MulanPSL-2.0）
│   ├── ResourceLocator/             # 執行期資源定位（MulanPSL-2.0）
│   └── vChewingSharedCLI/           # 跨模組共享命令列工具
└── Tests/
    ├── LibVanguardTests/
    ├── LexiconAssemblyTests/        # POM 測試族之大本營
    ├── HomaTests/ + HomaSharedTestComponents/
    ├── TrieKitTests/ / TekkonTests/ / BrailleSputnikTests/ / BPMFVSTests/
    ├── SharedTests/ / SwiftExtensionTests/ / ResourceLocatorTests/
    └── (本倉獨有) LexiconAssemblyTests/POMDecayWindowTests.swift
                 LexiconAssemblyTests/LXPlainBopomofoEtenDOSTests.swift
```

> **2026-09-14（Phase 213）之佈局變更**：本倉原有之 `Sources/LibVanguard/`、`Sources/_Modules/<模組>` 與
> `Tests/_Tests4Components/<模組>Tests`、`Tests/LibVanguardTests/` 已全數退場（73 檔），改為上表之
> **聚合式佈局**——即 `vChewing-macOS/Packages/vChewing_OSNeutral_LibVanguard` 的同源複製。該表所列 12 個
> `Sources/` 子目錄與 10 個測試靶與 macOS 側逐位元組一致，唯二差異為兩處硬編碼資源 bundle 名與本倉
> 獨有的 2 個測試檔。`TrieHub`／`FactoryTrieDBType`／`LexiconHub` 三個殘骸隨舊佈局一併退場。

> **本文件所屬的 `vChewing-DevLogs` 倉庫**：開發記錄已自 LibVanguard 獨立為工作區中的 `vChewing-DevLogs` 倉庫
> （原為 LibVanguard 的 `DevPlans/`，現供整個 vChewing 組織跨倉使用）：
>
> ```
> vChewing-DevLogs/
> ├── KnowledgeMemo4LLM.md               # 本文件
> ├── DevReqsHistory.md                  # 開發階段歷史
> ├── Reqs4LLM/                          # Phase 需求文件（現行卷見 Archive_P201-P300/）
> ├── Research/                          # 各 Phase 研究報告
> └── PendingFeatureReqs/                # FeatureRequest 類 PreResearch
> ```

### 2.2 技術架構

模組依賴關係（上層依賴下層）：

```
Vanguard (動態產品 / libVanguard.dylib) — 出貨產品之 target 清單共 10 個
└── LibVanguard (聚合靶；唯一的出貨動態庫之根)
    ├── BPMFVS ── ResourceLocator ── SwiftExtension
    ├── BrailleSputnik ── Shared ── SwiftExtension
    ├── LexiconAssembly ── TrieKit ── SwiftExtension
    │                    ├── Homa
    │                    └── Shared
    ├── Homa
    ├── ResourceLocator
    ├── Shared
    ├── SwiftExtension
    └── Tekkon

（出貨產品清單另以逐 target 列出，故上圖之邊即 `Package.swift` 內 `dependencies:` 之實際內容。）
```

**出貨產品（Products）**：`Vanguard`（`.dynamic`，targets 為 `LibVanguard`／`Shared`／`SwiftExtension`／`ResourceLocator`／`LexiconAssembly`／`TrieKit`／`Homa`／`Tekkon`／`BrailleSputnik`／`BPMFVS`）；另有 `LXAssemblyMaterials4Tests` 與 `HomaSharedTestComponents` 兩個**測試素材靶**動態產品（刻意不進出貨庫），以及 `vChewingSharedCLI` executable。

> **2026-09-14（Phase 213）後之模組集合**：本倉已無「公開／內部模組」之分——12 個 `Sources/` 子目錄即 10 個出貨 target ＋ 2 個支援 target（`LXAssemblyMaterials4Tests`、`vChewingSharedCLI`），其中 **`LibVanguard` 同時是唯一聚合靶**（SwiftPM 拒絕讓同一 target 同時被動態與靜態產品取用，故整個閉包只能以單一動態庫出貨）。舊圖（`LibVanguard` 聚合 target ＋ `Sources/_Modules/*` 六模組）已全部退場。

> **已移除模組（2026-09-13，Phase 206）**：`SharedCore`（`KBEvent` / `KeyCode` / `KBEventProtocol`）與 `CandidateKit`（`CandidateCellData`）兩模組已自本倉切除，連同其空殼測試靶。`SharedCore` 內與 FCITX 有關的相容項已遷入 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 兩倉、且於 Phase 206 改掛 MIT-NTL 標頭（惟該二檔皆落在 Phase 207 的授權閉包內，故其標頭已於同日隨閉包換軌回 LGPL-3.0-or-later）；`KBEvent` / `InputSignalProtocol` 在兩倉各有獨立副本，本倉移除不影響其運作。

---

## 三、核心資料模型

| 型別 | 模組 | 用途 |
|------|------|------|
| `Homa.Gram` | Homa | 元圖單位（Unigram/Bigram），承載讀音、字詞、機率、前驗詞 |
| `Homa.Node` | Homa | 組字節點，含元圖陣列、覆寫狀態、Bigram 快取 |
| `Homa.Segment` | Homa | 字詞幅節，`[Int: Node]` 字典（鍵為幅節長度） |
| `Homa.Assembler` | Homa | 組字核心處理器，持有遊標、段落、配置、動態規劃演算法 |
| `VanguardTrie.Trie.TNode` | TrieKit | Trie 節點，含 id / readingKey / children / entries |
| `VanguardTrie.Trie.Entry` | TrieKit | Trie 詞條，含 value / typeID / probability / previous |
| `Tekkon.Phonabet` | Tekkon | 單注音符號（Unicode Scalar + PhoneType） |
| `Tekkon.Composer` | Tekkon | 音節合成器，逐字元組合注音 |
| `VanguardTrie.TrieGram` | TrieKit | 元圖資料結構（`keyArray` / `value` / `probability` / `previous` / `anterior`）。原為 5 元 tuple，已改具名 struct。**Phase 213 前**的 LibVanguard `LexiconKit` 曾以 `public typealias HomaGram = VanguardTrie.TrieGram` 引用之，該模組已退場；macOS／LibVanguard 兩倉現以 `Homa.Gram` 為組字引擎的元圖型別，另有 `Homa.Gram.init(_ trieGram:)` 橋接。 |

---

## 四、核心組件詳解

### 4.1 Tekkon — 注拼引擎

負責注音符號與拼音的解析、輸入法鍵盤映射。

- **`Tekkon.Composer`**：音節合成器，接收按鍵輸入並組合出完整的注音音節。
- **`Tekkon.MandarinParser`**：支援 11 種注音排列（大千、ETen、許氏、IBM、星光等）與 6 種拼音風格（漢語拼音、耶魯、韋氏等）。
- **`Tekkon.PinyinTrie`**：簡化版拼音辨析 Trie，用於狂拼輸入的 `chop()` 拆解。
- **轉換 API**：`cnvPhonaToHanyuPinyin`、`cnvHanyuPinyinToPhona` 等注音⇄拼音雙向轉換。

### 4.2 TrieKit — 辭典樹

支援對讀音的多音字首字元檢索配對，分兩種實現：

- **`VanguardTrie.Trie`**（記憶體型）：全 RAM 常駐、無 QueryBuffer。序列化入口僅 **Vanguard Pragma TextMap 格式**一途（見下方說明）；不支援 Plist 序列化。
- **`VanguardTrie.TextMapTrie`**（TextMap 專用型）：Phase 35 定案後，已不再只是「惰性解析版 Trie」，而是 canonical specialized backend。其核心結構為 raw `Data` 常駐、排序後 full-key index、binary search exact lookup、lexicographic prefix-range scan、eager reverse lookup table、bounded parsed-entry cache，以及 partial-match 用的 `keyInitialsIDMap` prefilter。這讓它既維持低穩態記憶體占用，又能在 exact / longer-segment / revlookup 熱路徑上避免舊有通用 node materialization 成本。（Phase 11 曾嘗試 DFD 模式以 `pread()` 取代常駐 Data，但實機測試記憶體反飆升 20~30 MB 而作廢；詳見 Reqs4LLM/Archive_P001-P100/Reqs_0011-0020.md。）Phase 38 起，Typing TextMap 的 `KEY_LINE_MAP` canonical key 已改為 raw phonabet；TextMap caller layer 不得再對該 backend 做額外 encrypt/decrypt。
- **`VanguardTrieProtocol`**：統一查詢介面。核心 surface 為 `getNode(...)`、`getNodes(...)`、`getEntryGroups(...)`；高階 query API（`hasGrams` / `queryGrams` / `queryAssociatedPhrasesAsGrams`）已改以 entry groups 為主要抽象，使 `any VanguardTrieProtocol` existential 仍能命中 TextMapTrie specialized backend。
- **特殊功能**：部分比對（不完全讀音）、多音切割（分隔器 `&` 用於拼音→TrieKit 的多候選搜尋）。
- **三倉全等（Phase 200 收尾；Phase 213 後路徑更新）**：TrieKit 現於 `vChewing-LibVanguard`（`Sources/TrieKit/`）、`vChewing-macOS`（`Packages/vChewing_OSNeutral_LibVanguard/Sources/TrieKit/`）、`vChewing-OSX-legacy`（`Shared/vChewingComponents/LXAssembly/TrieKit/`）逐位元組一致，LibVanguard 的 `TrieKitTests/` 亦與 macOS 相同。兩處**不可清除**的工具鏈限制必須保留：① `TrieProtocol.swift` 的排序比較是手寫 unrolled 條件分支而**非** 5 元 tuple 的 `>`——後者在 legacy 的 Xcode-15／Swift-5 下 type-check 超時（語義已逐元核對等價，含 `seq` 交叉比較怪癖）；② `TK_QueryBuffer.swift` 的 `import SwiftExtension` 包在 `#if canImport(SwiftExtension)` 內，因 legacy 無該模組（其 `NSMutex` 與本檔同模組）。**另：TrieKit 的 5 元 tuple 已改為具名 struct `VanguardTrie.TrieGram`**（欄位名 `keyArray` / `value` / `probability` / `previous` / `anterior` 與原 tuple 標籤逐一相同，故成員存取點無需改動）；`TrieIO.parseValueLine` 改回傳 `[Trie.Entry]`（該 tuple 形狀與 `Entry` 逐欄相同，不需新型別）；`queryGrams` / `queryAssociatedPhrasesAsGrams` 回傳 `[VanguardTrie.TrieGram]`；LexiconKit 以 `public typealias HomaGram = VanguardTrie.TrieGram` 引用。`queryAssociatedPhrasesAsGrams` 內部那個帶 `seq` 的 6 元 tuple 與手寫 comparator 維持原樣。

#### TextMap 格式概要

`VanguardTrieIO` 提供 `parseTextMap` / `serializeToTextMap` / `loadFromTextMap` / `loadFromTextMapLazy`。`parseValueLine` 為共用的 VALUES 行解析方法，供全物化與惰性兩條路徑共用；RevLookup 由 MainTextMap 自動生成，不再讀取 external `.revlookup`。

TextMap 現行為單一 `.txtMap`（三段式 PRAGMA：HEADER / VALUES / KEY_LINE_MAP）。RevLookup 不再有獨立 sidecar，而是由 runtime 依 MainTextMap 自動構建。VALUES 行有三種型別：

- **型別 A 合併行** `>typeID\tencodedCell`：具有 DEFAULT_PROB 的 typeID 條目按讀音合併為單行，grouped cell 使用 escaped pipe 編碼。
- **型別 B CHS/CHT 機率分組行** `@probability\tchsCell\tchtCell`：僅限 `TYPE=TYPING`。`@` 前綴用來消除與一般三欄個體行的歧義，grouped cell 同樣使用 escaped pipe 編碼，BEL (`\u{7}`) 佔位空側。
- **型別 C 個體行** `value\tprobability\ttypeID[\tprevious]`：不具備 DEFAULT_PROB 或需要 Bigram `previous` 的條目。

完整格式規格引用：`vChewing-DevLogs/Reqs4LLM/Archive_P001-P100/Reqs_0000-0010.md` Phase 02 格式規格段落。

### 4.3 Homa — 護摩組字引擎

Megrez 的繼任者，實現漢字組句動態規劃演算法。

- **Bigram 支援**：Gram 承載真實讀音；Bigram 描述僅以前驗字詞為準（不糾結前驗讀音）。
- **節點覆寫**：`OverrideType.withTopGramScore`（頂分覆寫）與 `.withSpecified`（明確指定）。
- **候選字輪替與鞏固**：內建 `Homa_CandidateAPIs_Revolver`（輪替）與 `Homa_ConsolidatorAPIs`（上下文鞏固）。
- **路徑搜尋**：`PathFinder` 以動態規劃 + Unigram/Bigram 結合評分產生最佳組句。
- 取消了舊版 `LangModelProtocol` 協定，改用回調函式 `gramQuerier`。`gramAvailabilityChecker` 已於 Phase 44 移除（`insertKeys()` 直接使用 `queryGrams()` 檢查可用性，無需獨立回調）。
- **Phase 05 熱路徑最佳化**：`Assembler` 具備跨連續 `insertKey()` 的 bounded gram query cache；`queryGrams(using:cache:)` 以結構化 comparator / hash 去除原本字串插值排序與去重成本。

### 4.4 LexiconAssembly — 辭典聚合與洞察中樞（macOS／LibVanguard 兩倉同源）

整合多種辭典來源，向上層提供統一查詢介面。**本節原名 `LexiconKit`，其對位物自 Phase 213 起於 `vChewing-LibVanguard` 亦已更名為 `LexiconAssembly`（原 `Sources/_Modules/LexiconKit/` 整目錄退場）。`vChewing-macOS` 側早於 Phase 202 更名、Phase 210 併入 `Packages/vChewing_OSNeutral_LibVanguard`。**

- **`LXAssembly.LXFacade`**：辭典查詢門面（Phase 201~204 由 `LMInstantiator`／`LMI` 更名而來）。原 LibVanguard 的 `VanguardTrie.TrieHub`（Trie 資料庫中樞，唯一資料來源為 Vanguard Pragma TextMap 的 `updateTrieFromTextMapFile`）**已於 Phase 213 隨舊佈局退場**，其角色由 `LXFacade` ＋下述來源抽象承接。唯一公開查詢面為 `LXAssembly.LXQuerier`（Phase 201~204 之 API 壓縮）。
- **`LXAssembly.LXPerceptor`**：使用者習慣洞察器（POM），基於 Ngram 的行為追蹤、時間衰減三次曲線。原 LibVanguard 側名為 `Perceptor`。
- **`LXAssembly.LXPlainBopomofo`**：倚天中文 DOS 注音表（Phase 201~204 由 `Lexicon.LMPlainBPMF` 更名）。
- **`LXAssembly.LexiconGramSupplierProtocol`**：統一所有辭典來源的元圖供應協定。
- **`LXAssembly.LXGramSupplyHub`／`LXFactoryGramSupplier`**：多來源中樞與「把原廠辭典包成可掛載來源」的轉接器（Phase 212 落地，兩倉同源）。另有合流 API（`LXAssembly.GramConcatFlags`／`concatGramQueryResults`／`concatGramAvailabilityCheckResults`／`makeGramIdentityHash`）。宿主或測試可經 `LXFacade.mountGramSupplier(_:)`（或 `lxQuerier` 的同名方法）掛載任意來源，其元圖會併入 `unigramsFor` 的一般查詢結果、並被 `hasUnigramsForFast` 承認。**兩倉於此完全同源**（`vChewing-macOS` 為正本、`vChewing-OSX-legacy` 為減去模組 import 與 `nonisolated` 的同構副本；legacy 不承擔單元測試）；LibVanguard 自 Phase 213 起為 macOS 的逐位元組複製，故 Phase 212 所記之「四項刻意差異（命名空間／元圖型別／`makeGramIdentityHash` 之可見性／`Homa.Gram.init(_ trieGram:)` 橋接）」**已不再存在**。
- 不負責去重複化（交由 Homa 引擎處理）。

### 4.5 BrailleSputnik — 盲文點字

- 支援 1947 與 2018 兩種盲文標準。
- 核心 API：`convertToBraille(smashedPairs:, extraInsertion:) -> String`。
- 內含聲介韻調完整映射表與標點符號映射。

### 4.6 SwiftExtension — Swift 語言擴展（出貨產品名 `VanguardSwiftExtension`）

- Bool / Double / String / Array / Set 等型別的便利運算符與擴展。
- `LatinKeyboardMappings`：拉丁字母鍵盤映射表。
- `SwiftFoundationImpl`：Foundation 相容實現（跨平台）。
- **打包形態（2026-09-14）**：已自聚合體析出為獨立套件，置於**聚合體目錄之內**（macOS `Packages/vChewing_OSNeutral_LibVanguard/Deps/VanguardSwiftExtension/`、LibVanguard `Deps/VanguardSwiftExtension/`——如此 `.package(path:)` 兩倉同字串、聚合體 manifest 得以逐位元組相同），由該套件出貨 dynamic product `VanguardSwiftExtension`（Darwin 為 dynamic、其餘平台為 static）——**模組名仍為 `SwiftExtension`，故全倉一律 `import SwiftExtension`**（SwiftPM 不要求產品名與模組名相同）。聚合體之 `libVanguard.dylib` 對它是**動態相倚**而非靜態內嵌，故每個行程內本模組恰一份 image；App-based installer 亦僅同捆這一顆（383 KB）。授權為 MulanPSL-2.0（見該套件 `LICENSE`）。

---

## 五、已實作功能清單

- Phase 104 已完成：**Homa Revolver soft revolve 特性**。`Homa.Assembler.revolveCandidate` 新增 `softRevolve: Bool = false`；`softRevolve == true` 時在候選選擇層做「安全子集」過濾（新增私有 helper `isCandidateSafeToSoftRevolve`：segLength ≤ 1 短路放行，否則以 `calculateTargetCandidateRange` 算覆寫範圍，剔除會跨越「非當前節點」之 explicit 節點的多音節候選），再在安全子集上計算輪替索引。回傳的 `current` 為目標候選在**原始候選列表**中的索引，`total` 仍為原始候選總數，保留完整候選窗語義。鞏固邏輯不動（與 `skipInitialConsolidation` 正交）。Typewriter 端 `revolveCandidate(reverseOrder:softRevolve:)` 透傳該參數，凡由 SPACE 鍵（無論修飾鍵有無）啟動的輪替一律 `softRevolve: true`，其餘入口（Tab / ContextMenu / 方向鍵 / 方括號）維持 hard。解決「一個字一個字選」時鄰近已覆寫節點被多音節候選跨越毀掉的問題（根因：`calculateNextCandidateIndex` non-explicit 分支回傳索引 0 → segLength 最大候選 → `overrideCandidate` targetRange 跨越 explicit 鄰節點）。新增兩個 soft 驗收測試 + 保留兩個 hard 對照測試。跨 vChewing-LibVanguard + vChewing-macOS + vChewing-OSX-Legacy 三倉同步；LibVanguard Homa 48/48、macOS Typewriter 86/86、Legacy `make debug-core` 全數通過。
- Phase 105 已完成：**Homa Revolver soft revolve user-configurable 化**。新增 `UserDef.kPreferredRevolverForceLevel`（integer, default 2, range 0...2），讓使用者在偏好設定中選擇「一律強輪替（0）/ 僅 SPACE 路徑弱輪替（1）/ 一律弱輪替（2，預設）」。Typewriter 端所有 revolve 呼叫點（Tab / ContextMenu / 方向鍵 / 方括號 / SPACE 兩路徑）改為依 `prefs.preferredRevolverForceLevel` 動態決定 `softRevolve`：SPACE 路徑在 `!= 0` 時 soft，其餘入口在 `== 2` 時 soft。`PrefMgrProtocol` / `PrefMgr_Core` 新增 `preferredRevolverForceLevel: Int` @AppProperty 與 `fixOddPreferencesCore` 範圍自糾。Settings UI 控件由維護者從 Candidates 頁面調整至 Behavior 頁面（SwiftUI `VwrSettingsPaneBehavior` / Cocoa `VwrSettingsPaneCocoaBehavior`）。L10n 四語系（zh-Hant / zh-Hans / en / ja）5 條字串經 Gemma4 潤色，仔細闘述機理/目的/受眾/影響；ja 移除不自然的「就地」改用「その場での候補切り替え（お回し）」、各選項加上語義標籤。跨 vChewing-macOS + vChewing-OSX-Legacy 兩倉同步。macOS `swift build` + Typewriter 86/86 PASS、Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 106 已完成：**InputHandler `rotation` symbol name 消歧義為 `revolution`**。施術範圍限 InputHandler，跨 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 兩倉庫同步。`InputHandler_TriageInput.swift` 的 `triageByKeyCode` 內箭頭鍵分診臨時變數 `rotation` 改名為 `revolution`（該變數用來決定垂直向箭頭鍵在按住 Option/Shift 且處於 `.ofInputting` 時要改走 `revolveCandidate` 而非 `handleClockKey`，本意是「像左輪手槍一樣懸轉輪換」的 revolver-style revolution，與 client text writing direction 無關）；`InputHandler_TypingMethod.swift` 的 `// MARK: - Handle Rotation Toggles` 同步改名為 `// MARK: - Handle Revolution Toggles`，與其下的 `revolveTypingMethod` 語意對齊。既有 API 名 `revolveCandidate` / `revolveTypingMethod` / `revolveNext` / `preferredRevolverForceLevel` 不受影響。全倉 Swift 檔案已確認無 `rotation` 殘留。macOS `swift build` ✅、Legacy `make debug-core` ✅（兩倉 `make lint; make format` 均已執行並復編確認無回歸）。
- Phase 108 已完成：**Installer app bundle 舊版清除流程強化**。施術範圍為 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 app bundle 安裝包（pkg 不在本次範圍）。安裝前先 `killall vChewing` / `killall vChewingPhraseEditor`；以 `InputMethodConnectionName`（fallback `CFBundleIdentifier`）掃描 `~/Library/Input Methods` 與 `/Library/Input Methods` 中的 `.app` bundle；使用者層級舊版先改名為 `vChewing-yyyy-MM-ddTHH-mm-ss.appTrashed`（名稱碰撞時遞增 `-N`）再嘗試 trash；系統層級舊版以 `osascript` 索取管理員權限後 `mv` rename，失敗時記錄路徑並於安裝後以 alert 提示手動移除；`updateUpgradeableStatus()` 同步檢查使用者與系統雙目錄；新增 l10n key `i18n:Installer.AdminRenameFailureNotice`（四語系）。macOS `swift build`/`swift test`（InstallerAssembly4Darwin）✅、Legacy `make debug` BUILD SUCCEEDED（編譯目標為 `vChewingInstallerLegacy`）。
- Phase 109 已完成：**SwiftUI app installer alert refactor**。將 `VwrAppInstaller4SwiftUI.swift` 上多個 chained `.alert(...)` 改為單一 `.alert(item: $vm.config.alertItem)`；新增 `InstallerAlertItem` 與 `AlertType.makeAlertItem(paths:)`；SwiftUI 透過 `.onChange(of:)` 把 `currentAlertContent` / `adminRenameFailureAlertPaths` 同步成 `alertItem`；Cocoa 端統一由 `config.alertItem` 顯示 alert，僅在 `alertItem == nil` 時才轉換 `currentAlertContent`。所有 non-SwiftUI 修改同步至 `vChewing-OSX-Legacy`。macOS `swift build`/`swift test`（InstallerAssembly4Darwin）✅、Legacy `make debug` BUILD SUCCEEDED（編譯目標為 `vChewingInstallerLegacy`）。

- 注音符號與拼音的雙向解析與轉換（Tekkon）
- 11 種注音排列 + 6 種拼音風格支援（Tekkon）
- RAM 常駐 Trie + TextMap Trie 兩模式辭典（TrieKit）
- Vanguard Pragma TextMap 格式讀寫（TrieKit / TrieIO）：三種 VALUES 行型別（`>typeID\tencodedCell` / `@probability\tchsCell\tchtCell` / 個體行），DEFAULT_PROB 壓縮，escaped pipe grouped cell，BEL 佔位空側；RevLookup 由主 `.txtMap` 自動生成；Phase 38 起 Typing TextMap `KEY_LINE_MAP` 直接儲存 raw phonabet key（format version `1.1`）
- TextMapTrie canonical specialized backend（TrieKit，Phase 35）：原始 Data 常駐 + 排序後 full-key index + binary search + prefix-range scan + eager reverse lookup + entry-group fast path；穩態記憶體仍約為全物化 Trie 的三分之一（Phase 11 DFD 嘗試已作廢）
- SQLite 支援已全面移除（Phase 200，兩倉）：TrieKit 的 `VanguardTrie.SQLTrie`、`VanguardTrie.TrieSQLScriptGenerator`、`TrieHighFrequencyDecoder` 與本地 `CSQLite3` C 套件全數刪除，`Package.swift` 不再有 `CSQLite3` 依賴；`VanguardTrie.TrieHub` 不再持有 `sqlTrieMap`，`updateTrieFromSQLFile` / `updateTrieFromSQLScript` 一併退場，`isAppleSilicon()` 亦已無用而移除。`vChewing-VanguardLexicon` 端同步移除 `vanguardTrieSQL` / `vanguardSQLLegacy` 兩個建置目標、對應的 BuildTool Plugin 與 `compileSQLite(...)`，資料來源僅餘 Plist 與 TextMap。**Plist 支援亦已一併移除**：判死理由與生產級實測數據（同一份辭典下，plist 相對 TextMapTrie：啟動慢約 28×、查詢慢 12.11×、記憶體多約 7×、資產 41.0 MB 對 13.6 MB）見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0191-0200.md` 的〈死刑判決紀錄：SQLTrie 與 Plist〉。LibVanguard 側移除 `TrieIO.serialize` / `deserialize` / `save` / `load`、`TrieHub.updateTrieFromPlistFile` 與 `plistTrieMap`、`Trie` / `TNode` / `Entry` 的 `Codable`（含 `Entry` 的 tab-joined legacy 編解碼），以及**僅為 plist 加密鍵而存在**的 `Lexicon.encryptReadingKey` / `decryptReadingKey` 與 `GramConcatFlags.decryptReadingKeys` / `.decryptValues`（`GramConcatFlags` 的 `sort` / `deduplicate` 等通用選項保留）。序列化入口僅餘 TextMap 一途。**判死的是 plist 檔案格式，不是 `VanguardTrie.Trie` 這個 RAM 型資料結構**（後者保留）；`UserDef` / `PrefMgr` 等偏好存檔用的 plist 亦不受影響。**下游兩份 vendored TrieKit 副本亦已對齊**（`vChewing-macOS/Packages/vChewing_LangModelAssembly/Sources/TrieKit/`、`vChewing-OSX-legacy/Shared/vChewingComponents/LMAssembly/TrieKit/`），四檔與 LibVanguard 逐位元組一致（sha256 相同）；`TrieIO.validate(_:)` 的根節點假設亦由 `1` 修復為 `0`（LibVanguard 與兩份副本同步；原文一旦呼叫必然誤報 `isValid: false`）。
- Phase 35 的最終定案不是下游 `FactoryTextMapLexicon` 回退。`vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 factory dictionary runtime 最終皆維持 `factoryTrie: VanguardTrie.TextMapTrie?`，`VanguardTrieProtocol.getEntryGroups(...)` 則負責讓 existential query fast path 成立。
- 下游 Phase 36 已整體完成：`LMInstantiator.Config` 新增 `partialMatchEnabled = false` 作為 partial-match 總控；factory lookup 以 `FactoryCoreLookupStrategy.configuredLookup` / `.strictSuperset` 區分預設查詢與 strict-superset；plain BPMF / 倚天 DOS / cassette quick-result 以 `SupplementalLookupStrategy.configuredLookup` / `.exactMatch` / `.partialMatch` 收斂 vocabulary；最終 `LMInstantiator.lookupHub` 成為唯一 canonical public lookup surface，舊 `query*` / `cassetteQuickSetsFor(...)` wrappers 已自 current tree 移除。
- Phase 36 的完整敘事，以及 P5 wrapper retirement 的準入清單與 kickoff template，現已整併在 `vChewing-DevLogs/Research/Phase036_Research.md` appendix；不再維護獨立 checklist/template 檔案。
- Phase 38 已完成：`vChewing-LibVanguard` 的 `TrieHub` TextMap path，以及 `vChewing-macOS` / `vChewing-OSX-Legacy` 的 `LMInstantiator_TextMapExtension`，都已移除對 TextMap backend 的 phonabet encrypt/decrypt 假設；raw phonabet key 已成為目前唯一 canonical TextMap query surface。
- Phase 39 實作完成（A~D 全部）：
   - **Phase A**：`mergedAlternativeBucketUnigrams(for:)` 改寫，原廠辭典查詢改走 `factoryChoppedUnigramsFor()` / `factoryChoppedCoreUnigramsFor()`（`trie.getEntryGroups(keysChopped:...)` 路徑），原廠端查詢量從 O(tone_variants^syllable_count) 降為 O(1 per trie column)；`config.partialMatchEnabled` 正確傳遞至 `partiallyMatch` 參數。`makeTextMap` 測試輔助函式修正為 `VERSION 1.1`（修復 Phase 38 引入的 10+ 既有測試靜默失敗問題）。Review follow-up 再補 3 項 tone-bucket regression tests，鎖住單字升權、replacement/filter 排序與 CNS demote parity。跨 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 同步實裝。LangModelAssembly 87/87 通過。調研報告存為 `vChewing-DevLogs/Research/Phase038_PostResearch.md`。
  - **Phase B**：`TextMapTrie.getNodeIDsForKeyArray` 的 `keyInitials` 計算由 `getCachedFirstChar`（NSLock + Dictionary subscript + `_stringCompareInternal`）+ `internKey`（再一次 NSLock）改為直接 `$0.first?.description`（零鎖），消除每次呼叫 2N 次 lock 爭用與 N 次 string comparison overhead。
  - **Phase C**：`QueryBuffer<T>` 全面改寫——`DispatchQueue.sync` 改為 `NSLock`（消除 GCD 基礎設施開銷）；`Date()` / `TimeInterval` 時間戳改為 `DispatchTime.now().uptimeNanoseconds`（單調時鐘，無系統呼叫）；改以每 64 次操作才觸發一次 `removeExpiredEntriesLocked(now:)` 清理，取代每次 set/get 都建立 `Date()` 的模式。注意：`cleanupCheckInterval` 必須宣告為 `static var`（computed），因 Swift 不允許 generic type 內的 `static let` stored properties。
   - **Phase D**：`Homa.Assembler.gramQueryCache` 從 `[[String]: [Homa.Gram]]`（需對整個 `[String]` 陣列重新雜湊）改為 `GramQueryCacheKey`（完整 `keyArray` + 預先計算 hash）的結構鍵，`gramQueryCacheOrder` 亦同步改為 `[GramQueryCacheKey]`，維護插入順序以實現 LRU 半量淘汰（淘汰最舊 50%）取代舊的 `removeAll()` 全量清空。Review follow-up 再修補原先直接使用裸 `Int` hash 的碰撞誤命中風險，保留預先計算 hash 的成本優勢但恢復 structural correctness。
   - 三個 Phase（B/C/D）已同步鏡像至 `vChewing-macOS`（`vChewing_LangModelAssembly/TrieKit/`、`vChewing_Homa/`）與 `vChewing-OSX-Legacy`（`Shared/vChewingComponents/LMAssembly/TrieKit/`、`Shared/vChewingComponents/Homa/`）；Phase D review fix 亦同步補到 `vChewing-LibVanguard`。LibVanguard HomaTests 42/42、vChewing-macOS Homa 42/42、Typewriter 44/44 通過。
- Phase 46 已完成（Omega-1 + Alpha + Beta + Zeta）：**Omega-1** 以 `Homa.PossibleKey` 結構化枚舉取代 `&` 字串編碼；`Config.keys` / `Assembler.keys` 由 `[String]` 改為 `[PossibleKey]`；`assignNodes` 以笛卡爾積內部展開 `allValues`、跨組合去重後按 `keyArray.count` 降冪再 `probability` 降冪排序；`insertKey` 保留 `String` / `[String]` / `[[String]]` 三種過載橋接；Typewriter `makeToneInsensitivePinyinQueryKey` 與 `readingKeyForQuery` 改回傳 `[String]`；LMInstantiator 移除 `mergedAlternativeBucketUnigrams`、`expandAlternativeKeyArrays`、`AlternativeKeyArrayIterator` 與 `&` 分支；修正 `queryGramsForAlternatives` 未排序導致 test_IH107 工廠 unigram 壓過使用者臨時詞的迴歸。**Alpha+Zeta**：`queryGramsForAlternatives` 移除冗餘 `insertedIntel` Set、加入無替代讀音快速路徑。**Beta**：`unigramsFor` LRU cache（上限 1024 + config fingerprint 自動失效）。**補遺**：`insertTemporaryData` / `clearTemporaryData` / `injectTestData` / `loadUserPhrasesData` / `reloadUserFilterDirectly` 在 Phase 46 Beta 後未同步使 LRU cache 失效，導致 sub-LM 資料變更後查詢仍返回過時結果；已補上對應的 cache 清除邏輯。
- Phase 47 已完成：**動態 `maxSegLength`**。`Homa.PossibleKey` 新增 `count`（可能讀音數量）與 `isMultiple`（是否多讀音）屬性；`assignNodes` 在掃描半徑計算後、span 遍歷前，檢查 `rangeOfPositions` 內是否存在 `isMultiple` 的 key——若存在且 `maxSegLength > 4`，將 `maxSegLength` 動態縮減為 4。對完整注音 / 有聲調拼音（無 `.multipleKeys`）保持 `maxSegLength = 10`，長詞不受影響；對無聲調拼音（有 `.multipleKeys`）每次插入新增查詢量封頂在 780 次（5^1 + ... + 5^4），繞開 5^5 = 3,125 的 severe hang 臨界點。已同步至 `vChewing-macOS`、`vChewing-OSX-Legacy`、`vChewing-LibVanguard`。
- Phase 48 已完成：**LMCoreEX 前綴部分匹配**。在 `LMCoreEX` 之上疊加輕量前綴索引 `sortedKeys: [String]`（由 `rangeMap.keys.sorted()` 產生），新增 `keys(matchingPrefix:)`（二分搜尋 sortedKeys + 線性掃描 temporaryMap）與 `unigramsFor(keyPrefix:)`（對每個匹配 key 自動推導正確 `keyArray` 後走 exact-match 路徑）。`LMInstantiator.unigramsFor(keyArray:)` 新增 `partiallyMatch: Bool` 過載，預設 `false` 以維持既有 exact-match 行為；`LookupHub.grams(for:)` 改傳 `partiallyMatch: lmi.config.partialMatchEnabled`，使使用者片語（`lmUserPhrases`）在開啟 partial match 時也能回傳前綴匹配結果。單漢字權重上限邏輯修正為僅對單音節匹配結果生效（避免多音節前綴匹配結果被錯誤壓分）。`lmUserSymbols` 與 `lmFiltered` 維持 exact-match 不變。**Review follow-up**：`LMInstantiator.unigramsFor(keyArray:partiallyMatch:)` 的 LRU 快取鍵加入模式維度（`PM0/PM1`）以隔離 exact / partial 查詢結果；`LMCoreEX.keys(matchingPrefix:)` 新增 key 去重並將 `temporaryMap.keys` 排序，修復重複回傳與順序抖動。新增 `testPartialAndExactQueriesDoNotShareCacheEntry`、`testPrefixMatchingDeduplicatesOverlappingMainAndTemporaryKeys` 回歸測試。已同步至 `vChewing-macOS` 與 `vChewing-OSX-Legacy`；LangModelAssembly focused tests 16/16、Legacy `make debug-core` 均通過。
- Phase 49 已完成：**narrateTheComposer 朗讀改進**。A) 防禦性注音轉換：無論 `maybeKey` 來源為何，若內容含 ASCII 字母，一律嘗試以 `Tekkon.cnvHanyuPinyinToPhona` 轉為注音，失敗則退回 `composer.value`。B) 正常組字路徑改以 `actualKeys[cursor - 1]` 為朗讀對象（取代 `readingKey.first`），確保 pathfinding 結果正確反映。C) 後置聲調覆寫 `.success` 分支補上朗讀回饋。**額外修復**：`getPreviousRearSyllableSnapshot` 原以 `receiveSequence(readingKey, isRomaji: true)` 重建 composer，但 `actualKeys` 永遠儲存注音，導致拼音模式下後置聲調覆寫完全失效；修正為統一使用 `receiveKey(fromPhonabet:)`。已同步至 `vChewing-macOS` 與 `vChewing-OSX-Legacy`；Typewriter 47/47 PASS。
- Phase 50 已完成：**冗餘 `assemble()` 清理與 Homa `dropKey` 根因修復**。Typewriter / InputHandler 層面刪除 18 處冗餘 `assemble()` 呼叫（macOS 9 + Legacy 9），涵蓋 `insertKey`、`dropKey`、`overrideCandidate`、`consolidateCursorContext`、`updateUnigramData`、`commitOverflownComposition` 等路徑。**手術中發現**：`assembler.dropKey` 的 `try? assignNodes()` 在 `noNodesAssigned` 時會跳過 `assemble()`，導致 `assembledSentence` 停留在刪鍵前狀態；`test_IH204_DropKeyAgainstAnOverriddenCandidate` 因此失敗。根因修復：將 `dropKey` 改為 `do { try assignNodes() } catch { assemble() }`，確保無論異常與否均重新組句。已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`；Typewriter 47/47、Homa 43/43、LangModelAssembly 89/89、LibVanguard Homa 44/44、Legacy `make debug-core` 全部通過。
- Phase 51 已完成：**Homa revolver 首輪 pre-consolidation 過強鎖定修復**。`test_IH201_RevolvingCandidates` 在 `main` 失敗（`流一縷` 退化成 `留一縷`）的根因為 Homa revolver 在「旅 -> 一縷」輪替路徑中，首輪 pre-consolidation 以強覆寫方式鎖住 overlap edge（前一節點的「留」），壓掉 LM 重新評分後應得的 `流`。修復以「Homa 可配置首輪 pre-consolidation + Typewriter 游標結構策略」落地：`revolveCandidate(...)` 新增 `skipInitialConsolidation: Bool = false`（預設維持既有行為）；Typewriter 依 `homaCandidateCursorType` 與 `assembler.isCursorAtAssemblerEdge(direction:)` 決定是否跳過首輪鞏固——僅 `.placedFront` 且游標在 front edge、或 `.placedRear` 且游標在 rear edge 時啟用。這使 `IH201` 的 edge-overlap case 避免過強鞏固，同時保持 `JiHuQiKeng` 非 edge 情境的既有穩定路徑。新增 Homa 層回歸測試 `Assembler_RevolveCandidateAvoidsOverConsolidatingLeadingOverlap`（macOS + LibVanguard），以及 Typewriter 層 `test_IH201` 增強 case。已同步至 `vChewing-macOS`、`vChewing-LibVanguard`、`vChewing-OSX-Legacy`；Typewriter 47/47、MainAssembly4Darwin 63/63、Homa 44/44（macOS）/ 45/45（LibVanguard）、Legacy `make debug-core` 全部通過。
- Phase 56 已完成：**MixedAlphanumericalTypewriter 系統性前綴吸收修復 + Izanami 窮舉測試落地**。Izanami 測試以 `["", "ai", "AI"]` 為參數、窮舉 `lmPlainBopomofo.sortedKeys` 所有 reading，核心斷言為「non-empty prefix 必須保留於 commissions」，一旦 composer 誤吸收即失敗。經三輪迭代共定位八類機制缺陷：純注音路徑超長吞噬、auto-split 排序偏向長後綴、Space 缺乏 non-word-like fallback、ETenDOS 被 `hasUnigramsForFast` 過濾、Tekkon auto-correct 干擾 suffix 判斷、`shouldPreferASCIIWordPath` 誤傷 2-char 前綴、整段注音路徑優先於 auto-split、`hasNoDestructiveOverwrite` 未檢查詞庫命中。修復：動態碼長上限（Dachen26=6，其餘=4）、關閉 auto-correct、引入 `twoCharPrefixIsPhonetic` 豁免、長後綴優先、結構化排序、Gated `hasNoDestructiveOverwrite`。驗證：`vChewing-macOS` Typewriter 78/78 PASS（含 Izanami 3 cases）；Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 68 已完成：**MixedAlphanumericalTypewriter camelCase 後綴防禦**。`macOS ` 被 auto-split 誤拆為 ASCII 前綴 `ma` + 注音後綴 `OS`（Tekkon 對大小寫不敏感）。修復：`buildAutoSplitCandidate` 新增 `suffixHasUppercase` guard，拒絕後綴含大寫字母的候選。僅檢查 suffix（prefix 允許含大寫如 `Hello你好`），不影響既有 auto-split 行為。新增 `test_IH429`（macOS + This）。兩倉庫同步，Typewriter 10/10 mixed-mode PASS，Legacy BUILD SUCCEEDED。已知限制：`iPhone ` 等中段大寫 + 單字母後綴仍可能誤拆（既有問題，留待後續 Phase）。
- Phase 55 已完成：**MixedAlphanumericalTypewriter 聲調與 Shift ASCII 阻斷修復**。第一要務：在 `MixedAlphanumericalTypewriter` 內對 `BPMFFullMatchTypewriter` 委派點（拼音模式 fallback、Space finalize、fully-parser-covered 注音確認）強制關閉 `isToneOverrideEnabled` 與 `isLeadingIntonationAccepted`；`isLeadingToneBlocked` 與 auto-split 後綴首鍵檢查均移除對 `handler.prefs.acceptLeadingIntonations` 的依賴，改為無條件阻斷——Mixed mode 永遠不接受聲調前置鍵入。第二要務：新增 `isShiftASCII` 與 `shouldBlockPhoneticAbsorption`（digit + uppercase）閘門，確保 Shift 敲入的 ASCII 及純數字/大寫字母不會被 Tekkon composer 吸收；`isFullyParserCovered` 路徑排除以 ASCII 數字開頭或包含大寫字母的序列，讓 auto-split 有機會正確切分「ASCII 前綴 + 注音後綴」。新增 IH425A–IH425C（digit/uppercase/Shift 阻斷）與 IH426–IH427（leading digit + uppercase auto-split）回歸測試。i18n 同步更新：四語系 `Localizable.strings` 為 `kAcceptLeadingIntonations` 與 `kSpecifyIntonationKeyBehavior` 新增 `i18n:` 鍵值，並在描述中明確載明 mixed mode 無視這些設定。其中 digit 阻斷以 `hasIntonation(withNothingElse: true)` 精準區分聲調數字鍵（大千 `3`/`4`/`6`/`7`）與聲母/韻母數字鍵（`1`/`2`/`5`/`8`/`9`/`0`），避免 `5k4`→`這` 被誤阻斷；`isFullyParserCovered` 移除 `!fullInputStartsWithASCIIDigit`，交由既有 `isLeadingToneBlocked` 統一判定。新增 IH428（`5k4`→`這`）。驗證結果為 `vChewing-macOS` Typewriter 77/77 PASS，`vChewing-OSX-Legacy` `make debug-core` BUILD SUCCEEDED。
- Phase 54 已完成：**LMInstantiator TextMap path 直接收斂到 `VanguardTrie.Trie.EntryType`，並修補 partial-match kana suppression parity**。`CoreColumn` 已自 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 LMInstantiator factory lookup surface 退場，`defaultScore` 直接掛到 `EntryType` extension；`factoryUnigramsFor`、`factoryStrictSupersetUnigramsFor`、`factoryPartiallyMatchedUnigramsFor`、`factoryChoppedUnigramsFor`、`hasFactoryCoreUnigramsFor` 等呼叫面統一改為 `entryType:`。同時將 `suppressFactoryUnigramsOfKanaSyllables` 補進 `queryGrams(...)` 對應的 `makeFactoryUnigrams(queriedGrams:...)`，修復 `partialMatchEnabled = true` 時 kana-only 結果仍漏出的 regression；其後又將 suppression predicate 固化為 value/script-based kana detection，以避開 TextMap build 對 raw kana weights 的正規化。`vChewing-macOS` 新增 `testSuppressFactoryKanaSyllablesStillWorksWithPartialMatchEnabled`；`LMInstantiatorTextMapTests` 22/22 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 53 已完成：**Typewriter 層中英文混輸**。新增 `MixedAlphanumericalTypewriter`（`TypewriterProtocol` 實作），採 `composer`（注音）+ `mixedAlphanumericalBuffer`（ASCII）雙軌模型，由 `mixedAlphanumericalEnabled` 偏好控制（預設關閉），與 cassette/pinyin 互斥，不侵入 Session 層。核心行為：auto-split 演算法即時計算最優 ASCII/Phonetic 邊界（單音節後綴搜尋，四層排序：digit-leading → longer-pure-alnum → best-probability → suffix-length）；`shouldPreferASCIIWordPath` 啟發式偵測英文詞的 destructive overwrite trace 防止 `tod `/`film ` 被誤判注音；Space finalize 設 `onLexiconMatchFailure` fallback（詞庫查無時 commit 中文段+ASCII+空白）；Enter/Space 提交時先 flush 中文段再提交 ASCII；`resolveVisibleInputText` 保留 Shift+符號可見語義；Option+主鍵盤區 ASCII 直交；`acceptLeadingIntonations = false` 在 mixed mode 下雙重防護。Session 層僅 `resetInputHandler()` 新增一行 buffer 提交。後續 follow-up 已整併：修復 `?c96` digit-leading 邊界、`tod `/`film ` 誤判、聲調前置繞過。驗證結果為 IH401–IH424 共 42 項測試全數 PASS，Legacy `make debug-core` BUILD SUCCEEDED。已知限制：全 parser-covered 短英文 token 在持續鍵入時仍可能 zhuyin-first。完整研究報告見 `vChewing-DevLogs/Research/Phase053_Research.md`。
- Phase 52 已完成：**nonKanji（假名、鴨蛋零等）原廠辭典查詢修復**。根因確認為下游 `LMInstantiator` 的 `theDataMISC` 查詢被 `keyChain.hasPrefix("_")` 條件限制，導致普通讀音 key 下的 `nonKanji`（typeID 8）條目（如假名 `あ`、鴨蛋零 `〇`）永遠不會被返回。修復以新增 `CoreColumn.theDataNonKanji`（textMapTypeIDs = [8]）並在 `unigramsFor` / `hasUnigramsForFast` 中為所有普通讀音 key 啟用該查詢路徑落地，同時保留 `_` 開頭 key 的 MISC（typeID 4 + 8）既有行為。`vChewing-macOS` LangModelAssembly 新增 2 項回歸測試鎖住假名/鴨蛋零查詢與標點隔離。已同步至 `vChewing-macOS` 與 `vChewing-OSX-Legacy`；LangModelAssembly 91/91、Typewriter 74/74 PASS，Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 52 follow-up 已完成：新增偏好旗標 `suppressFactoryUnigramsOfKanaSyllables`，允許使用者手動停用原廠假名音節輸出。LMI 在 `theDataNonKanji` 路徑新增條件過濾，最終改為以詞值的 kana script 判定是否濾除，不再依賴會被 TextMap build 正規化的 raw probability bucket，並保留 `〇` 等非假名 nonKanji；Settings 與四語系 i18n 亦已同步補齊。另在 mixed mode 下，除了 `input.isSymbolMenuPhysicalKey` 例外鍵之外，主鍵盤所有數字鍵 / 字母鍵 / 符號鍵只要 Alt 被摁著，就直接以 keyCode + `LatinKeyboardMappings` 還原 raw ASCII；若 assembler / `mixedAlphanumericalBuffer` 已有內容，先 flush 舊內容，再把當前 ASCII 直接 commit。`vChewing-macOS`（SwiftUI + Cocoa）與 `vChewing-OSX-Legacy`（Cocoa）均已鏡像同步；LangModelAssembly focused tests、Typewriter IH412–IH424 focused tests 與 Legacy `make debug-core` 均通過。
- 完整 Lexicon partial-match 熱路徑最佳化（Phase 05）：Assembler 跨插入查詢快取、Trie split cache 重用、node.id 去重、SQL prepared statement reuse
- 下游驗證（Phase 06 / Phase 07 Review）：`vChewing-macOS/Packages/vChewing_LangModelAssembly` 已完成以 `vanguardTextMap` 取代 legacy SQLite，採 full-match-only TextMap 存取層（sorted-array key index + binary search + bounded NSCache）。Phase 07 最終規則為：runtime 與測試路徑都不再接受 external `.revlookup` payload；反查只索引 single-segment `@` 分組行內的 ideographic characters，並無條件納入全部 CNS 條目；輸出會將 ASCII phonabet key 還原為注音。
- 不完全讀音部分比對與多音切割查詢（TrieKit）
- Unigram + Bigram 動態規劃組句（Homa）
- 節點覆寫、候選字輪替、上下文鞏固（Homa）
- Homa rare-case candidate revolver / consolidator 修補（Phase 26）：`CandidatePairWeighted.weight` 明確退回語言模型 metadata 身分，不再被當成 `.withSpecified` explicit override score；`consolidateCandidateCursorContext()` 對 overlap range 改以真實 node range 錨定；`revolveCandidate()` 對 non-explicit 且 target span 改變的首輪情形新增條件式 consolidation，並以「句子確實改變 + 目標候選落到邏輯游標 + 節點成為 explicit」作為成功條件。新增 `JiHuQiKeng` rare-case 與 true-node-anchor regressions。
- 多來源辭典聚合（`LexiconAssembly`；Phase 213 前於本倉名為 `LexiconKit`）
- 使用者習慣 Ngram 洞察與時間衰減（`LexiconAssembly.LXPerceptor`；Phase 213 前於本倉名為 `LexiconKit.Perceptor`）
- 1947 與 2018 盲文標準轉換（BrailleSputnik）
- 下游原廠 VanguardTextMap 非同步載入（Phase 10）：`connectFactoryDictionary` 依 `asyncLoadingUserData` 拆分同步/非同步雙路徑；在當前 final tree 當中，生產環境走 `VanguardTrie.TextMapTrie` 初始化路徑，以避免輸入法載入時 1-2 秒 UI 凍結。跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。
- 下游 factory dictionary completion-driven 載入狀態修補（Phase 40）：`connectFactoryDictionary` 對外改為 completion callback 回報最終載入結果，`LMMgr.connectCoreDB()` 改為先發 Started 通知、再於 completion 成功時發 Complete 通知，消除 async 載入路徑的「提前宣告完成」謊報。該修補已同步鏡像至 vChewing-macOS / vChewing-OSX-Legacy；Legacy 端另依現況以明確 main-queue + `@MainActor` 發送完成通知以滿足 Swift 5.10 actor isolation。
- 下游拼音免聲調長句效能手術（Phase 41）：TrieKit `TextMapTrie` 新增 chopped lookup specialized fast path（避免 protocol default 的笛卡爾積全展開）；`LMInstantiator.mergedAlternativeBucketUnigrams(for:)` 改為先索引原廠候選再做 deferred filter removal，移除展開迴圈中的 repeated full scans；`QueryBuffer.removeExpiredEntriesLocked` 由 `cache.filter` 重建字典改為 in-place 逐鍵移除，直接收斂 `_NativeDictionary.filter` 熱點。三項修補已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`。另依任務要求，worst-case profiling 入口已新增到 `vChewing-macOS` 的 `vChewingDebuggable` target（baseline / worst-case 雙按鈕）。驗證結果為 LibVanguard `swift test --filter TrieKit` 通過、LangModelAssembly `swift test --filter LMInstantiator` 通過、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游拼音免聲調長句效能精修（Phase 42）：TrieKit `TextMapTrie` chopped path 再往 byte-oriented 推進（ASCII initials / prefix/equality 走 UTF-8 bytes，比對遇非 ASCII 自動 fallback string path 保語義）；`exactNodeByReadingKey` 命中後改重用 `getNode(...)` cache；`LMInstantiator` 的 alternatives 展開新增去重與 joined key query-scope cache，收斂 `expandAlternativeKeyArrays` / `joined(separator:)` 殘餘成本；Tekkon `PinyinTrie` 新增 parser-keyed shared cache，`pinyinAutoChopResult` 改走 shared trie，移除連續 keydown 的重建成本。上述修補已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`。驗證結果為 LibVanguard `swift test --filter 'TrieKit|Tekkon'` 通過、LangModelAssembly TextMap/LMInstantiator 回歸測試通過、Tekkon Pinyin/auto-chop 測試通過、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 QueryBuffer NSMutex 漸進清理 + LMInstantiator 單輪 memo（Phase 43）：`QueryBuffer` 全面改為每個 mutable property 各自 `NSMutex` 保護，移除單一 `NSLock` critical section；cleanup 改為時間閘門 + 單輪互斥 + queue 頭段壓縮，避免熱路徑反覆重掃與重建。`LMInstantiator.mergedAlternativeBucketUnigrams` 新增單輪 scope memo（`FactoryLookupMemoKey`）與 `joinedKey` cache，收斂同輪重複 factory query 與 key 串接。已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`。驗證結果為 LibVanguard `swift test --filter TrieKit` 通過、LangModelAssembly `swift test --filter 'LMInstantiatorTextMapTests|TrieKitTextMapTests|TrieKitTests'` 通過、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 `hasUnigramsForFast` 輕量可用性檢查 + 非原廠迴路批量預篩選 + 惰性展開生成器（Phase 44）：**方案 A**：`LMInstantiator` 新增 `hasUnigramsForFast(keyArray:)`，跳過濾除表、語彙置換、InputToken 展開、DateTime、倚天排序等後處理步驟；對含 `&` 的 tone bucket 原廠查詢改走 `factoryChoppedCoreUnigramsFor`（O(1) per keyArray）；user phrases / symbols / cassette 僅做 hash lookup 且 early-exit。`LookupHub.hasGrams(for:)` 與 `Typewriter_BPMFFullMatch` 的 `composeReadingIfReady` / `performPinyinAutoChopIfNeeded` 同步改走 fast path，消除 `hasUnigramsFor` 造成的完整 `unigramsFor → mergedAlternativeBucketUnigrams` 重入。**方案 B**：`mergedAlternativeBucketUnigrams` 迴路前新增 `Set<String>` 批量預篩選，先將所有 expanded key 的 joined string 與 `lmUserPhrases` / `lmUserSymbols` / `lmFiltered` 的 `rangeMap + temporaryMap` keys 做 `Set.intersection`；只有交集內的 key 才執行 `unigramsFor()`，將非原廠迴路從 O(M^N) 降為 O(1)（無 user data 命中時）或 O(K)（K = 實際有資料的 key 數）。**方案 C**：`expandAlternativeKeyArrays` 改為 `AnySequence<[String]>` 惰性生成器（`AlternativeKeyArrayIterator` 以遞增計數器取代遞迴物化），`hasUnigramsForFast` early-exit 時不再生成後續組合，徹底避免 M^N 的 peak heap 壓力。同步修復 `lmCoreEX.hasUnigramsFor(key:)` 未檢查 `temporaryMap` 的 false negative，避免單元測試臨時資料被誤判為不存在。已同步至 `vChewing-macOS`、`vChewing-OSX-Legacy`。驗證結果為 LangModelAssembly 87/87 通過、Typewriter 44/44 通過、Legacy `make debug-core` BUILD SUCCEEDED。
- ~~下游 FactoryTextMapLexicon DFD 硬碟直讀（Phase 11，已作廢）~~：實機測試記憶體飆升 20~30 MB，全數撤銷。失敗根因：pread() 碎片化 heap allocation + 物化讀音鍵字串陣列開銷 + Data(contentsOf:) 隱性 mmap purgeable 優勢被低估。
- 下游 LMCassette（CIN2 磁帶模組）連續記憶體最佳化（Phase 12）：仿照 `FactoryTextMapLexicon` 模式，將 `charDefMap`、`charDefWildcardMap`、`symbolDefMap`、`reverseLookupMap` 四個大型 Dictionary 替換為 `CassetteSortedMap`（contiguous Data blob + sorted byte-range index + binary search）；`octagramMap` 與 `octagramDividedMap` 分別改為 `CassetteOctagramMap` 與 `CassetteOctagramDividedMap`。第二輪修正再將 builder 改為直接接受 grouped Dictionary，並以 lightweight prototype grouping 直接生成 wildcard / reverse lookup，移除 `tmpReverseLookup`、`tmpCharDefWildcard`、`tmpDict.map { ... }` 與 sort comparator 的短命 `Array(rawData[range])` allocation，針對載入期 HEAP 峰值進一步收斂。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。
- 下游 LMCassette 索引壓縮與 wildcard map 去重（Phase 128）：在 Phase 12 的 contiguous-memory 基礎上進一步壓縮索引：所有 offset / count 欄位由 `Int` 改為 `UInt32`（`CassetteMapEntry` 32 bytes → 16 bytes）；移除 `CassetteByteRange`，將 `CassetteSortedMap.allValues` 改為 `[UInt32]` flat array，每個 value 只佔兩個 slot（start、end）。同時刪除獨立的 `charDefWildcardMap` 與 `buildWildcard(from:wildcard:)`，wildcard 查詢改為直接對 `charDefMap` 做前綴掃描，經由新 API `wildcardValuesFor(key:wildcard:)` / `containsWildcardMatch(key:wildcard:)` 即時產生結果，避免載入期複製大量 value 字串；掃描熱徑再以 byte-length 判斷取代 `String` 解碼，避免熱迴圈配置。`LMCassette.open()` / `clear()` / `unigramsFor(key:keyArray:)` / `hasUnigramsFor(key:)` 同步改接新 API；單元測試移除對 `charDefWildcardMap.count` 的斷言，改以 wildcard 查詢結果非空覆蓋語義。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 94/94 通過、root `swift build -c release` 成功、Legacy `make debug` BUILD SUCCEEDED。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0121-0130.md` Phase 128 的 wildcard 效能估算。
- 下游 LMCassette CIN v2.7 花牌鍵與任意單字元鍵分化（Phase 129）：新增 `%anysinglecharkey` 解析（僅首次生效、與 `%wildcardkey` 碰撞時後來者被忽略），並將其與 wildcardKey 一併寫入 `keyNameMap` 以進入 `allowedKeys`。原 `wildcardValuesFor` / `containsWildcardMatch` 改為通用 pattern matcher `patternValuesFor(key:wildcard:anySingleChar:)` / `containsPatternMatch(key:wildcard:anySingleChar:)`：以 UTF-8 bytes token 化 pattern，開頭 wildcard 且後方全 literal 走 anagram（任意字根序）分支，其餘以 literal 前綴 lower-bound 加回溯匹配（wildcard 消秏 1+ 字元、anySingleChar 消秏恰好 1 字元），`y*` 這類查詢因此正確對應 `key count >= 2` 的全部結果。Typewriter 端移除「leading wildcard 視為錯誤」分支，開頭花牌改為合法筆畫；花牌立即組字改以 `calligrapherWasEmpty` 判定；`processCassetteQuickSelection` 新增 functional-key bypass，使 quick-candidate 狀態下的 `Shift+?`（服務選單鍵）在 `?` 對當前磁帶為 functional key（wildcard / anySingleChar / allowed key）時讓位給組筆錄入。`LMInstantiator` 新增 `cassetteAnySingleCharKey`。新增 `testCassetteCIN27WildcardAndAnySingleCharExamples`，直接載入 `array30.cin2` 重現 CIN v2.7 規格範例（「培」以 `r?;` / `ry?` / `?y;` / `r??` 可查、`y*e` 與 `yk*` 的 wildcard 聯集語義、`*ry;` 開頭花牌 anagram 找出 `ry;` 培與 `yr;` 詰）。Typewriter 端另新增 `test_IH105E_CassetteShiftQuestionTypesAnySingleCharKey`（搭配測試組件新增的可指派 `MockCandidateController`）、MainAssembly 端新增 `test108D_InputHandler_CassetteShiftQuestionTypesAnySingleCharKey`（完整 NSEvent → Session → InputHandler 路徑），雙雙鎖定「array30 敲 `y?` 應打出『熟』、不得叫出服務選單」的迴歸情境；兩者均已實測確認停用 bypass 時能精確重現原始故障。注意：bypass 生效的前提是當前磁帶確實將 `?` 定義為 `%anysinglecharkey`（或 `%wildcardkey`），且該定義未被 quick/chardef/symboldef 章節的同名資料鍵失效化；若使用者實機上的磁帶檔（或 LMMgr 快取的磁帶副本）缺少該定義，服務選單攔截 `Shift+?` 屬預期行為。另追加調整：組筆區顯示字根的 fallback 由一律「？」改為花牌鍵「♧」（U+2667）、任意單字元鍵「⍰」（U+2370）——配合 v2.7 語義分化，且刻意選用使用者無法由鍵盤直接敲出的符號；仍僅為 `%keyname` 未定義時的預設值。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 95/95 通過、Typewriter `swift build` 成功、Legacy `make debug` BUILD SUCCEEDED。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0121-0130.md` Phase 129。
- 下游 LMCassette 反查索引零複製化（Phase 130）：廢除 `reverseLookupMap: CassetteSortedMap`（對全部反查 key＋value 的第三份全量複製 blob），改為 `CassetteReverseIndex`（`revChars` 僅收去重後唯一反查字詞 bytes、`revEntries` 16B、`revCodeRefs` 指向 charDef/symbolDef 合併 namespace 的 entry 索引），查詢時依 refs 自來源 map 的既有 `rawData` 按需物化碼字串。建置掃描兩張 map 的全部 entries（含 Phase 128 起混存於 charDefMap 的花牌 pattern keys，與舊版自暫存辭典建置等價）；同字詞多碼順序由非穩定 sort 的未定義順序改為確定性（charDef 先、各依碼 bytes 序），屬已記錄的可接受漂移。採三步差分驗證：新舊並存 → 以 array30.cin2 與 wubi.cin 對每一個反查字詞逐筆比對（全數一致）→ 移除舊欄位與 `buildReverseLookup`、立正式斷言 `testCassetteReverseLookup`（「培」→ `ry;`、symboldef 合併「ㄅ」→ `w0`、nil 案例）。`open()` 於全部 map 建完後即刻釋放六個暫存 Dictionary。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 96/96 通過、Legacy `make debug` BUILD SUCCEEDED。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0121-0130.md` Phase 130。
- 下游 LMCoreEX / LMReplacements / LMAssociates 索引壓縮（Phase 131，Phase 127 手法推廣）：三模組的 Dictionary + `strData: String` 改為 `rawData: [UInt8]` + sorted UInt32 byte-offset entries + binary search。`strData` 對外維持 `String` computed 唯讀屬性（`LMInstantiator.retrieveData`、詞庫編輯器、MainAssembly 測試等既有唯讀消費端零修改——此為對原試作案的必要修訂）。`RangeParserAPI` 新增 `[UInt8]` byte 級解析 helper（`parseByteLines` / `parseByteCells` / `compareByteRange` / `compareByteSlices`），完整複刻 `String.parseRanges` 語義（跳過空片段、dense 索引、提早停止、尾端無分隔符的最後片段照常給出）。LMReplacements：entries 16B/key 直指 rawData，保留「同 key 最後一行覆蓋」語義。LMAssociates：converted-key blob + 16B entry + per-line `(lineStart, lineEnd, valueCellCount)` 12B lineRef（取代 24B tuple），保留 `#` cell 提早停止、雙鍵查詢 + dedup、dictRepresented 不 dedup；並確認 `cnvNGramKeyFromPinyinToPhona` 對純英數 pinyin 不生效屬既有行為。LMCoreEX：converted-key blob + per-line 16B entries（同 key 依檔案行序），`_sortedKeysBox` 惰性排序盒刪除（sorted entries 直接做前綴下界掃描），保留 tab 正規化、replaceData 清空 temporaryMap、第三欄權重與 `#` 壞權重、省略單漢字過濾、saveData 附加 temporaryMap 等全部行為。新增 `LMReplacementsTests`（6 項）與 `LMAssociatesTests`（7 項），`LMCoreEXTests` 既有 6 項原樣保留並追加 strData / saveData round-trip 2 項。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 111/111 通過、Typewriter 88/88 通過、MainAssembly4Darwin 64/64 通過、Legacy `make debug` BUILD SUCCEEDED。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0131-0140.md` Phase 131。
- 下游磁帶模式花牌鍵三明治組合修復（Phase 132）：刪除 `Typewriter_Cassette` 的「組筆區非空時敲花牌鍵立即組字」條件——該條件導致敲 `y*y` 時在 `*` 當下就被迫以 `y*` 組字完畢、三明治 pattern 永遠敲不出來（任意單字元鍵 `?` 自始無此條件）。花牌鍵與任意單字元鍵一律僅作普通筆畫錄入，組字時機與一般筆畫一致（滿筆長自動組字、或空白/Enter 強制組字）；`shouldFormLongestCassetteKey` 移除花牌鍵豁免，花牌敲入後同樣參與最長可能碼判定（`zzzx*` 類無更長可能時仍自動組字，`y*` 因 `y**` 仍有匹配而保留錄入空間）。注意行為變更：敲 `y*` 後需按空白/Enter 組字，與 `y?` 及一般短碼一致。新增 `test_IH105F_CassetteWildcardSandwichStaysInCalligrapher`（`y*` / `y*y` 全程留在組筆區、空白組字後候選含「誰」「譶」），並已做「恢復舊條件→精確重現故障→恢復修復」的迴歸有效性驗證。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 Typewriter 89/89 通過、MainAssembly4Darwin 64/64 通過、Legacy `make debug` BUILD SUCCEEDED。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0131-0140.md` Phase 132。
- Homa Assembler 查詢結果去重與元圖位置唯一性修復（Phase 134）：`queryGrams` 以 keyArray 內容為鍵的快取會將同一批 `[Homa.Gram]`（含相同 FIUUID）原樣分發給不同位置的節點，導致 `generateKeyForPerception` 以 `lastIndex(id ==)` 定位 head 時誤判至最後出現的節點、POM 生成錯誤上下文記憶鍵——「分屬不同 nodes 的 Gram 其 FIUUID 必不同」的假設對快取命中共享的 Gram 並不成立。修法：快取改為純模板角色，節點收治查詢結果時（`assignNodes` 新建節點與 `syncingGrams` 兩條路徑）一律以新增的 `Gram.withNewIdentity()` 物化 FIUUID 全新的節點專屬副本；同時刪除與 instance 級 `gramQueryCache`（bounded 512 + 半量淘汰）功能重疊的 `assignNodes` 區域 `queryBuffer`，`queryGrams(using:)` / `queryGramsForAlternatives(_:)` 移除 `cache:` 參數，`insertKeys` warmup 路徑同步簡化。`assignNodes(updateExisting: Bool)` 改為 `assignNodes(updateBehavior: NodeUpdateBehavior = .fillVacancies)`（`.fillVacancies` / `.refreshExisting`），全部呼叫端（Typewriter `updateUnigramData` ×2 倉庫、HomaTests_Advanced ×2、TrieKit_Tests ×2）已遷移。新增 `Assembler_GramIdentitiesArePositionallyUnique`（相鄰同讀音節點 gram 內容雷同但 FIUUID 集合互不相交、路徑 id 唯一、`refreshExisting` 後仍成立）與 `Assembler_PerceptionKeyGenerationRespectsPositionalIdentity`（「的 的」句中 cursor 0/1 的 head 定位與上下文索引鍵正確）兩項測試，並完成「還原物化 → 兩測試精確失敗 → 恢復修復」的迴歸有效性驗證。修改範圍跨 vChewing-LibVanguard、vChewing-macOS 與 vChewing-OSX-Legacy 三倉庫；驗證結果為 LibVanguard Homa+TrieKit 50/50 通過、macOS Homa 50/50 通過、Typewriter 89/89 通過、MainAssembly4Darwin 64/64 通過、LangModelAssembly 111/111 通過、Legacy `make debug` BUILD SUCCEEDED（三倉 lint/format 後複驗均通過）。詳見 `vChewing-DevLogs/Reqs4LLM/Archive_P101-P200/Reqs_0131-0140.md` Phase 134。
- 下游 LMAssembly 共用 parser / cell tokenizer 最佳化（Phase 13）：未直接將 `lmAssociates` / `lmCoreEX` / `lmReplacements` 全面改為 `Data`，而是先將共用 `RangeParserAPI.parse()` 改為「單一 ASCII separator 走 `String.UTF8View` byte scan、非 ASCII separator 保留 `Character` fallback」，再新增 `parseCells(in:splitee:task:)` 收斂每行 space-separated token 掃描，移除三個 SubLM 內部的 `split(separator: " ")` allocation。`lmCoreEX`、`lmAssociates`、`lmReplacements` 因此都改為直接在 `Range<String.Index>` 上抽取所需 cell；其中 `lmAssociates` 仍維持 `(lineRange, cellIndex)` 索引語義，但改為需要時對該行重掃 cell，不再物化整行 token array。以約 13.7 MB UTF-8 文本測得 `String.parse(Character)` 約 158.9 ms、`String.UTF8View` 約 14.9 ms、純 `Data` byte scan 約 5.1 ms；replaceData profiling 亦指出 LMCoreEX 的下一個熱點是 `split(separator: " ")`、字串物化與 Dictionary 寫入，因此本階段正式把第二刀落在共用 tokenization，而非全面 `Data` 化。驗證期間曾出現 associated phrase 測試誤報，最終確認根因是 MainAssembly / Typewriter 單元測試 `UserDefaults` reset 順序不當造成的 stale `cassetteEnabled` 狀態，亦已一併修正。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。
- 下游 InputHandler 倚天 DOS 候選補缺字修復（Phase 14）：為解決 Issue 580，本輪不動 `LMInstantiator`，而是在 `vChewing-macOS` / `vChewing-OSX-Legacy` 的 `InputHandler_CoreProtocol.generateArrayOfCandidates()` 尾段新增 `segregateCandidatesForETenDOS(from:)` 與 `supplementalETenSingleKanjiCandidates(reading:existingSingleSegments:)`。當 `prefs.enforceETenDOSCandidateSequence` 開啟時維持既有單字重排；關閉時則只在單一讀音的單字候選情形下，查詢倚天中文 DOS 序列，將現有候選中缺席的 ideographic 單漢字以 `-9.5` 權重追加到尾端，避免組句模式比逐字選字模式少字。Review 後追加修正 `segregateCandidatesForETenDOS` 對空 `keyArray` 候選的防禦——`switch` 改為 `keyArray.count` pattern match（`case 1:` / `case 2...:` / `default: break`），空陣列候選不再落入 `singleSegments`。`vChewing-macOS` 另新增兩個最小 TextMap regression tests，以 reversed factory order fixture 驗證 tail-append 與 enforced reorder 兩種行為。驗證結果為 Typewriter package 30 項測試全數通過，Legacy `make debug-core` 建置成功。
- 下游 ButKo BPMFVS 特殊遞交（Phase 15）：`vChewing-macOS` 新增本地 package `ButKo_BPMFVS` 的 `phonic_table_Z.txt` parser、cached lookup table、`convert(value:reading:)` / `convert(value:readings:)` 與 `convertToBPMFVS(smashedPairs:)`，以 ButKo 的讀音順序直接決定 IVS slot：首讀音維持原字、次讀音起附加 `U+E01E0 + slot`。查表前會先把 Typewriter raw key 的後置輕聲（如 `ㄉㄜ˙`）正規化成教材式前置輕聲（`˙ㄉㄜ`），避免與 ButKo 資料格式錯位。`vChewing_Typewriter` 新增 `CommitableMarkupType.bpmfvsAnnotationButKo = 4` 並在 `specifyTextMarkupToCommit(behavior:)` 直接走該 transformer；後續追加 `UserDef.kReflectBPMFVSInCompositionBuffer`，使組字區、選字狀態與候選預覽只在 behavior 4 且開關啟用時才投影成 BPMFVS 顯示，避免污染一般 commit path。`UserDef` / `PrefMgr` / 設定面板 / 四語系文案同步擴充。其後將同一套邏輯同步到 `vChewing-OSX-Legacy`，但資源路徑改用 `Bundle.main`。驗證結果為 `ButKo_BPMFVS` package 6 項測試通過、Typewriter package 32 項測試通過、MainAssembly4Darwin 狀態測試子集 15 項通過、Legacy `make debug-core` 建置成功。Review（Claude Opus 4）通過，無需修正。
- `vChewing-VanguardLexicon` C-based 新酷音辭典純 Swift 生成器（Phase 16）：`chewingCBasedCHS` / `chewingCBasedCHT` 已改由 `ChewingCBasedDatabaseGenerator` 直接生成 `dictionary.dat` 與 `index_tree.dat`，不再依賴 precompiled `libchewing-database-initializer`。生成器本體依掛靠的 `libchewing-sans-tsi` C 原始碼重建 `phone.cin` / `tsi.src` 解析、例外詞條檢查、字串去重與 index tree 序列化流程；其中單字 `char-misc-nonkanji` 類的負詞頻行會直接忽略，避免把 libchewing 無法表達的權重調整語義誤塞進 C-based 產物，而 root node key 仍採 16-bit truncation 以對齊原版 C 行為。驗證結果除 `swift run VCDataBuilder chewingCBasedCHT chewingCBasedCHS` 與 `LibVanguardChewingDataTests` 通過外，亦已將產物回灌至 `libchewing-sans-tsi` 既有工具 / public API 做回讀與開候選 smoke test，確認 C-based libchewing 可正常利用該產物。**Performance follow-up (2026-04-14)**：builder 端改以 reserved `Data` 直接組裝 `tsi.src` / `phone.cin`，generator 端改用 `Substring` 解析、scalar-based word lookup、binary-searched internal children 與 append-only leaf collection，將同機器同語料下 `swift run VCDataBuilder chewingCBasedCHT chewingCBasedCHS` 的總耗時由約 30.56 s 壓到約 16.29 s；兩個 target 的追加建置階段則由 CHT 約 10.28 s / CHS 約 11.19 s 降至 CHT 約 3.54 s / CHS 約 3.50 s。
- `vChewing-VanguardLexicon` HealthCheck duplicate 偵查修復（Phase 17）：確認 duplicate 偵查在 SPM 化後退化的根因是雙重的：舊版 `cook_mac.swift` 的 `value + "\t" + key` duplicate 掃描沒有移植進 `Collector.healthCheckPerMode()`，而 `prepareRawUnweightedUnigramsForPhrases()` 的 `processedPairs` / `handledHashes` 又會先吞掉部分 duplicate raw entries，使單純掃描最終 `getAllUnigrams()` 也會漏報。本輪修復改為在 `Collector_HealthCheck.swift` 直接重讀 raw source assets（`char-kanji-core`、`char-misc-bpmf`、`char-misc-nonkanji`、當前語系所有 `phrases-*`），以與 ingest 相同的 normalization 規則重建 occurrence 清單；只要 `phrase + reading` 相同即視為 duplication，不論 freq/count 是否一致，並將該組所有 occurrence 連同 `source:line` 完整彙總後寫入 `healthCheckException([String])` 再拋出失敗。另新增 2 項 synthetic tests 驗證 grouping 與 report summary。實測目前主語料沒有 duplicate 會觸發失敗，但 HealthCheck 已重新具備在 raw source 層完整列舉並中止建置的能力。**1st Review (Claude Opus 4.6)**：修正 `normalizedLinesForDuplicateScan` 行號偏移 bug（bulk-normalize + filter 後 enumerated index 不等於原始檔案行號），改為逐行正規化並保留原始行號；強化測試資料驗證 comment 行不影響行號追蹤。
- `vChewing-VanguardLexicon` Rust 版新酷音辭典純 Swift 生成器（Phase 18）：`chewingRustCHS` / `chewingRustCHT` 已改由 `ChewingRustDatabaseGenerator` 直接從 `tsi.src` / `word.src` 生成 libchewing-rust 可讀的 `tsi.dat` / `word.dat`，不再依賴外部 `chewing-cli`。生成器本體重建 libchewing-rust `TrieBuilder` 的 syllable `u16` 編碼、BFS trie index layout、DER `CHEW` 封裝、leaf phrase ordering 與 duplicate update semantics。實作過程另確認 `char-misc-nonkanji` 存在單字負詞頻（如 `ば -1 ㄅㄚ`），因此比照 Phase 16 採相容策略：單字負詞頻忽略、多字負詞頻拒絕。驗證結果為 `ChewingRustBuilderTests` 6 項測試通過、`swift run VCDataBuilder chewingRustCHS chewingRustCHT` 建置成功，且產物已回灌至 `libchewing-rust` 倉庫並由 `trieloader` 與 `chewing-cli info/dump` 成功讀取。另確認 `fuzzer/src/bin/fuzzer.rs` 目前在 Rust 2024 edition 下因 `gen` 保留字問題無法編譯，屬下游既存瑕疵，故未作為最終驗證路徑。**1st Review (Claude Opus 4.6)**：逐段比對 Swift 生成器與 Rust `TrieBuilder::write()` / `Syllable` bit layout / DER document spec，確認所有路徑一致（syllable 編碼、arena 結構、BFS serialization、leaf phrase ordering、DER 封裝含 IMPLICIT context-specific tag）。手算 syllable golden values 正確。觀察到 `appendUInt16BE`/`appendUInt32BE` 為 dead throws、`encodeUTF8String`/`encodeInteger` 以 `try?` 靜默吞錯但實務安全。**2nd Review (Claude Opus 4.6)**：追加修正——移除 dead throws 並清除 call site `try`、將 `encodeUTF8String`/`encodeInteger` 改為 properly throwing（fail-fast）、BFS 改為雙陣列層輪替（峰值記憶體 O(最大層寬)）。6/6 測試通過。**Performance follow-up (2026-04-14)**：children insertion/lookup 改為維持 syllable-sorted 陣列 + binary search、leaf duplicate 覆寫改用 `phraseIndicesByLeafID` 索引表、source parsing 改為串流灌入 builder，移除 encode 階段對每個 internal node 的重複排序。以同機器同資料對比 baseline commit `9fee096`，`writeArtifacts` 耗時由 CHS 約 20.31 s / CHT 約 20.60 s 降至 CHS 約 3.27 s / CHT 約 3.35 s，約 6x 改善。
- 下游 BPMFVS commit 洩漏修復與詞庫 Unicode 污染稽核（Phase 19）：修補 `prefs.reflectBPMFVSInCompositionBuffer` 導致 display projection 滲入一般 commit path 的問題，在 `vChewing-macOS` / `vChewing-OSX-Legacy` 新增 `committableDisplayText(...)` 作為統一 raw commit source，將一般 Enter、Space 邊界提交、候選與關聯詞提交、SCPC 選字、reset，以及 state 轉 `.ofEmpty` 的自動提交全部收斂到不含 BPMFVS 投影的原始字串。`vChewing-macOS` 新增 3 項回歸測試，鎖住 plain Enter、empty transition 與 SCPC commit 三條路徑。另對 `vChewing-VanguardLexicon` 執行 `4.3.3..HEAD` 的 components Unicode scalar 稽核，直接掃描 `U+FE00...U+FE0F` 與 `U+E0100...U+E01EF`：結果顯示歷史 diff 沒有任何 Variation Selector，現行 tree 唯一帶 VS 的 component file 是既有且未改動的 `common/data-symbols.txt`（內含 `U+FE0E` / `U+FE0F` emoji/text presentation selectors），因此未發現 `給` vs `給󠇡` 這類 Unicode identity-splitting 詞條污染。**1st Review (Claude Opus 4.6)**：逐一追蹤 Typewriter + Session 層全部 15 條 commit 路徑，確認每條都正確走 `committableDisplayText(...)` 而非 `displayedText`；唯一產出 BPMFVS 的路徑是刻意的 `commissionByCtrlOptionCommandEnter`。Legacy mirror 與所有 3 項 regression tests 通過。觀察到 `InputSession_HandleStates.swift` 的 `.ofEmpty` auto-commit 分支 `?? previous.displayedText` fallback 在理論上可傳回帶 BPMFVS 的文字；維護者據此追加修正，三處（macOS production、Legacy、Mock test session）統一改為 `if let inputHandler` 條件綁定，完全消除理論洩漏路徑。
- 下游 BPMFVS marking state 使用者詞語污染修復（Phase 20）：Phase 19 的 `committableDisplayText` 僅覆蓋 commit 路徑，但 marking state → user dictionary 加詞走的是 `state.displayTextSegments` → `displayedText` → `userPhraseKVPair` 管線，BPMFVS Variation Selectors 直接滲入使用者詞語。修復方式為在 `IMEStateData` 新增 `rawDisplayTextSegments: [String]?` 欄位實作 raw/display 雙軌分離：`displayTextSegments` 繼續持有投影文字供渲染，`rawDisplayTextSegments` 儲存保證未投影的原始字串；`userPhraseKVPair` 改從 `rawDisplayedText`（`rawDisplayTextSegments?.joined() ?? displayedText`）截取標記範圍。Typewriter 端新增 `rawDisplayTextSegmentsIfNeeded` 與 `insertReadingIntoSegments` helper，6 處 state 建構點全數填入 raw segments。同步移植至 `vChewing-OSX-Legacy`。**Review follow-up (GPT-5.4)**：補修 candidate preview 只更新 `displayTextSegments` 卻未同步 `rawDisplayTextSegments` 的一致性漏洞，新增 `test_IH103E_ButKoBPMFVSCandidatePreviewKeepsRawStateInSync`。最終驗證結果為 Typewriter 35/35、LangModelAssembly 55/55、MainAssembly4Darwin 50/50 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 Hotenka 繁簡轉換引擎 v2.0.0 升級（Phase 21）：將 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 Hotenka 繁簡轉換引擎從 SQLite 後端（v1.3.1）升級至 StringMap 後端（v2.0.0）。核心變更：`HotenkaChineseConverter` 移除 `CSQLite3Lib` 依賴，新增 `HotenkaStringMap.swift`（binary-search-based sorted text map）；`convdict.sqlite` 替換為 `convdict.stringmap`；`init(sqliteDir:)` 替換為 `init(stringMapPath:)` throwing initializer。`ChineseConverterBridge` 的 singleton 從直接建構改為 `Optional` + `try?`（graceful degradation on load failure），所有 `convert` call site 改為 optional chaining + `?? string` fallback。`vChewing-macOS` Hotenka Package 升級至 Swift 6.2 + `defaultIsolation(MainActor.self)`，測試從 XCTest 轉換至 Swift Testing（`@Suite struct` + `@Test`）。`vChewing-OSX-Legacy` 的 Xcode project 同步更新：新增 `HotenkaStringMap.swift` source ref、替換 `convdict.sqlite` → `convdict.stringmap` resource ref。驗證結果為 Hotenka 8/8、Typewriter 35/35、LangModelAssembly 55/55、MainAssembly4Darwin 50/50 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 CNS11643 全字庫讀音過濾 UX 優化（Phase 22）：`kFilterNonCNSReadingsForCHTInput` 原先對所有不合規 Unigram 一律 `removeAll` 濾除，導致啟用該選項時「播」（CNS 僅收 ㄅㄛˋ）等常用漢字在 ㄅㄛ 讀音下完全消失。本輪改為：對單一讀音（`keyArray.count == 1`）的不合規 Unigram 以 `-9.5` 權重 demote（建構新 `Megrez.Unigram`，因 `score` 為 `let`），多讀音詞組仍維持濾除。同時為該 UserDef case 新增 `description` 欄位與四語系（en / zh-Hant / zh-Hans / ja）翻譯。既有 `testCNSMask()` 斷言更新為驗證 demote score。**Review follow-up (GPT-5.4)**：補 `test_IH113_FilterNonCNSReadingsStillAllowsSelectingDemotedSingleKanji()` Typewriter regression test，以最小 `bo` TextMap + CNS side entry 驗證 demoted `播` 仍可在候選窗被選取並成功覆寫組字結果。跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。最終驗證結果為 LangModelAssembly 54/54、Typewriter 36/36、MainAssembly4Darwin 50/50 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 BookmarkManager 熱點快取修補（Phase 23）：根據 Instruments 可確認卡頓主因不是 userphrase write flow 本身，而是 `LMMgr.dataFolderPath(isDefaultFolder: false)` / `cassettePath()` 每次都會無條件 `BookmarkManager.shared.loadBookmarks()`，而舊版 `loadBookmarks()` 又會先 `stopAllSecurityScopedAccesses()` 再逐筆 `restoreBookmark(...)`。在 iCloud Drive 路徑與 macOS 26.4.1 下，這段 security-scoped bookmark restore 會放大成 `Security::CodeSigning::KernelCode::identifyGuest` / `_CFBundleCreate` / plist parse 熱點，進而拖慢 `initUserLangModels()` 與 userphrase write。修復方式是在 `Jad_BookmarkManager` 與 legacy copy 內新增 bookmark store signature 快取：當 bookmark 檔案未變且 access 尚在時直接返回；只有在書籤檔更新、access 被 stop、或 override 改變時才重新 restore。`vChewing-macOS` 另新增 `testLoadBookmarksSkipsRestoreWhenBookmarkStoreUnchanged()` regression test。驗證結果為 BookmarkManager 10/10、MainAssembly4Darwin `swift build` 成功、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游沙盒卸除 Fallback UX 設計（Phase 24）：`vChewing-macOS` 與 `vChewing-OSX-Legacy` 在收緊 sandbox 後，已不再嘗試透過 NSOpenPanel 取得 `~/Library/Input Methods/` 目錄授權來自刪；改由 `AppDelegate.selfUninstall()` 顯示指引 NSAlert，並以 Finder 依序揭示使用者詞語資料夾、App Support 父資料夾與目前執行中的 bundle 位置，讓使用者手動完成卸除。CLI `uninstall` / `uninstall --all` 則直接輸出 wiki 與 `uninstall.sh` 提示。**Review follow-up (GPT-5.4)**：發現 GUI alert 與 CLI banner 仍沿用「未能取得目錄存取權限」舊敘述，與本 phase 已證實的 entitlement 根因不符；已修正 macOS / Legacy 兩倉庫 `Uninstaller.swift` 與四語系 `Localizable.strings`，統一改為說明 sandboxed IME 無法自行將自身 bundle 移到垃圾桶，並更正文檔中的 Finder 第三個揭示目標為 runtime `Bundle.main.bundleURL`（通常是 `~/Library/Input Methods/vChewing.app`）。驗證結果為 vChewing-macOS `swift build` 成功、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 iCloud Drive 磁帶書籤持久化失敗 workaround（Phase 25）：iCloud Drive 管理的 `~/Documents`、`~/Desktop` 等目錄中的 CIN2 磁帶檔案的 security-scoped bookmark 無法跨 reboot 持久化（macOS 26.4.1 資安升級加劇，但此行為已被開發者報告約十年）。修復方式為在 `LMMgr` 新增「Import to AppSupport cache」機制：使用者選取 CIN2 檔案時同步複製到 `~/Library/Application Support/vChewing/Cassettes/`（App Sandbox 天然可讀寫，不需 bookmark）；`cassettePath()` 在 bookmark 還原失敗時自動 fallback 至快取副本（私有 `isFileReadable()` 避免 `Broadcaster` side effect flash alert）；`loadCassetteData()` 在外部路徑可用時靜默重新整理快取副本保持最新；`resetCassettePath()` 新增快取檔案清理。所有 UI 入口（SwiftUI drag-drop/fileImporter、Cocoa drag-drop/NSOpenPanel）皆在 `saveBookmark` 後呼叫 `importCassetteFileToCache`。`toggleCassetteMode()` 與 SwiftUI toggle 的可用性判斷收斂為 `cassettePath().isEmpty`（走完整 fallback 解析）。**Review follow-up (GPT-5.4)**：快取目錄名稱統一為 `Cassettes`；`cassetteCacheDirectoryURL` 在 unit tests 下改走 unit-test sandbox，而不是實際 App Support；`resetCassettePath()` 不再誤刪使用者直接指定在 `Cassettes/` 目錄內的來源檔；`vChewing-macOS` 新增 4 項 regression tests。**UX/localization follow-up (GPT-5.4)**：新增 path-aware alert description helper，對 `~/Library/Mobile Documents/com~apple~CloudDocs/` 仍直接視為 iCloud 路徑；但對 `~/Documents` / `~/Desktop` 則改以 `getxattr` 檢查 `com.apple.icloud.desktop` / `com.apple.icloud.documents`，只在 mirror sync 真的開啟時才追加「移到本機位置、必要時可洽詢 Apple Support」提示。這套警告也同步到 macOS / Legacy 的 en / zh-Hant / zh-Hans / ja 設定 prompt，另新增 3 項 regression tests 鎖住 guidance 的顯示條件。**Symbolic link follow-up (GPT-5.4)**：新增 `resolveUserSpecifiedURL(_:)` 專門把 Phase 25 的使用者指定 URL 先做 symbolic link 解碼，再交給 path validity、bookmark、cache import 與 user-data folder 指定流程使用；涵蓋 Cocoa `NSOpenPanel`、Finder drag-drop 與 SwiftUI `fileImporter`，避免 App 只記住 symlink 殼層。另新增 2 項 regression tests 鎖住 symbolic link 檔案與目錄都會 resolve 到來源 URL。複驗結果為 MainAssembly4Darwin 59/59 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游磁帶最長可能碼自動組字、overflow trap、滿碼刪退與 Shift+Backspace 析構修補（Phase 27）：`vChewing-macOS` / `vChewing-OSX-Legacy` 的 `Typewriter_Cassette.handle(...)` 不再於 append 當前按鍵之前就判定 `isStrokesFull` / longest-possible key，而是拆成「pre-existing full buffer overflow」與「post-append auto-combine」兩段，修復 `wubi.cin` 的 `qqqq` 第 4 碼被誤當 overflow、無法直接形成「金」的 regression；同時將 quick-set refresh 後移到 combine 判定之後，且只允許在本次事件真的 append 了磁帶筆畫時才刷新，避免組字事件被提早 `return true` 吃掉，也避免 Backspace/Delete/Shift+Backspace 這類未改動 calligrapher 的事件在 cassette path 尾端被 `renderQuickSetsIfNeeded(...)` 誤吞。cassette 專用 overflow handler 仍負責讓關閉自動組字時的第 5 碼只回報 `2268DD51`，不再漏到 `triageInput` 尾端的 `A9BFF20E` blocked-data trap。這代表 Task 2 的正解不是 editing-key early-return，而是把 quick-set refresh 重新綁回真正的 stroke-append 事件，讓刪鍵自然回到通用 `handleBackSpace(...)` / `handleDelete(...)`，同時保住 Shift+Backspace 的析構語義。`vChewing-macOS` 新增 `test_IH105A_CassetteAutoCompositeWithLongestPossibleKey()`、`test_IH105B_CassetteOverflowDoesNotLeakToBlockedDataTrap()`、`test_IH105C_CassetteBackspaceWorksAtFullCalligrapherLength()`、`test_IH105D_CassetteShiftBackspaceDisassemblesPreviousCalligraph()`，另於 MainAssembly4Darwin 新增 `test108A_InputHandler_CassetteBackspaceShrinksCalligrapher()`、`test108B_InputHandler_CassetteBackspaceWorksAtFullCalligrapherLength()`、`test108C_InputHandler_CassetteShiftBackspaceDisassemblesPreviousCalligraph()`；array30 cassette tests 也已改為透過 `LMATestsData.getCINPath4Tests(...)` 取用 bundle fixture，不再因錯路徑而靜默 skip。驗證結果為 Typewriter 40/40 通過、MainAssembly focused regressions 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 Perception Override / Megrez 低風險演進落地（Phase 32）：依 Phase 31 調研結果，先在 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 落地 P0~P3。Megrez `Compositor` 新增 Assembler 級 `perceptor` 注入，`3_KeyValuePaired.swift` 以 `perceptionHandler ?? perceptor` dual-dispatch 自動送出 `PerceptionIntel`；`LMPerceptionOverride` 將 `kDecayThreshold` 由 `-9.5` 放寬至 `-13.0`，並把 WAL / JSON snapshot / CRC32 compaction 抽離至新 `PerceptionPersistor`，POM 只保留觀測與權重邏輯。`LMInstantiator_POMRepresentable` / `LMMgr_Core` 新增 `pomReducedLifetime` → `reducedLifetime` 注入鏈，移除權重熱路徑 `UserDefaults` 讀取；legacy 端同步移植並將 `lmPerceptionPersistor.swift` 納入 Xcode project。**Review follow-up (GPT-5.4)**：移除 macOS / legacy POM 內殘留的 debug `print`，並將 rapid-forget 測試改為直接驗證 `reducedLifetime` 注入與 `LMInstantiator` 轉發，不再依賴舊的 `UserDefaults` 路徑。驗證結果為 Typewriter 36/36、`POMRapidForgetTests` 3/3、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 Phase 33 Homa transplant / LXPerceptor 對齊（2026-04-20）：`vChewing-macOS` 完成 Megrez → Homa、InputHandler A2 委派化、`LMPerceptionOverride` → `LXPerceptor` canonical rename，並修補 `CandidatePair` 權重語義、Homa revolver / consolidator identity 對位與同分標點保序；`vChewing-LibVanguard` 同步補齊 `LX_Perceptor.reducedLifetime` parity 與 Homa mirror regression。其後 `vChewing-OSX-Legacy` 亦完成對位 transplant，包含 Homa runtime、LMAssembly compat / adapters、Typewriter / MainAssembly bridge、Xcode project 與 Homa 授權文件同步。repo-local dotnet audit 報告為 `Actionable = 0`（37 mirrored / 1 equivalent-noop / 10 no-counterpart / 23 test-only skip），Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 LMCoreEX tab→space 正規化（Phase 70）：使用者直接以 tab 字元編輯語彙檔案（如 `userdata-cht.txt`）時，因 `LMCoreEX.replaceData()` 內部 `parseCells(in: splitee: " ")` 只認 ASCII space；且 `LMConsolidator.consolidate()` 在 pragma header 完好時會 early-return 略過格式整理，導致 `open()` 與非同步 `readFileContentAsync` 兩條載入路徑的 tab→space 正規化都無法被執行，所有 tab 分隔行被靜默丟棄。修法：在 `replaceData()` 最前端加一行 `let rawStrData = rawStrData.replacingOccurrences(of: "\t", with: " ")`，以唯一匯流點確保所有載入路徑都能正確處理 tab 分隔資料。`vChewing-macOS` 新增 `testReplaceDataHandlesTabDelimitedInput` 回歸測試。`vChewing-OSX-Legacy` 同步移植。驗證結果為 LangModelAssembly 107/107 PASS（含新測試），Legacy BUILD SUCCEEDED。
- 下游候選字排序雙重修復 + 原廠辭典重載 LRU cache 自動失效（Phase 77）：Task 1 修復兩個排序缺陷：(a) `LMInstantiator.unigramsFor()` consolidate 後補 `sort { $0.probability > $1.probability }`——使用者片語 reversed 後 prepend 到原廠結果前，consolidate 去重後陣列未按 score 重排，導致低分詞條可能排在高分詞條之前；(b) `Homa.Assembler.fetchCandidates()` 排序 tuple 補第三級鍵 `$0.weight` / `$1.weight`——舊排序僅包含 segLength 與 keyArray，同讀音不同字時分數被徹底無視。Task 2 新增 `static var factoryGeneration` 世代計數器，於 `factoryTrie` setter 內遞增，並將 `unigramsFor()` 的 LRU fingerprint 改為 `config.hashValue ^ factoryGeneration`，確保原廠辭典重載後舊快取自動失效。修改範圍跨 `vChewing-macOS`、`vChewing-LibVanguard`、`vChewing-OSX-Legacy` 三倉庫。驗證結果為 LangModelAssembly 109/109、Typewriter 82/82、Homa 44/44（macOS）、LibVanguard 105/105 全數通過。

---

## 附錄一、開發階段歷史

因該章節過於龐大，故挪至 `vChewing-DevLogs/DevReqsHistory.md` 單獨管理。

---

## 附錄二、已知問題與注意事項

### A2.1 當前已知問題

1. `TrieJoinedTestSuite/testTrieJoinedAssemblyingUsingPartialMatchAndChops`（Phase 213 前於本倉名為 `LXTests4TrieHub/testTrieHubAssemblyingUsingPartialMatchAndChops`）若暫時使用完整 Lexicon 測資，會因語料語義與 Tiny Sample 不同而出現 sentence expectation failure；目前應將 benchmark 視為有效信號、將 assertion mismatch 視為已知暫時狀態。
2. **CI（linux／WinNT）測試行程硬崩（`Illegal instruction`／`c000001d`）——`mainSync` 對主佇列之遞迴 `sync`（2026-09-16 查明；已修於 P219）**：自 **`95a609c`**（2026-09-15，該筆把 CI 之 Swift 版自 `latest`＝當時的 6.3.3 抬到 **6.4**）起，兩條 workflow 之 `swift test --no-parallel` 每次都在同一處崩潰、且恰為兩個測試靶各一次（`LibVanguardTests`／`LexiconAssemblyTests`）。**堆疊**（WinNT 版有符號）：`InputSession.deinit` → `String.mainSync` → `DispatchQueue.main.sync` → `__DISPATCH_WAIT_FOR_QUEUE__` → libdispatch 之 client crash（`src/queue.c:1646`「`dispatch_sync called on queue already owned by current thread`」；Darwin 編成 SIGTRAP、Linux／WinNT 編成 ud2＝SIGILL）；另一條為 `LXAssociates.saveData()` → `withFileHandleQueueSync` 內之 `mainSync`。**機理**：`mainSync` 以 `Thread.isMainThread` 判斷「是否已在主執行緒」——在 Linux／WinNT 上主佇列之工作是由 dispatch worker 執行（該執行緒 `Thread.isMainThread` 為假、卻已持有主佇列之 drain 鎖），於是走 `DispatchQueue.main.sync` 慢路徑而遞迴 `sync`。**macOS 不受影響**（主佇列跑在主執行緒、走就地執行之快速路徑；本機同款 `test202_InputHandler_EscapeBehaviorVariants` 綠燈）。**同期 macOS 那條 CI 之紅燈與此無關**：`setup-xcode` 找不到 Xcode 27.0（runner 映像只到 26.6）。**已落地之修法（P219，2026-09-16；兩倉 byte-sync）**：① `mainSync`（`SwiftExtension/SwiftFoundationImpl.swift`）改以「註冊在 `DispatchQueue.main` 上之 `DispatchSpecificKey`」判斷是否已在主佇列（`getSpecific` 問的是「當前正在執行的佇列」、非執行緒身分，故兩平台通用）——命中則就地執行；② `withFileHandleQueueSync`（`LexiconAssembly/vChewingLXAssembly_Common.swift`）同法加掛 `fileHandleQueueKey` 之判斷。**兩項 6.2+ 側之實作約束**：該判斷函式須標 `nonisolated`（否則自身即成為 MainActor 隔離、反而無法自 `nonisolated` 的 `mainSync` 呼叫），且該 `DispatchSpecificKey` 與其註冊動作須收進**函式內之區域型別**——置於檔案層級會被 `-default-isolation MainActor` 收歸 MainActor，`nonisolated(unsafe)` 雖可編過、但 6.4 會對它誤發「對 Sendable 常數為多餘」之診斷（照該診斷移除即編不過）。**本機驗收**：6.4 側 **535 項 0 失敗**、5.10 側（兩倉）與 macOS 側皆 `Build complete!`；`vChewing-OSX-legacy` 之同構同步（`mainSync` 改走新增之 `isOnMainQueue()`、`withFileHandleQueueSync` 之檔案本已符合、僅補 doc comment）經 `make debug-core` `** BUILD SUCCEEDED **`。**未驗**：Linux／WinNT 之止崩須推一次 CI 方能確認。**仍未辦**：③ `InputSession.deinit`（本體僅一行 log）與 `Debouncer.deinit`（僅動 lock 保護之狀態）不再 hop 至主佇列——此僅為先前提議、未採納為 P219 之範圍。

   **第二輪（2026-09-16，CI 複驗後）**：上述修法只解了一半——`InputSession.deinit` 那條已止（linux／WinNT 之 `LibVanguardTests` 各 **60 項全綠**），惟 `LexiconAssemblyTests.testSaveDataRoundTrip` 仍崩、兩平台各一次。**真因**（WinNT 符號化堆疊：`testSaveDataRoundTrip` → `LXAssociates.saveData()` → `withFileHandleQueueSync` → `closure #1` → `mainSync` → `DispatchQueue.main.sync` → `_dispatch_sync_f_slow` → `__DISPATCH_WAIT_FOR_QUEUE__`）：`fileHandleQueue.sync` 於未受競爭時**就地在呼叫端執行**（libdispatch 之 `_dispatch_sync_f_fast`），而該呼叫端（`@MainActor` 之測試本體）**正在主佇列上**；於是內層 `mainSync` 眼中「當前正在執行的佇列」已被壓成 `fileHandleQueue`——`Thread.isMainThread` 為假、`DispatchQueue.getSpecific(key: mainQueueSpecificKey)` 亦為 `nil`（**它問的是當前佇列**），兩者皆不命中，遂對主佇列 `sync`，而主佇列之 drain 鎖正由本執行緒持有。**教訓（通用）**：`getSpecific` 這一招問得出「當前佇列是否為主佇列」，卻**問不出「本執行緒是否代持著主佇列」**；凡「先 `Q.sync` 進某佇列、再於其內 `mainSync` 回主佇列」之寫法，都必須在**進入 `Q.sync` 之前**擋掉在主佇列上的呼叫方——補在內層無效。**修法**：① `isOnMainQueue()` 由 `private` 改 `public nonisolated func`；② `withFileHandleQueueSync` 之第一關由在 corelibs 上形同虛設的 `if Thread.isMainThread` 換成 `if isOnMainQueue() { return try execute() }`（macOS 側行為不變）。`withFileHandleQueueAsync`／`readFileContentAsync` 走 async、block 必落別的 worker，無此互鎖，不動。`vChewing-OSX-legacy` 之 `withFileHandleQueueSync` 不 hop `mainSync`、亦無此互鎖，只對齊 `isOnMainQueue()` 之存取級別並在 doc 註明「本倉不應加掛那一關」。**本機驗收**：LibVanguard 6.4 側 **535 項 0 失敗**；三倉建置與格式管線皆過。**未驗**：本機無法複現（Darwin 上主佇列恆在主執行緒），故止崩只能靠 CI 判準——第二輪推上去後（run `35087157715`＝linux／`35087157691`＝WinNT，同 SHA `2b5b360a`）**兩平台全綠、`Program crashed` 計 0 次**（9 靶共 534 項；此前必崩之 `LexiconAssemblyTests` 為 193 項 30 套全綠）。**本項自此結清。** 附帶之同批改動：`TekkonTests` 之 `"[Tekkon] StringProcessingPerformance"` 門檻自 0.1 放寬至 0.2（`vChewing-LibVanguard` `caeaccc`／`vChewing-macOS` `3d7938a6`）——實測散佈：本機 0.016s／linux runner 0.034s／WinNT runner **0.1035 → 0.0560 s**（同一 runner 兩輪相差 1.85×），故 0.2 對最慢觀測值僅約 1.9× 餘裕、與 runner 自身抖動同量級；再遇 flake 宜升至 0.3。

### A2.2 開發注意事項

1. 始終保持對 Linux 與 Windows 的可建置性（透過 `#if canImport(Darwin)` 處理平台差異）。
2. TrieKit 不計畫直接支援 Regex Fuzzy Match；不完全讀音檢索需先經 Tekkon 的 `chop()` 拆解。
3. Homa 引擎取消了舊版 `LangModelProtocol` 協定，改用回調函式。
4. `LexiconAssembly` 不負責查詢結果的去重複化，該工作由 Homa 引擎完成。（Phase 213 前於本倉名為 `LexiconKit`。）
5. 下游 `vChewing_LangModelAssembly` 目前的預設 factory lookup 已由 `FactoryCoreLookupStrategy.configuredLookup` 表達；當 `partialMatchEnabled` 開啟時，這條預設路徑可走 partial-match path。`FactoryCoreLookupStrategy.strictSuperset` 則是獨立的 strict-superset strategy，不得將兩者混為一談。另在 plain BPMF / 倚天 DOS 與 cassette quick-result facade 上，`SupplementalLookupStrategy` 已明確區分 `.configuredLookup` / `.exactMatch` / `.partialMatch`；其中 `configuredLookup` 保留各 backend default，不等於強制 exact，也不等於強制 partial。Phase 36 結束後，repo 內唯一 canonical public lookup surface 已是 `LMInstantiator.lookupHub`；舊 `query*` / `cassetteQuickSetsFor` wrappers 已自 current tree 移除。若未來再做類似 breaking cleanup，請直接以 `Phase036_Research.md` appendix 內的 P5 gate 與 kickoff template 為範本。
6. **Phase 46 後 `&` 字串編碼已從 Homa ↔ LMInstantiator 介面移除**。`Homa.Assembler` 現以 `[PossibleKey]` 儲存每個位置的可能讀音，並在 `assignNodes` 內部以笛卡爾積展開為精確 `[String]` 陣列後呼叫 `gramQuerier`。Typewriter 不再產生 `&` 字串；TrieKit `keysChopped:` 仍保留內部 `&` 處理供自身 partial-match 路徑使用，但 Homa 傳遞至 `LMInstantiator` 的皆為精確陣列。
6. 下游測試 fixture 的 typeID 必須與 VanguardLexicon 建置器的實際 typeID 分配一致（Phase 09 教訓：`_punctuation_list` 等 `_` 前綴 key 在 production 為 typeID=4，fixture 誤標為 5/6 導致漏洞）。
7. 不要再把 Phase 35 理解成「回退到下游 `FactoryTextMapLexicon`」。那條路徑只屬中間驗證；最終 source of truth 是 canonical `VanguardTrie.TextMapTrie` 與 `VanguardTrieProtocol.getEntryGroups(...)`。

---

## 九、關鍵檔案速查

| 檔案路徑 | 說明 |
|----------|------|
| `Package.swift` | SPM 套件定義（package name `LibVanguard`、出貨 product `Vanguard`） |
| `makefile` | `make lint` / `make format` / `make lintFormat` / `make lintFormatUncommitted` / `make test` / `make dockertest` |
| `EVOLUTION_MEMO.md` | 各模組研發備忘錄（人工維護） |
| `.github/workflows/test_{ubuntu,winnt,darwin}_LibVanguard.yml` | CI（Phase 213 新增；倉根直接 `swift test --no-parallel`） |
| `Sources/Homa/Homa_MainComponents/Homa_Assembler.swift` | 組字核心引擎（Phase 05：跨插入 gram query cache / 熱路徑去 allocation） |
| `Sources/Homa/Homa_MainComponents/Homa_PathFinder.swift` | 動態規劃路徑搜尋 |
| `Sources/Tekkon/Tekkon_SyllableComposer.swift` | 音節合成引擎 |
| `Sources/TrieKit/VanguardTrie_Core.swift` | Trie 核心資料結構 |
| `Sources/TrieKit/TrieProtocol.swift` | Trie 共用查詢邏輯（Phase 35：`getEntryGroups(...)` / existential fast path） |
| `Sources/TrieKit/TrieTextMap_Core.swift` | TextMapTrie canonical specialized backend（Phase 35） |
| `Sources/TrieKit/VanguardTrieIO.swift` | Trie IO（Vanguard Pragma TextMap 序列化/反序列化 + 結構驗證） |
| `Sources/TrieKit/TK_QueryBuffer.swift` | 7 秒動態清理查詢快取 |
| `Sources/LexiconAssembly/LXFacade.swift` | 辭典查詢門面（原 `LMInstantiator`／`LMI`） |
| `Sources/LexiconAssembly/LX_GramConcatAPI.swift` | 元圖查詢結果聚合（構造化排序 / 具型雜湊） |
| `Sources/LexiconAssembly/LXGramSupplyHub.swift` | 多來源元圖供應中樞（Phase 212） |
| `Sources/LexiconAssembly/SubLMs/LXPerceptor.swift` | 使用者習慣洞察器（POM；原 `Perceptor`） |
| `Sources/LibVanguard/` | 作業系統中立層（輸入控制器與狀態機；原 `OSNeutralAssembly`） |
| `Tests/LexiconAssemblyTests/` | POM 測試族之大本營（含本倉獨有之 `POMDecayWindowTests.swift`／`LXPlainBopomofoEtenDOSTests.swift`） |

---

## 十、建置與開發

### 10.1 建置要求

- Xcode 16.0+（CI 目前 pin `27.0`；5.10 legacy 側另需 Xcode 15 的 `MacOSX13.3.sdk`，見 §10.5）
- **Swift 6.4+**——**6.2／6.3 為封堵對象**：各套件皆備 `Package@swift-6.2.swift`／`6.3.swift` 兩份 `#error` 封堵檔，該兩版 toolchain 會被明文拒絕（理由見 §10.5 末段與 `Phase216_PostResearch.md` §13.6）
- 出貨 runtime 目標 **macOS 12+**（`platforms: [.macOS(.v12)]`；5.10 legacy 側為 `x86_64-apple-macosx10.9` 之靜態 `.a`）

### 10.2 建置指令

```bash
swift build
```

### 10.3 測試指令

```bash
make test          # Release 模式測試
make test-debug    # Debug 模式測試
make dockertest    # Docker 容器測試（Linux）
```

### 10.4 程式碼格式化

```bash
make lintFormatUncommitted  # 交差／commit 前慣用：僅對 tracked 且未 commit 的 Swift 檔依序執行 lint＋format
make lintFormat             # 全倉：SwiftLint 自動修正（先）＋ SwiftFormat（後）
make lint                   # 僅 SwiftLint 自動修正
make format                 # 僅 SwiftFormat（縮排 2 空格）
```

> 交差前「一次性運行」 `make lintFormatUncommitted` 即可（先 lint 後 format 的順序已內建）。注意 lint+format 可能將 `count == 0` 轉為 `.isEmpty`，需確認對應型別確有此屬性，否則改用 `.count * 1 == 0`。

### 10.5 雙 toolchain 建置（Swift 5.10 legacy 側；Phase 216 起、**Phase 217 擴及全倉**）

本倉**同一份 `Sources/`** 同時具備兩套建置能力：6.4+ 側即 §10.2／§10.3 之 `swift build`／`make test`（產物 `libVanguard.dylib`）；5.10 側供 macOS 10.9 之 legacy 靜態庫，走下列目標（產物 `libVanguard.a`／`libVanguardSwiftExtension.a`）：

```bash
make build510               # 聚合體：libVanguard.a（14 MB）
make build510SwiftExtension # 只建置巢狀子套件（SwiftExtension 模組本身）：libVanguardSwiftExtension.a（540 KB）
make clean510               # 清兩條 scratch
```

**三項環境配對缺一不可**（`makefile` 內之 `LEGACY_*`，可用 `?=` 覆蓋）：

| 項 | 值 |
|---|---|
| toolchain | open-source `swift-5.10.1-RELEASE` toolchain（`LEGACY_TOOLCHAIN`） |
| SDK | Xcode 15 的 `MacOSX13.3.sdk`（`LEGACY_SDK`） |
| 部署目標 | `x86_64-apple-macosx10.9`（`LEGACY_TRIPLE`）；另 scratch 分兩條：`.build/.legacy`（聚合體）與 `.build/.legacy-swiftExtension`（子套件） |

**兩個硬事實**：

1. **Xcode 27 的 SDK 對 5.10 不可用**——`MacOSX27.0.sdk` 配 5.10.1 toolchain 會得到 312 個掩蓋式錯誤（`unknown argument: '-target-arch-variant'`、`could not build module 'Darwin'`），且 `swift package dump-package` 在該 SDK 下**照樣成功**——故「manifest 載得動」不足以證明該 SDK 可用。
2. **部署目標之有效手段**——SwiftPM 會吞掉你想壓低的部署目標（`--triple` 之版本段被丟掉、`platforms:` 亦被抬到 toolchain 地板），唯一有效之手段是 `-Xswiftc -target -Xswiftc x86_64-apple-macosx10.9`。早前之「`10.9` 不可用、因 `Data` 標為 macOS 10.10 起」一說已經重測推翻（2026-09-16），見 §十。

> 5.10 側之 manifest（`Package@swift-5.10.swift`——Phase 216 先在聚合體與子套件各鋪一份，**Phase 217 起 `vChewing-macOS` 之 19 個 subpackage 各一份**）刻意**不宣告測試靶**、**不收 `vChewingSharedCLI`**、**`platforms: nil`**，產物一律 static；`Package@swift-6.0.swift`／`6.1.swift`（自 Phase 217 起另含 **`6.2.swift`／`6.3.swift`**）為 `#error` 封堵檔（令各該版 toolchain 明文拒絕建置）。擇定規則與其餘坑見 `vChewing-DevLogs/Research/Phase216_PostResearch.md` 與 `Phase217_SOP.md`。

#### 10.5.1 Phase 217：`vChewing-macOS` 全倉（19 個目錄）的逐包入口

**每個 subpackage 都自成一個可單獨驗證的單位**——各自六份 manifest（`Package.swift`@6.4 ＋ `Package@swift-5.10／6.0／6.1／6.2／6.3`）與**各一份 `makefile`**：

| 入口 | 用途 |
|---|---|
| `cd Packages/<name> && make build510` | **單包深挖**（產物落該包自己的 `.build/.legacy`；`make clean510` 清之） |
| `cd Packages && make build510-all` | **全倉批次**：逐包跑、個別失敗不中斷、逐包列 ✓／✗（`make clean510-all` 清全部分支） |
| `make build510-<目錄名>`（倉根 `Makefile`） | 同上的單包快捷；`make build510-swiftExtension` 走巢狀子套件 |

**性質**：這是**可建置性驗證（compilation test）**，**不是單元測試**——5.10 側的 manifest 與 makefile **刻意不含任何測試靶／測試目標**；測試一律在 6.4 側跑（§10.3）。

**5.10 側的原始碼層原則（P217 貫徹）**：SwiftUI 之任何內容——**含 `#Preview` 這類巨集**——一律整段圈進 `#if compiler(>=6.2)`；`nonisolated` 自 extension／型別標頭**下放到成員**；availability 用 `if #unavailable() {} else {}` 兩分支；`nonisolated deinit` 改樸素 `deinit`（內容以 `mainSync` 裹上）；`@Sendable` 閉包內不得碰 MainActor 成員（legacy 形狀為 `DispatchQueue.main.async { @MainActor in … }`）；**不得為消錯新增 `@MainActor`**（會殺死 ≤ macOS 10.14 的執行期相容性）。

**終點與驗收**：兩支終點產物 **`libInstallerAssembly4Darwin.a`（3.2 MB）** 與 **`libMainAssembly4Darwin.a`（26 MB）**（`file` → `current ar archive`、`vtool` → `not mach-o`，即不帶任何 load command、minOS 不進產物）；`make build510-all` 於 2026-09-15 達 **19／19 rc=0、0 error**，6.4 側 `swift build` rc=0＋**535 項測試 0 失敗**。

**現值一項例外（2026-09-16）**：`vChewing_MainAssembly4Darwin` 之 5.10 manifest 依事主指示**改回與 `Package.swift` 同構**（收遠端 `vChewing-VanguardLexicon` 依賴與兩支 build plugin），故**該包於 5.10 側復歸 rc≠0、全倉 18／19**——5.10 SwiftPM 對非根套件的 `unsafeFlags` 一律硬拒（該 lexicon 的 `CSQLite3` 靶帶 `.unsafeFlags(["-w"])`），`--disable-sandbox` 無效、改本地 `.package(path:)` 則撞 macOS 10.15 平台下限；**正解在 lexicon 倉**（見 `Phase217_SOP.md` §十四 **N4**）。

**本項已由 P218 接管（2026-09-16）**：lexicon 之 deploy 工具真正用上 Swift Concurrency（故其 manifest 須宣告 macOS 10.15），與本側 5.10 之 10.13 地板相衝；現行作法是 5.10 manifest 不再收該依賴與兩支 plugin（`make build510-all` 因而復歸 19／19 rc=0），「另尋資料注入他法」移入 P218——原因、實測與後續見 P218 之紀錄。（此補註同時取代上方「現值一項例外」所述之狀態。）

**Swift 6.2／6.3 之封堵（Phase 216 起、Phase 217 補齊）**：五個位置各鋪兩份 `#error` 封堵檔（兩倉 12 組 manifest 逐位元組相同），`Package.swift` 之 tools version 抬到 **6.4**，三系統 CI 之 toolchain pin 同批抬到 ≥6.4。**封堵訊息之正本模板**見 `Phase217_SOP.md` §十二 R22 末段（46 份封堵檔同構）。**測試紀律**：`swift test --no-parallel`（CLI 層）與 `@Suite(…, .serialized)`（型別層）**缺一不可**，且單元測試下 `LXFacade.asyncLoadingUserData` 恆為 `false`——見同 SOP §十二 R23。

#### 10.5.2 雙 toolchain 之終點推進（Phase 218 起）

5.10 側自此**真的產出可交付的執行檔**：倉根 `Package@swift-5.10.swift` 出 `vChewing` 與 `vChewingInstallerLegacy`，並以 `-Xlinker -force_load -Xlinker ./LegacyZone/ARCLite/libarclite_macosx.a` 摻入 libArcLite（`-L` ＋ `-larclite_macosx` 實測為虛——`ld` 逕以 SDK 版 libobjc 之匯出滿足符號、**不抽任何 arclite 成員**；swift driver 亦不認裸 `-force_load`）。執行期採**兩段式 rpath**：記錄保持 `@rpath/libswift*.dylib`，由 `BundleAppsLegacy` plugin 把 Xcode 內 `usr/lib/swift-5.0|5.5/macosx` 之 back-deployment 副本（21 支）放進 `<build-dir>/Frameworks/` 並追加 `@loader_path/Frameworks`——現行 macOS 之 `/usr/lib/swift/*` 係由 dyld shared cache 供貨故系統執行期勝出，10.9–10.13 才落到內嵌副本（以 `install_name_tool -change` 改寫記錄的舊作法則會強制載入 10.9 副本而在現行系統 abort）。建置入口 `make debugLegacy`／`cleanLegacy`（scratch `.build/.legacy-root`）：`DEVELOPER_DIR` 須 pin Xcode 15，C／ObjC 靶另須 `-Xcc -target -Xcc x86_64-apple-macosx10.9`（`-Xcc -mmacosx-version-min` 會被 SwiftPM 自己那條 `-target …10.13` 壓住）。lexicon 因**真正用上 Swift Concurrency**（deploy 工具需 macOS 10.15）而無法被 5.10 側消費，故 `MainAssembly4Darwin` 之 5.10 manifest 不收它（資料注入之替代方案未決）。**2026-09-16 追加（第三輪）**：入口更名為 `debugLegacy`／`cleanLegacy`（並另立 `releaseLegacy`），`bundle510-root` 隨之更名 `bundleLegacy`；`releaseLegacy` 出 **universal**（`x86_64-apple-macosx10.9` ＋ `arm64-apple-macosx11.0`，逐支 `lipo`），libArcLite 之 link flag 因 arm64 支線不能摻而**自 manifest 移交倉根 `makefile`**（逐 triple 下）。為此修掉兩處阻礙：① Swift 5.10 `-O` 之 SIL verifier 崩潰（`CGRect.zeroValue` 為跨模組常數，序列化為 `$Double` 而消費端欄位為 `$CGFloat`）——落點 `AppKitImpl_Misc.swift` 之 `textKitSize(for:)`，該 null 分支本即冗餘、刪之；② `BundleAppsLegacy` 原把 `/usr/lib/swift/…` 之絕對記錄也當成自己的債（universal 之兩 slice 對 `libswiftUniformTypeIdentifiers` 的 weak 標記不一致，遂誤判為致命）——改按記錄性質分流（`.rpath`／`.system`／`.stale`）。**第四輪追加（2026-09-16）**：① **`CGRect` 常數下放**——`SwiftExtension` 的 `zeroValue`／`seniorTheBeast` 兩個跨模組 `static let` 刪除、改由各消費檔在檔案底部以 `fileprivate` 自行定義（呼叫端寫法不動）；跨模組內聯的 SIL 型別不符自此無從發生，`-O` 崩潰由「繞開」升為「修好」，`vChewing-OSX-legacy` 之同構一行亦鏡射修正（其 `SwiftExtension` 是同 target 來源檔、無跨模組問題，常數保留）。② **concurrency 清剿至零**——動工前逐 archive ＋ 逐 object 點名，得全倉唯一漏點是 `SwiftFoundationImpl.swift.o`（5.10 側 `VanguardSwiftExtension` 係 static，其 object 被併進每一個消費者 archive）；三處拆解：`asyncOnMain` 之 `<6.2` 分支去 `Task` 改走 GCD、`OtherIMEDataReader` 的 `AsyncGramSequence`（`next() async`）圈進 `#if compiler(>=6.2)`、兩支「只為 async 而 async」的 `asyncInit()` 同樣圈起來。結果：兩 slice 之 **20／20 archive** 與兩支執行檔之 concurrency 引用皆為 **0**，`@rpath/libswift_Concurrency.dylib` 不再是任何載入記錄——10.12 以前的系統在**連結層面**就與 concurrency 執行期無關（`BundleAppsLegacy` 仍整批備齊該副本，屬未被引用的死重；**事主 2026-09-16 裁定不用管**）。③ **`BundleAppsLegacy` 清 foreign rpath**——新增 `pruneForeignRPATHs(of:)`，把建置機 toolchain 之絕對 `LC_RPATH`（`…/swift-5.10.1-RELEASE.xctoolchain/usr/lib/swift-5.5/macosx`）以 `install_name_tool -delete_rpath` 清掉，`/usr/lib/swift`、`@loader_path*`、`@executable_path/../Frameworks` 一律保留；universal 兩 slice 皆清、驗章照常。④ **`CGRect` 常數同步進 `vChewing-OSX-legacy`**（事主：「要同步的也就 `CGRect` extensions per file 而已」）——該倉 `SwiftExtension` 的 `seniorTheBeast`／`zeroValue` 整塊刪除、`seniorTheBeast` 改由 `LibVanguard/Session/{SessionProtocol,InputSession_HandleDisplay}.swift` 與 `PopupCompositionBuffer/PopupCompositionBuffer.swift` 各自 `fileprivate` 定義（`zeroValue` 零消費端逕刪），前兩檔自此與 macOS 側逐位元組相同；`make debug-core` 通過。⑤ **真機實跑（本 phase 唯一的執行期證據）**：legacy `.app` 於**現行 macOS** 上實跑、可正常打字且**吃得到 lexicon assets**——即 `LegacyZone/LexiconBuildTrigger` ＋ `MainAssembly4Darwin_MainAssembly4Darwin.bundle` 之路線由實跑證實有效，P218 未決 #1 隨之結案；`debugLegacy` 之打字遲滯係 `-Onone` 之預期表現（`releaseLegacy` 存在之理由），非缺陷。**仍未驗**：**10.9 之真機實跑已於 2026-09-16 完成（輸入法本體正常、安裝程式之崩潰見第五輪追加與 `Reqs_0211-0220.md` Phase 218〈十九〉，修後待複驗）**；10.10／11.x 仍未驗（其餘 back-deployment 與 concurrency 結論皆為靜態量測）。`project.pbxproj` 事主明示**不動**。**追加任務（2026-09-16）——legacy 安裝程式之在地化名稱**：兩份安裝程式原本共用同一份 `Sources/Installer_macOS/Resources/<lproj>/`，故 legacy 產物在 Finder 裡與主線**完全同名**（實測 `URL.localizedName` 皆為 `vChewing Installer`）。解法照 `vChewing-OSX-Legacy` 之參照機制：`LSHasLocalizedDisplayName`（兩側 `Info.plist` 本已帶）＋ 各 `<lproj>/InfoPlist.strings` 的不同 `CFBundleName`。新增 `LegacyZone/InstallerLocalizations/<lproj>/InfoPlist.strings`（每檔只放該一鍵，值照抄參照倉），`BundleAppsLegacy` 於複製 lproj 後 **merge**（非取代——`CFEULAContent` 必須存續；舊式 `.strings` 以 `PropertyListSerialization` 之 `openStep` 解析、寫回 XML）。四語系名自此與參照實作**逐語系相同**，主線則一字未變。**落點為何是 `LegacyZone/` 而非 `Sources/Installer_macOS/Resources-Legacy/`**：後者在 `vChewing.xcodeproj` 裡是 `PBXFileSystemSynchronizedRootGroup`，未被 `membershipExceptions` 排除者一律自動成為 target 成員，故新目錄會被 Xcode 掃進**現代**安裝程式的 copy-resources phase（輕則 `Multiple commands produce`、重則改掉主線顯示名）；`LegacyZone/` 在 pbxproj 中零引用，落此對 Xcode 不可見，兩份 `Package*.swift` 因此**無須改動**。另記：事主實測 legacy 產物之總記憶體佔用**降至 macOS 15 Sequoia／Intel Mac 之水位**（開著偏好設定視窗亦僅 80 餘 MB），機理未查。**第五輪追加（2026-09-16，真機 macOS 10.9 實測回饋）**：① **10.9 上按「安裝」數秒即崩**——`stopInstallRetryTimer()` 的 `timer.setEventHandler(handler: nil)` 被本路徑內建的 Swift 5.0 back-deployment Dispatch shim 原樣轉成 `dispatch_source_set_event_handler(source, NULL)`，而 10.9 的 libdispatch 收到 NULL block 直接 BUG（`BUG IN CLIENT OF LIBDISPATCH: NULL was passed where a block should have been`、`EXC_BAD_INSTRUCTION (SIGILL)`）。**證據**：崩潰堆疊（`_dispatch_Block_copy` ← `dispatch_source_set_event_handler` ← shim 的 `setEventHandler…FTm + 535` ← `installInputMethod`）、shim 之反組譯（**先無條件 `_Block_copy(handler)` 才做版本判斷**，nil 一路放行到 C API）、以及現代系統之對照實驗（同款 Swift 5.10 小程式在現行 macOS 上傳 nil **不崩**——現代 libdispatch 容忍 NULL，故 6.4 側從未顯露）。修法＝改傳空 block `{}`（語意等價、兩邊都吃得下），**兩倉同步**（`vChewing-OSX-legacy` 同構一行亦鏡射）。此路徑的輸入法本體在 10.9.5 上**運作正常**。② **辭典資產自動建置**——倉根 `Makefile` 新增 `lexiconLegacy` 目標並使之成為 `bundleLegacy` 之前置，`debugLegacy`／`releaseLegacy` 一併自動滿足（原先得手動先跑一次，否則 plugin 在長建置末端才報錯）。③ **辭典建置走 release**——`LegacyZone/LexiconBuildTrigger` 之 config 變數改用 `LEXICON_CONFIG`（預設 `release`，冷啟 `Build complete! (48.70 s)`／wall 50.24 s）；刻意**不**叫 `LEGACY_CONFIG`（後者指 *app* 之 config，且以 target-specific 變數外洩至 sub-make——實測首輪驗證即因此跑成 debug）。④ **環境事實兩則**：Xcode 之套件解析**不吃**外部（OpenSource）toolchain（固定用自己內建的那條），故 Intel Mac（Xcode ≤ 26.x、內建 Swift < 6.4）**已不能以 Xcode 建置本倉**，只能走 SwiftPM CLI；`MacOSX13.3.sdk` **非 Xcode 15 內建**（Xcode 15 自帶 macOS 14 系），係 CLT 那份的 **symlink**（`ld` 之警告把 SDK 路徑印成 CLT 側即其旁證），換機重建須照樣處置。**commit**：崩潰修補＝`b48b07b5`（macOS）／`b48e1c4f`（legacy 對位），倉根 `Makefile` 與 lexicon 之建置改動併入 `5793727b`、README 併入 `c7812115`。**本 phase 已於 2026-09-16 由事主宣告完結**，末輪為兩倉 `README.md` 之建置環境交代（macOS 側：非 macOS 27 上以 6.4+ 編譯之 shell 環境自負、Swiftly 於 macOS 15 之限制、不提供 `build640` 類版本鎖定入口；`vChewing-LibVanguard` 側：〈Building〉一節，**Linux／Windows 僅建議最新正式版 Swift、至少 6.4.0**）；尚未結清者僅「10.9 實機對安裝程式之修後複驗」與「10.10／11.x 真機」。**末後檢討（2026-09-16，事主提問）——legacy 靶是否非得 Xcode 15？**：**Xcode 15 本身不是本質需求，但「一個預設 macOS SDK 夠舊的 Xcode」是**，以現行架構無法用 CLT／自製 dev dir 頂替。機制：`swift build`（5.10）會一併編譯命令型 plugin `BundleAppsLegacy`，而**該 plugin 的 SDK 取自 active developer dir 的預設 macOS SDK**——實測記錄在 `<scratch>/plugins/cache/BundleAppsLegacy-state.json` 之 `commandLine`：以 Xcode 15.4 為 `DEVELOPER_DIR` 時為 `MacOSX14.5.sdk`（**不是** `--sdk` 所指的 13.3）；`--sdk`／`SDKROOT`／`-Xswiftc -sdk` 三者對它**皆無效**（逐一實測，plugin 指令仍記 `MacOSX27.0.sdk`）；而 5.10 吃不下 SDK 27（實測：`unknown argument: '-target-arch-variant'` ＋ `module '_c_standard_library_obsolete' requires feature 'found_incompatible_headers__check_search_paths'`；13.3／14.5 皆正常）。**不可頂替之實測**：CLT 當 `DEVELOPER_DIR` 會在 `xcrun … PlatformPath` 一步即死（CLT 無 `Platforms/`）；自製 dev dir（.app 形、`SDKs/MacOSX.sdk` 指向 13.3）被 `xcode-select` 判 malformed 或逕自退回系統預設 Xcode。故 **Xcode 15 的兩個實際職責＝（a）提供「預設 SDK ≤ 14.x」的 dev dir（任何 ≤ 15.4 的 Xcode 皆等價）＋（b）提供 `actool`**（CLT 無 actool；惟 Xcode 27 的 actool 對 `--minimum-deployment-target 10.9` 實測可用，rc=0）。**已改綁（2026-09-16，事主裁定）**：`LEGACY_SDK` 之 21 份（倉根 `Makefile`、`Packages/` 之 19 份逐包、鄰倉 `vChewing-LibVanguard/makefile`——後者仍與聚合體那份逐位元組相同，`cmp` 已驗）一律改指 CLT 真身 `/Library/Developer/CommandLineTools/SDKs/MacOSX13.3.sdk`；`LEGACY_XCODE` 維持 Xcode 15（build-plugin 編譯所需，見上）。**該 SDK 之來歷（事主囑查明）**：內容＝原廠 macOS 13.3 SDK（`usr/include/stdlib.h` 時間戳 **2023-03-29**，即 Xcode 14.3 次日發行那批）。**它並非現行 CLT 所提供**：本機 CLT 為 **27.0**（`com.apple.pkg.CLTools_Executables` 27.0.0.0.1788430756，2026-09-10 16:17 裝入），該版把 macOS 11／12／13／14 之 legacy SDK 一律改成**移除包**（`com.apple.pkg.CLTools_SDK_macOS13` 之 payload 即 `CLTools_macOS_DevSDK_Remove_macOS13.pkg`）；本機這份 SDK 目錄頂層時間戳為 **2026-05-27 16:55**，且 `softwareupdate --history` 除 27.0 外無任何 CLT 安裝記錄（install.log 僅回溯至 2026-06-10），故係當日由外部置入。**換機時得自 macOS 13.3 之 CLT／Xcode 14.3 自行備妥**；此結論已寫入兩倉 README。**改綁同時修掉一個真缺陷**：`BundleAppsLegacy` 原以 `<sdk>/../../Info.plist` 推導平台模板（Xcode 佈局成立；CLT 佈局會走到 `/Library/Developer/Info.plist` 而拋 `Cannot read plist`、`bundleLegacy` rc=1），現改為「先試 SDK 自身平台、落空則退回 active developer dir 之平台（`xcode-select -p`，尊重 `DEVELOPER_DIR`）」，兩者皆不可讀時照舊拋錯；`DT*` 與改綁前逐鍵相同（`DTSDKName macosx13.3`／`DTSDKBuild 22E230`／`DTXcode 1540`／`DTPlatformVersion 14.5`）。**commit**：`vChewing-macOS` 之 `ec729322`（Swiftly 段）＋ `2bee7640`（`Makefile // Cancel the needs of putting macOS 13 SDK into Xcode 15.`，即本段全部內容）；`vChewing-LibVanguard` 之對位尚未 commit。詳見 `Reqs_0211-0220.md` Phase 218。

## 十一、參考資料

- 詳見 `vChewing-DevLogs` 目錄的其餘 Markdown 檔案。
- `vChewing-LibVanguard/EVOLUTION_MEMO.md` 記錄了各模組的原始設計備忘錄。

---

> ⚠️ **注意**: 本文檔需要定期更新以反映最新程式碼狀態。最後更新：2026-09-16（**Phase 219 ＝**「CI（linux／WinNT）測試行程硬崩之修復」：`mainSync` 與 `withFileHandleQueueSync` 改以「是否已在主佇列」（`DispatchSpecificKey`／`getSpecific`）判斷、不再只看 `Thread.isMainThread`，兩倉 byte-sync（其後另經事主指示把 macOS 之 `1bd075b0` 同構同步至 `vChewing-OSX-legacy`）。**第二輪（CI 複驗後）**：第一輪只解一半——`InputSession.deinit` 已止、`LexiconAssembly` 之 `testSaveDataRoundTrip` 仍崩；真因是 `fileHandleQueue.sync` 於未受競爭時就地在呼叫端執行，而該呼叫端正在主佇列上，遂令內層 `mainSync` 問不出主佇列身分——修法＝把主佇列身分判斷提到 `withFileHandleQueueSync` 之入口（`isOnMainQueue()` 隨之公開）。**CI 已驗（2026-09-16，run `35087157715`／`35087157691`，同 SHA `2b5b360a`：兩平台全綠、`Program crashed` 計 0 次、9 靶共 534 項）**。此前一段（**Phase 219 ＝**第一輪：`mainSync` 與 `withFileHandleQueueSync` 改以「是否已在主佇列」（`DispatchSpecificKey`／`getSpecific`）判斷、不再只看 `Thread.isMainThread`）；本機 6.4 側 535 項 0 失敗、兩倉之 5.10 側皆 `Build complete!`；Linux／WinNT 之止崩待推 CI。此前一段（**Phase 218（已完結）＝**Swift 5.10 legacy 路徑之終點推進：倉根套件出兩支執行檔（`vChewing`／`vChewingInstallerLegacy`）＋ libArcLite 摻入 ＋ back-deployment Swift 執行期之兩段式 rpath（`vChewing-macOS` ＋ `vChewing-VanguardLexicon` 之 `makefile` ＋ 本倉文書）**。**第三輪追加（2026-09-16）**：倉根入口更名為 `debugLegacy`／`releaseLegacy`／`cleanLegacy`（app 組裝之 `bundle510-root` 隨之為 `bundleLegacy`），`releaseLegacy` 改出 **universal**（`x86_64-apple-macosx10.9` ＋ `arm64-apple-macosx11.0`，逐支 `lipo`；libArcLite 之 link flag 因 arm64 支線不能摻而自 manifest **移交倉根 `makefile`**），並為之修掉 Swift 5.10 `-O` 之 SIL verifier 崩潰（`CGRect.zeroValue` 跨模組常數）與 `BundleAppsLegacy` 對 `/usr/lib/swift/…` 絕對記錄之誤判。**第四輪追加（2026-09-16）**：① `SwiftExtension` 之跨模組 `CGRect` 常數（`zeroValue`／`seniorTheBeast`）自該模組**刪除**、改由各消費檔以 `fileprivate` 自行定義——`-O` 之 SIL verifier 崩潰自此由「繞開」升為「無從發生」，`vChewing-OSX-legacy` 之同構一行亦鏡射修正（`make debug-core` 通過）；② **concurrency 清剿**——`SwiftExtension` 之 `<6.2` 分支去 `Task`（改走 GCD）、`OtherIMEDataReader` 之 `AsyncGramSequence` 與兩支 `asyncInit()` 圈進 `#if compiler(>=6.2)`，兩 slice 之 **20／20 archive** 與兩支執行檔之 concurrency 引用**歸零**（`@rpath/libswift_Concurrency.dylib` 不再是任何載入記錄）；③ `BundleAppsLegacy` 追加 `pruneForeignRPATHs`，把建置機 toolchain 之絕對 `LC_RPATH` 自 `.app` 內清掉（兩 slice 皆清、驗章照常）。**追加（2026-09-16）**：legacy 部署地板自 10.10 收斂至 **10.9**（`LEGACY_X86_TRIPLE`／`legacyDeploymentTarget`／20 份逐包 `LEGACY_TRIPLE` 一併翻轉），代價是 `vChewing_SettingsUI` 之三處 `#available(macOS 10.10, *)` 守衛（57 筆 availability 錯誤，全在兩檔，依 `vChewing-OSX-legacy` 體例結清）；兩支 `.app` 之 `LSMinimumSystemVersion`、`minos`、`sdk` 皆 10.9，安裝程式顯示「支援 macOS 10.9 及之後的系統版本」；同日再補：兩支 `.app` 之 `Info.plist` 原**完全沒有** `DT*` 建置環境鍵，現由 `BundleAppsLegacy`（新增 `--sdk` 引數，`make bundleLegacy` 傳 `$(LEGACY_SDK)`）讀 `<sdk>/../../Info.plist` 之 `AdditionalInfo` 模板並即時解析其 `$(…)` 補上，九鍵與 `vChewing-OSX-legacy` 之 Xcode 產物逐鍵相同。同日又結兩事：① **安裝程式無暗色模式之真因不在 plist 而在執行檔**——AppKit 讀 `LC_VERSION_MIN_MACOSX` 之 `sdk` 欄判「是否以 10.14 後 SDK 鏈出」，SwiftPM 5.10 把它填成 triple 版本（產物記 `sdk 10.9`，實以 13.3 SDK 鏈結）→ 鎖進 Aqua；IME 靠其 plist 的 `NSRequiresAquaSystemAppearance` 倖免。修法＝兩個 legacy 建置目標補 `-Xlinker -platform_version -Xlinker macos -Xlinker <min> -Xlinker <sdk 版本>`（版本讀 `$(LEGACY_SDK)/SDKSettings.plist`），產物自此 `version 10.9 sdk 13.3`。② **`make releaseLegacy` 之產物慢十幾倍**＝`bundleLegacy` 之 config 預設 `debug`，release 建完卻把 debug 產物包進 `.app`；修法＝兩個建置目標收尾各自 `$(MAKE) bundleLegacy LEGACY_CONFIG=<cfg>`。**第五輪追加（2026-09-16，真機 macOS 10.9 實測）**：10.9 之 legacy 安裝程式按「安裝」數秒即崩，根因＝`setEventHandler(handler: nil)` 經內建的 Swift 5.0 Dispatch shim 直通 `dispatch_source_set_event_handler(source, NULL)`、10.9 的 libdispatch 收 NULL block 即 SIGILL（現代 libdispatch 容忍 NULL，故 6.4 側從未顯露）；改傳 `{}`，兩倉同步，輸入法本體於 10.9.5 運作正常。另：辭典資產自此自動建置（`bundleLegacy` 之前置 `lexiconLegacy`），且 `LegacyZone/LexiconBuildTrigger` 改以 `LEXICON_CONFIG`（預設 `release`；刻意離散於 app 的 `LEGACY_CONFIG`，否則會被 sub-make 帶回 debug）。環境事實兩則：Xcode 之套件解析不吃外部 OpenSource toolchain（Intel Mac 自此不能以 Xcode 建置本倉）、`MacOSX13.3.sdk` 係 CLT 那份之 symlink。**commit**：崩潰修補＝`b48b07b5`（macOS）／`b48e1c4f`（legacy 對位）；倉根 `Makefile` 與 lexicon 之建置改動併入 `5793727b`、README 併入 `c7812115`。此前一段（**Phase 217 ＝「把雙 toolchain 建置能力推及 `vChewing-macOS` 全倉（19 個目錄）」**：方法為**各個擊破**（事主指定 18 項擊破序，終點為 `InstallerAssembly4Darwin` 與 `MainAssembly4Darwin`）。**⓪ 基礎設施**：19 個 subpackage **各六份 manifest**（`Package.swift` 抬到 tools **6.4** ＋ `Package@swift-5.10／6.0／6.1／6.2／6.3` 五份對位與封堵檔）與**各一份 `makefile`**（`build510`／`clean510`、scratch 各自 `.build/.legacy`、**不含任何 test 目標**）；`Packages/` 另有批次入口 `make build510-all`／`clean510-all`。**① 擊破／② 收斂**：SwiftUI 之任何內容（**含 `#Preview`**）一律整段圈進 `#if compiler(>=6.2)`、`nonisolated` 自 extension／型別標頭**下放至成員**、availability 用 `if #unavailable() {} else {}` 兩分支、`nonisolated deinit` 改樸素 `deinit`、`@Sendable` 閉包內不得碰 MainActor 成員（legacy 形狀＝`DispatchQueue.main.async { @MainActor in … }`）、`SettingsUIHost` 之顯式 `@MainActor` **撤除**（6.2 側由 `defaultIsolation` 供同一隔離）。**終點產物**：`libInstallerAssembly4Darwin.a`（3.2 MB）＋ `libMainAssembly4Darwin.a`（26 MB），皆 `current ar archive`／不帶 load command。**驗收**：`make build510-all` **19／19 rc=0、0 error**（2026-09-15）；6.4 側 **535 項 0 失敗**；`make lintFormatUncommitted` 淨變動為零。**Swift 6.2／6.3 之封堵**：五個位置各兩份 `#error` 封堵檔、`Package.swift` 抬到 6.4、三系統 CI pin 同批抬到 ≥6.4。**測試結構之整肅**（`208554e`，16 檔 +490/−412）：`LibVanguardTests` 收進單一 `.serialized` 根 suite、Izanami 測試補上同步載入、Homa 堆積哨兵改多次取樣——`--no-parallel` 與 `.serialized` **缺一不可**，兩倉各連跑 5 次全綠。**2026-09-16 追加**：① 46 份封堵檔之 `#error` 訊息**同構化**；② `vChewing_MainAssembly4Darwin` 之 5.10 manifest 依事主指示**改回與 `Package.swift` 同構**（收遠端 `vChewing-VanguardLexicon` 依賴與 `TextTemplateAssetInjectorPlugin`／`VanguardTextMapPlugin`）——**該包於 5.10 側因此復歸 rc≠0、全倉 18／19**：5.10 SwiftPM 對非根套件的 `unsafeFlags` 一律硬拒（該 lexicon 的 `CSQLite3` 靶帶 `.unsafeFlags(["-w"])`），`--disable-sandbox` 無效、改本地 `.package(path:)` 則撞 macOS 10.15 平台下限；**正解在 lexicon 倉**（`Phase217_SOP.md` §十四 **N4**）。**一則判讀更正**：首輪曾把該錯誤記為「巢狀沙箱假象」，已由冷啟實驗（同一 manifest、只切 `--disable-sandbox` 一變因）推翻。施工規範與逐包進度：`vChewing-DevLogs/Research/Phase217_SOP.md`）。**本項已由 P218 接管（2026-09-16）**：lexicon 之 deploy 工具真正用上 Swift Concurrency（故其 manifest 須宣告 macOS 10.15），與本側 5.10 之 10.13 地板相衝；現行作法是 5.10 manifest 不再收該依賴與兩支 plugin（`make build510-all` 因而復歸 19／19 rc=0），「另尋資料注入他法」移入 P218——原因、實測與後續見 P218 之紀錄。 **此前一段**（**Phase 216 ＝「`vChewing-LibVanguard` 的雙 toolchain 建置能力：Swift 5.10（macOS 10.10 legacy 靜態庫）與 Swift 6.2+（macOS 12+、動態 `libVanguard.dylib`）並存（兩倉）」**：同一份 `Sources/` 自此隨 toolchain 版本給出不同建置行為——`Package@swift-X.Y.swift` 之版本擇定（判準為**檔內宣告的 tools version**、非檔名；含 `Package@swift-6.0.swift`／`6.1.swift` 兩份 `#error` 封堵檔，令該兩版明文拒絕建置），`makefile` 新增 `build510`／`build510SwiftExtension`／`clean510` 與 `LEGACY_*` 五個變數。**5.10 側 manifest 之四項刻意差異**：產物一律 **static**、**`platforms: nil`**、**不含任何測試靶**、**不收 `vChewingSharedCLI`**。**手法**：`nonisolated` 自 extension／型別標頭下放到成員、**`@MainActor` 自協定撤除**（只改 3 行即令 1771 筆錯誤歸零，係本輪唯一的「單一形態、零取捨」大手術）、FileHandle 現代 API 之整組語法糖、dispatch 調度以 `mainSync` 兜底、`deinit` 樸素化、`NSMutex` 退場。**硬配對**：Swift 5.10.1 toolchain ＋ Xcode 15 的 `MacOSX13.3.sdk`（Xcode 27 的 SDK 會給 312 個掩蓋式錯誤，且 `dump-package` 在該 SDK 下照樣成功、不足以證明可用）＋ `-target x86_64-apple-macosx10.10`（**10.9 不存在**：`Data` 標為 macOS 10.10 起；且 SwiftPM 會吞掉你想壓低的部署目標）。（2026-09-16 更正：`10.9 不存在` 之說已被推翻、且地板已收斂至 10.9，見 P218〈十六〉）**驗收**：6.2 側 `swift build` ✅ ＋ **535 項 0 失敗**；5.10 側 `make build510` **rc=0**（`libVanguard.a` 14 MB）、`make build510SwiftExtension` ✅（`libVanguardSwiftExtension.a` 540 KB）。**三個 commit**（現行 hash）：`13097e0`（6 份版本擇定 manifest ＋ `makefile`）、`ac781c1`（`SwiftExtension`）、`826a744`（聚合體 23 檔）；**原有一筆 `FormatConfig` 經事主複核後整筆刪除**——該格式管線謀求的是 SwiftLint→SwiftFormat 順序運行後的最終結果，故本次手術無須動兩份 config。macOS 側亦已對位並推送（`89af456f`／`3e72cddb`），兩倉 `Sources/`／`Deps/`／`Package.swift`／`Package@swift-5.10.swift` 之 byte-sync 實測零差異（P214 之同源性恢復並維持）。**殘留**：Tekkon 的 `nonisolated(unsafe) sharedCache`、5.10 側 2 個 warning、全倉唯一 1 處 `#if compiler(>=6.2)`、`withFileHandleQueue*` 的退役時機、legacy 的 10.9 疑點。建置指令與環境配對見 §10.5）。此前一段（**Phase 215 ＝「`SwiftExtension` 自聚合體析出為獨立動態載體 `VanguardSwiftExtension`（兩倉）」**：事主抱怨「App-based installer 現在也帶一份 LibVanguard，很怪」——該 installer 自身只用得到這一個模組（`String.i18n`／`Process.consoleLog`／`realHomeDir`／`isAppleSilicon`），卻因 P210 併包（`vChewing_SwiftExtension` 被消滅、塞進聚合體）而被迫連帶吃下整條打字閉包；tag 4.7.4 時它只依賴當時獨立存在的該套件，**本 phase 即在還原該形狀**。**前置研究**（`Research/Phase215_PreResearch.md`）：五支隔離探針釘死 SwiftPM 之能力邊界——同套件之 product 依賴**不存在此語法**（`unknown package …; valid packages are:`／`package:` 自 tools 5.2 起必填）；「target 屬於某 dynamic product」對同套件消費者**仍靜態併入**（探針 C／D／E 皆然，且 SwiftPM **不報錯**、靜默產出兩份 image）；唯**跨套件** dynamic product 才真動態相倚（探針 G）。故原定「聚合體內新增窄產品、內部以 product 形式取用」不可表達，改採：該模組**析出為獨立套件**（macOS `Packages/vChewing_VanguardSwiftExtension/`、LibVanguard 於倉根同名目錄），出貨 dynamic product `VanguardSwiftExtension`（**非 Darwin 為 static**，以頂層 `#if canImport(Darwin)` 分流，故 Linux／Windows 不產生第二顆動態庫），聚合體 5 個內部靶與三消費端（`OSFrameworkImpl`／`IMKUtils`／`InstallerAssembly4Darwin`）改以**跨套件** product 取用之，其原始碼一行未動；**模組名一度亦更名，其後經事主裁定回退**（現行：模組仍為 `SwiftExtension`、`import SwiftExtension` 全倉不變，**僅產品名**為 `VanguardSwiftExtension`；SwiftPM 不要求兩者同名。連帶使 `TK_QueryBuffer.swift` 之守衛還原、**三倉 TrieKit 七檔 sha256 全等復原**）。**硬不變式**：每個行程內該模組恰一份 image——因 `OSFrameworkImpl` 的 public 簽名含 `@ArrayBuilder`、且對 `AppProperty` 宣告了 retroactive `DynamicProperty` conformance，而聚合體內 `PrefMgr` 帶 103 項 `@AppProperty`，同名兩份 image 即 ABI 危險型態。**結果**：installer 自身同捆由整條閉包降為**一顆 383 KB**，`vChewing.app` 為兩顆（6.59 MB＋383 KB）。**驗證**：macOS 聚合體 **529 項 0 失敗**（＝539−10）／新套件 10／安裝器 5，LibVanguard 聚合體 535＋新套件 10；`otool -L libVanguard.dylib` 含 `@rpath/libVanguardSwiftExtension.dylib`、兩顆 dylib 之**已定義符號交集為 0**；`make debug`／`make release` 同捆實查、四款產物**實跑存活**；Xcode `vChewingInstaller`／`vChewing` 兩 scheme rc=0、驗章通過、**未動 `project.pbxproj`**。**佈局（事主修正）**：子套件置於**聚合體目錄之內**的 `Deps/VanguardSwiftExtension/`，故 `.package(path:)` 兩倉同字串——**兩倉 `Package.swift` 維持逐位元組相同**，P214 之不變式未破。**殘留**：無——原本列為殘留的 legacy TrieKit 文字差異，已由模組更名回退一併復原（三倉七檔 sha256 全等）。此前一段（**Phase 214 ＝「`OSNeutralAssembly` → `LibVanguard` 全面更名（含 macOS 套件目錄正名）＋ `Package.swift` 之 ArrayBuilder 化（兩倉）」**：本 phase 原為 Phase 213 之「同日第二輪追加」，事主複核後裁定其分量已足自成一 phase，故獨立；並補上第一輪漏佈置的 **macOS 套件目錄正名**（該項原列於 Phase 213〈附註〉之「留待事主裁定」）。事主三條指示——事主三條指示——① LibVanguard 的 `Package.swift` 恢復 `@resultBuilder` 的 `ArrayBuilder` 結構（「這允許 OS-based compiler flags 的直接套用」），OSNA 的 `Package.swift` 亦照此重寫；② macOS 側的 OSNA 套件亦更名為 `name: "LibVanguard"` ＋ 產品 `Vanguard`（產物 `libVanguard.dylib`、不會出現 `libLibVanguard.dylib`；`LibVanguard_BPMFVS.bundle` 屬可接受範圍）；③ 兩倉 `import OSNeutralAssembly` → `import LibVanguard`（「這樣也能確保 Package.swift 的 byte-identical 乃至兩個倉庫的 LibVanguard 內容的 ByteIdentical」）。**落點**：(a) manifest 改以 ArrayBuilder DSL 撰寫，**並補上原 DSL 所無的 `buildSwiftSettings` / `buildLinkerSettings`**——Swift 的 `#if` 是指令而非表達式、無法寫在陣列字面量內，只有 result-builder body 容得下它，此即「OS-based compiler flags 直接套用」之機制；`platforms:` 走 `buildSupportedPlatform { #if canImport(Darwin) … }` 而**值維持 `.macOS(.v12)` 不變**；`swiftLanguageModes: [.v6]`／14 處 `.defaultIsolation(MainActor.self)`／`iconv` 的 `linkerSettings`／3 處 `resources` 數值一律照舊，只換形制。(b) macOS 的 `Packages/vChewing_OSNeutralAssembly/Package.swift` 同步改名，並連帶修正**不改即壞**的三處：11 個 consumer manifest 的 14 處 `.product(name:)`（`vChewing_IMKUtils`／`vChewing_FolderMonitor`／`vChewing_ModifierKeyHitChecker`／`vChewing_Shared_DarwinImpl` ×2／`vChewing_OSFrameworkImpl`／`Jad_BookmarkManager` ×2／`HangarRash_SwiftyCapsLockToggler`／`vChewing_UpdateSputnik`／`vChewing_SettingsUI` ×2／`vChewing_InstallerAssembly4Darwin`／`vChewing_MainAssembly4Darwin`）、`ButKo_BPMFVS_Accessors.swift` 與 `ResourceLocatorTests.swift` 的硬編碼資源 bundle 名、`Plugins/BundleApps/plugin.swift` 的 `excludedBundleNames`。(c) 更名清剿以「**凡是名稱皆改、凡是路徑所指之封裝目錄則不改**」為判準：兩倉 `Sources/OSNeutralAssembly/` → `Sources/LibVanguard/`、`Tests/OSNeutralAssemblyTests/` → `Tests/LibVanguardTests/`（`git mv`）、manifest 之 target 與 testTarget 名、全部 import 三態、註解／doc comment、`UserDefaults` suite 名與 `PortableSessionHarness` 的 mock client bundle ID、macOS `MainAssembly4Darwin` 三檔、CI 檔名（`test_{ubuntu,winnt}_OSNeutralAssembly.yml` → `test_{ubuntu,winnt}_LibVanguard.yml`）與 job 名、macOS `Makefile` 的 `spmLinuxTest-OSNeutralAssembly` → `spmLinuxTest-LibVanguard`、以及兩倉 README／EVOLUTION_MEMO／AGENTS／CLAUDE／algorithm／copilot-instructions／DevLab 之散文與**已改名子目錄**之路徑。**刻意不動**：macOS 的套件**目錄名** `Packages/vChewing_OSNeutralAssembly`（故 `.package(path:)` 與 `.product(…, package: "vChewing_OSNeutralAssembly")` 原樣——SwiftPM 對本地路徑相依之 identity 取自目錄名；一併正名則須連帶改 11 個 manifest 的路徑、CI 的 `--package-path`、`Makefile`、`project.pbxproj` 與多份文件路徑，手術範圍將不成比例膨脹，**留待裁定**）、`project.pbxproj`（僅含該路徑）、CI 的 `--package-path`。**量化**：macOS 81 筆（28 修改 ＋ 53 更名，其中 22 筆更名兼改內容）；LibVanguard 58 筆（7 修改 ＋ 51 更名，其中 20 筆更名兼改內容）。(d) 依事主指示以**單一 `make lintFormat`**（不分開跑 lint／format）在兩倉各跑一次，皆 rc=0。**施工中一項發現**：LibVanguard 側跑完 `make lintFormat` 後，`InputHandlerTests_Basics.swift` 與 `SessionTests_Basics.swift` 的 import 次序與 macOS 側出現 1 行差異——該次序在更名前是依 `OSNeutralAssembly` 的字母序排定的（`lexiconassembly` < `libvanguard` < `lxassemblymaterials4tests`，大小寫無關）；**於 macOS 側跑完同一個 `make lintFormat` 後兩倉即重新逐位元組一致**，此即「更名後必須兩倉同跑 lintFormat」之理由，否則 byte-identity 會以一行 import 之姿悄悄破裂。**驗證**：`cmp` 兩倉 `Package.swift` **逐位元組相同** ✅；`diff -r` 兩倉 `Sources/` **零差異** ✅；`diff -r` 兩倉 `Tests/` 唯一差異為本倉獨有的 2 個搶救測檔（`POMDecayWindowTests.swift`／`LXPlainBopomofoEtenDOSTests.swift`）✅；LibVanguard `swift build` ✅（18.02 秒，`Vanguard-product` 產出 `libVanguard.dylib`）＋ `swift test --no-parallel` **545 項 0 失敗**；macOS 套件 `swift build` ✅（19.15 秒）＋ `swift test --no-parallel --package-path Packages/vChewing_OSNeutralAssembly` **539 項 0 失敗**；macOS 倉根 `swift build -c debug` ＋ `make debug` ✅（`vChewing.app` 與 `vChewingInstaller.app` 皆內含 `Contents/Frameworks/libVanguard.dylib`，執行檔 `otool -L` 為 `@rpath/libVanguard.dylib`，資源 bundle 實名 `LibVanguard_BPMFVS.bundle`）；資源 bundle 實名於兩倉皆為 `LibVanguard_*` ✅。**未驗**：macOS 三條 CI 的實際執行（本機無 Docker／無 Windows）、Xcode 三 scheme 之建置（端以 `.framework` 形態嵌入動態產品，其名隨產品名變 `Vanguard.framework`，係依 SwiftPM 命名規則更新 `AGENTS.md`、未實跑 Xcode 驗證）、`swift test -c release`。**教訓（記錄備查）**：施工中一度把兩倉測試**併行**執行，得 LibVanguard 硬崩（`Tests/LibVanguardTests/InputHandlerTests_Cases1.swift:3406` 的 `testSession.state.candidates.first!` 因狂拼候選窗尚未生成而 nil、訊號 5）與 macOS 該靶 39 項失敗；改為**逐倉單獨執行**後兩倉各自連續兩次全綠。根因是 CPU 競爭下的時序敏感性（狂拼路徑依賴即時生成候選），與本次更名無關——`swift test --no-parallel` 只保證靶內不平行，不保證跨靶／跨行程無競爭。另一筆：於更名前已建置過的工作樹上跑 `make debug`，app 內會同時出現新的 `LibVanguard_BPMFVS.bundle` 與殘留的 `OSNeutralAssembly_BPMFVS.bundle`（`plugin.swift` 掃描建置產物目錄內全部 `*.bundle` 再逐一複製，對殘留舊產物無辨識力）；清 `.build` 後 from-scratch 重跑即只剩新名——建置衛生問題、非產品缺陷。另一筆：macOS 兩個 CI workflow 的**檔名與 job `name:` 均已改**，**job `name:` 決定 GitHub check-run 名**，分支保護若有 required status check 指向舊名須同步更新（P201~204 同類變更已有同樣提醒）。。此前一段（**Phase 213 ＝「`OSNeutralAssembly` 之聚合體原樣移植進 `vChewing-LibVanguard`（僅一倉）」**：事主裁定「將 OSNA 原樣移植到 `vChewing-LibVanguard`」、並確認 `TrieHub`／`FactoryTrieDBType`／`LexiconHub` 三殘骸退場（`LXFacade` 取代 `TrieHub`、`LexiconHub` 只是空命名空間、`FactoryTrieDBType` 是 SQL 時代遺物）、CI 遷移、`defaultIsolation` 與 minOS 現階段承襲 macOS。**同名追加的範圍修正（決定性）**：「注意：macOS 端的消費不用改。這次盡量只修改 LibVanguard 倉庫。（原因：LibVanguard 是個實驗田。我會在之後嘗試擴增 LibVanguard 的向前建置能力。）」——故本 phase 係**複製（forward-port）**而非「搬走＋改 macOS 消費」，`vChewing-macOS` 與 `vChewing-OSX-legacy` **一行未動**（11 個 consumer manifest、`Makefile`、`plugin.swift`、`project.pbxproj`、三條既有 CI workflow 皆原樣）。**落點（全在本倉）**：① `git rm -r` 73 檔——`Sources/_Modules/{Homa 16,LexiconKit 10,TrieKit 7,Tekkon 5,SwiftExtension 3,BrailleSputnik 2}`＋`Sources/LibVanguard/LibVanguard.swift`＋`Tests/_Tests4Components/*` 28 檔＋`Tests/LibVanguardTests/LibVanguardTests.swift`；② 複製 OSNA 之 `Sources/` 127 檔、`Tests/` 70 檔、`LICENSES/` 13 檔（後者與本倉原有 3 檔並存）；③ `Package.swift` 改三處——`package.name` 取 `"LibVanguard"`、**出貨產品名取 `"Vanguard"`**（故產物為 `libVanguard.dylib`、不會出現 `libLibVanguard.dylib`；事主原話「這個 Bundle 名稱就是 Vanguard，生成的檔案就是 libVanguard.dylib」），**target 名一律不變**（`import OSNeutralAssembly` 等照舊），`platforms: [.macOS(.v12)]`／14 處 `.defaultIsolation(MainActor.self)`／`swiftLanguageModes: [.v6]`／`iconv` `linkerSettings` ×2／`resources` ×3 全數承襲 macOS 側；④ 兩處硬編碼資源 bundle 名（SwiftPM 之 bundle 名取套件名＋target 名）：`ButKo_BPMFVS_Accessors.swift:132` 與 `ResourceLocatorTests.swift:132`；⑤ **搶救舊測試靶的 6 項獨有覆蓋**——為維持「複製檔逐位元組等同 macOS」之可稽核性，一律落進本倉獨有新檔：`Tests/LexiconAssemblyTests/POMDecayWindowTests.swift`（3 項：有效視窗自**最近一次使用**起算之 7.5／8.01 天非 nil 與 9.1 天 nil 之邊界探針、`[(0,true)…(80,false)]` 逐日掃描含 8 天後之長時程防復活探針、`getSavableData()` 整體次序斷言；後者施工時改以逐次 `overrideCandidate(...) { … }` 收集 `PerceptionIntel` ＋ `nowTimeStamp + Double(index)` 寫入，以免依賴 `Date()` 解析度與 Swift 不保證穩定之 `sort`）與 `Tests/LexiconAssemblyTests/LXPlainBopomofoEtenDOSTests.swift`（3 項：倚天表無 `ㄉㄨㄟˇ` 讀音故不供應「㨃」、`valuesFor` 回 `["堆","頧","痽"]`、`partiallyMatchedValuesFor` 之候選次序）；**刻意不移植**原 LV 版之 `partiallyMatchedKeysHandler` 回報契約斷言——三倉的 `hasGrams(_:filterType:partiallyMatch:partiallyMatchedKeysHandler:)`（`TrieKit/TrieProtocol.swift:217-239`）**接受該參數卻從不呼叫它**（LV 原靠其已退場之 `TrieHub` 回報），故在 macOS 這一側它不成契約；判準為「逐一比對舊 `LXKitTests_Perceptor.swift`（845 行）與 macOS 各 POM 測試族之斷言內容、只挑 macOS 側不存在者」（故 `testPOM_01`／`03A`／`11` 等等義重複者不搬）；⑥ CI 遷移：本倉原**完全沒有** `.github`，新建三條 workflow（`test_ubuntu_LibVanguard.yml`／`test_winnt_LibVanguard.yml`／`test_darwin_LibVanguard.yml`，後者 `macos-26` ＋ Xcode 26.6），內容以 macOS 側同名者為底、**改為在倉根直接 `swift test --no-parallel`**（本倉套件即在倉根，故移除 `--package-path`），原 `make spmClean` 本倉不存在故改用 `swift package clean`；⑦ 文件：`COPYING`（失效路徑 `Sources/_Modules/SwiftExtension/` → `Sources/SwiftExtension/`，並補上先前漏列之 `Sources/ResourceLocator/`——同為 MulanPSL-2.0）、`README.md`（新增〈What Is In This Repository〉；〈Credits〉年份自 `2025` 改 `2022`、另列 `Homa`／`TrieKit`／`BrailleSputnik` 為 `2025`）、`EVOLUTION_MEMO.md`（頂部加改組說明；`PerceptionKit`／`LexiconKit` 兩節合流為 `LexiconAssembly` 並補上原缺漏之 7 個模組）；`AGENTS.md` 與 `VALUEADD/` **未動**（前者 33 行全為工作哲學、無失效引用；後者為歷史報告，P201~204 已裁定不動）。**驗證**：`swift build` ✅（11.95 秒，`Vanguard-product`）、產物 `libVanguard.dylib` 與 `LibVanguard_*.bundle` ×4 實測成立、`swift test --no-parallel` **545 項 0 失敗 0 警告**（10 靶：TrieKit 18／Tekkon 37／SwiftExtension 10／BPMFVS 7／ResourceLocator 6／OSNeutralAssembly 203／LexiconAssembly **193**／Homa 61／BrailleSputnik 3／Shared 7），**與 macOS 側對照 545 ＝ 539 ＋ 6、193 ＝ 187 ＋ 6，差額恰為搶救回來的 6 項**；`diff -r` 實證 `Sources`／`Tests` 與 macOS 側的唯一差異為**兩個 bundle 名（刻意）**與**兩個本倉獨有新測檔**，其餘 127＋70 檔逐位元組相同；`make lintFormatUncommitted` rc=0（SwiftLint 對 `Package.swift` 動 `trailing_comma` ×27，改後與 macOS 版之差異仍僅檔首註解與兩個名字）、`swiftlint` 對新增 2 測檔 rc=0（餘 4 條 `explicit_top_level_acl`／`identifier_name` 為不可自動修正且有同儕先例者——`Tests/` 全目錄此二規則現有 55 ＋ 21 處）、`swiftformat --swiftversion 6.0 --lint` `0/3 files require formatting`、格式後全量重跑 545 項全綠。**未驗**：三條新 CI 的實際執行（本機無 Docker／無 Windows）、LibVanguard 對 macOS 之「向前建置能力」（事主明示屬後續 phase）、`swift test -c release`。**未 commit**（73 staged deletion ＋ 215 untracked ＋ 4 modified）。**反面提醒**：SwiftPM 資源 bundle 名取**套件名**，故現為 `LibVanguard_*.bundle`；若欲 `Vanguard_*.bundle`，改 manifest `name:` 一行即可（代價是倉名與套件名不再一致）。此前一段（**Phase 212 ＝「OSNA 的語言模組來源可插拔化與多來源元圖合流 API（macOS）」**：把 LibVanguard `LexiconKit` 的 `LexiconGramSupplierProtocol`（`LX_Components/LX_GramSupplierProtocol.swift`）與 `GramConcatFlags`／`concatGramQueryResults`（`LX_GramConcatAPI.swift`）兩道「湯」單向移植進 OSNA 的 `LexiconAssembly`——兩者在 OSNA 側原為**零引用**（LibVanguard 的生產消費者是 `VanguardTrie.TrieHub`）。移植一律落進 `LXAssembly` 命名空間、元圖型別由 `Lexicon.HomaGram`（其本質是 TrieKit 的 `VanguardTrie.TrieGram`，欄位為 `value`）換成 `Homa.Gram`（`current`／`probability`）。**新增 4 檔**：`LexiconGramSupplier.swift`（三項要求逐一對應，另含預設參數擴充、`queryAssociatedPhrasesPlain` 與 `Homa.Gram.init(_ trieGram:)` 橋接；保留 `AnyObject` 約束——來源以參照被掛載）、`LX_GramConcatAPI.swift`（`GramConcatFlags` 的 `.sort`／`.deduplicate`／`.all`，**空集合即原樣串接保序**；`concatGramAvailabilityCheckResults` 取邏輯或；`concatGramQueryResults` 在 `forbiddenKeyValueHashes` 非空時改以該集合為唯一過濾依據、不再另行去重；`sortGrams` 政策＝幅長降冪→索引鍵字面升冪→權重降冪，**刻意異於** `Homa.Assembler.sortGram` 的權重優先；`makeGramIdentityHash` 的身分＝讀音索引鍵＋詞值＋前驗詞值、不含 `anterior` 與權重）、`LXGramSupplyHub.swift`（多來源中樞，對位 LibVanguard `TrieHub` 的角色：`mount`／`unmountAll`／`mountedSuppliers`／`concatFlags`／`generation`；另有 `LXFactoryGramSupplier` 把原廠辭典包成可掛載來源、每次查詢即時取 `factoryTrie`）、`LXFacade_GramSupplierMounting.swift`。**接進生產管線**（`LXFacade.swift` 六處）：`unigramsFor` 兩條路徑各在「原廠辭典區塊最末」追加合流結果（替代讀音路徑以 `&` 連讀鍵詢問）、`hasUnigramsForFast` 於同一 cassette 守衛內追加在庫檢查（**必要接點**：`Homa.Assembler` 靠它判定讀音合法性，不接則掛載來源永遠查不到）、兩處 LRU 快取指紋追加 `gramSupplyHub.generation`（掛載異動即自動失效；空掛載時恆為常數，故**預設零行為變更**）。**未做**：讓 `LXFacade` 自身 conform（其查詢是已過濾除表／置換／POM／排序的後處理結果，`filterType` 對它無意義——硬套會做出語義不精確的 conformance）；讓子語言模組 conform（OSNA 側 `LXCoreEX`／`LXCassette`／`LXAssociates` 皆為值型別，以參照掛載會整份複製；LibVanguard 側同類為 `final class`）。**兩倉同步（事主 2026-09-14 追加：影響範圍亦包括 `vChewing-OSX-legacy`）**：legacy 側 `Shared/vChewingComponents/LXAssembly/` 新增同名四檔、`LXFacade.swift` 改七處、`project.pbxproj` 16 處接線（`plutil -lint` OK）。該倉係單一 Xcode target，故移植時不寫 `import Homa`／`import TrieKit`／`import SwiftExtension`（三者皆為同 target 的來源目錄／檔案；在該倉寫 `import SwiftExtension` 會直接 `no such module` 失敗——P206 實錄）、不寫 `nonisolated`，餘者逐位元組等同 macOS 版；legacy 依慣例不承擔單元測試，故 20 項測試只在 macOS 側。**驗證**：套件 `swift test` **539 項 0 失敗**（＋20，`LexiconAssemblyTests` 167 → 187）、倉根 `swift build` 連帶編過 `MainAssembly4Darwin`／`InstallerAssembly4Darwin` ✅、`make lintFormatUncommitted` rc=0、`swiftformat --lint` 8 檔零 churn；legacy `make debug-core` ✅ `** BUILD SUCCEEDED **`（Xcode-15／SDK 13.3／Swift 5 模式）、legacy `swiftformat --lint` 5 檔零 churn。**待裁**：`concatGramQueryResults` 的排序政策與既有區塊（`unigramsFor` 十餘處 `+=`、`abbreviatedWordCandidates` 的雙來源合流）的「分區優先＋依詞值去重」語義不同，直接套用會改變候選次序，故本 phase 未動該等區塊。LibVanguard 未動。）。此前一段（**Phase 211 ＝「POM 測試發現缺陷修復 ＋ `bleachSpecifiedSuggestions(headReadingTargets:)` 測項移植（macOS）」**：`Packages/vChewing_OSNeutralAssembly/Tests/LexiconAssemblyTests/POMBasicTests.swift` 的 16 個 `func testPOM_BS01…BS16` 中僅 3 個帶 `@Test`，其餘 **13 個自 2026-02-06 `4cd6c9b8`「Tests: Deprecating XCTest.」的 swift-testing 遷移起無聲停跑**（XCTest 靠命名慣例收集，遷移只補 2 個 `@Test`；2026-04-22 新增 BS16 時有補對），以 `git log --follow` 逐版清點證實為**遷移事故而非刻意停用**。**落點一**：補回 13 行 `@Test`（BS02–BS14），檔內 3 → 16。**落點二**：BS03 補回後首度失敗——其 `b` 條目之 key head 段為 2 音節 `(ㄐㄧㄤˇ-ㄐㄧㄣ,獎金)` 卻配 3 字候選「伙食費」，被 2026-09-05 落地之 P192 錯位守衛（`LXPerceptor.swift:407`）擋下而必然回 `nil`；該行自遷移前即未變動，屬測試資料自相矛盾，故修正 key 之 head 段為 `(ㄏㄨㄛˇ-ㄕˊ-ㄈㄟˋ,伙食費)`，**未放寬任何斷言、生產碼一行未動**。**落點三（事主同日追加）**：把 LibVanguard `LXKitTests_Perceptor.swift:356` 的 `headReadingTargets` 語意測項移植進 `POMBleacherTests.swift`（新增 `testBleachSpecifiedSuggestionsByHeadReading`）——該重載在 OSNA 側原為**實質可達卻零斷言覆蓋**（`InputSession_Delegates.swift:417` → `SessionHostWiring.swift:29` → `LXMgr_Core.swift:283` → `LXFacade_POMRepresentable.swift:80-88` → `LXPerceptor.swift:817`；測試端僅在 mock 內被無斷言叫用），新測項兼查 `mutLRUMap` 與 `getSuggestion` 兩層。**驗證**：`LexiconAssemblyTests` 153 → 166 → 167、OSNA 套件總數 505 → 518 → 519 全綠、`make lintFormatUncommitted` rc=0、同類漏網掃描 0 命中。**未動**：生產碼、LibVanguard 倉。**附註**：錯位記憶「存得進、讀不出」之不對稱未動、留待裁定）。此前一段（**Phase 210 ＝「`OSNeutralAssembly` 的去 bundle 化 ＋ 動態庫編譯產物（兩倉）」**：動工前實查得整個 `OSNeutralAssembly` 依賴閉包（7 個 SPM package）在執行期**只讀取一份檔案資源**——`BPMFVS` 的 `phonic_table_Z.txt`，其餘 package 對 `Bundle` 一無引用；故落點僅三處。（一）**新增 `ResourceLocator`**（`vChewing_SwiftExtension` 套件內的新 target；macOS `Sources/ResourceLocator/`、legacy `Shared/Extensions/`）：以「不假設編譯產物必定身處於某個 bundle 內」為前提**依序**嘗試候選位置，全部落空則回 `nil`、絕不 `fatalError`；順序＝手動指定 → `Bundle.main.resourceURL` → `Bundle(for: anchor).resourceURL` → `Bundle.main.bundleURL` → 錨定 bundle 之同層目錄 → **編譯產物所在目錄**（`dladdr(#dsohandle)` 取當前 image 路徑，再取其上層與其 `Contents/Resources/`）——最後一項為關鍵，使「動態庫與資源 bundle 同層」這種無法從宿主推得的佈局得以命中。（二）**三處改動**：`BPMFVS.getBPMFVSDataURL()` 由 `Bundle.module`（找不到即 `fatalError`）改走 `ResourceLocator` 並新增 `dataURLOverride`／`specifyDataURL(_:)`／`isDataTableLoaded`（`cachedLookupTable` 由 `let` 改 `var`，載入失敗時所有 `convert` 原樣回傳）；`MainAssembly4Darwin` 的 `Bundle.currentSPM` 由 `Bundle`（`fatalError`）改 `Bundle?` 且**成功才快取、失敗不快取**；`InputHandler_HandleStates` 的 `Bundle.main.preferredLocalizations[0]` 改 `.first ?? "en"`。兩倉生產碼自此 `Bundle.module` 零殘留。（三）**OSNeutralAssembly 層級的資源指定介面**：新增 `ResourceProvision.swift`（`specifyResourceRoot`／`specifyResourceBundle(_:forBundleNamed:)`／`specifyBPMFVSTable`／`clearAllSpecifications`／`availabilityReport()` 回報資源**是否已成功載入**），`ResourceLocator` 另以 `@_exported import` 併入 `OSNeutralAssemblySPM.swift`。（四）**動態庫**：`Packages/vChewing_OSNeutralAssembly/Package.swift` 於既有 static product 之外新增 `.library(name: "OSNeutralAssemblyDylib", type: .dynamic, …)`——另立 product 係因根 `Package.swift` 的兩個 executable 只遞移依賴 static product，故全倉 `swift build` **不會**連帶建置此 dylib（實測命中 0），零常規建置成本；產物為 `Mach-O 64-bit dynamically linked shared library arm64`，`otool -L` 無任何 bundle 假設，整組閉包已靜態併入。（五）**新測試**：`ResourceLocatorTests` 6 項、`BPMFVSTests` +1（該靶自此標 `.serialized`）、`ResourceProvisionTests` 4 項。（六）**端到端實測**：以 `swiftc` 另編一支**純 CLI 宿主**（`Bundle.main.bundleIdentifier` 為 `nil`）連結該 dylib，三種佈局實測——dylib 與資源 bundle 同層而宿主在別處：命中；**只有 dylib、旁邊無資源 bundle：`availabilityReport` 為 `false`、`bpmfvsDataURL` 為 `nil`、轉換原樣回退、退出碼 0 不崩潰**（改前的 `Bundle.module` 在此必然 `fatalError`）；補回 bundle 後：命中。（七）legacy `project.pbxproj` 8 處插入、僅掛 `vChewing` target，`plutil -lint` OK。**未動**：legacy `DictionaryData/`（pinned submodule）與 `LXAssemblyMaterials4Tests/TestData.swift` 的 `Bundle.module`；`build_darwin_SPMTestsAndPackage.yml` 的 `packages=(…)` 未加 `./Packages/vChewing_SwiftExtension`（CI 範圍變更屬事主決策）。**未驗**：Linux／Windows CI（本機無 Docker）。**追加檢證（同日，事主指示）**：五條既有建置路徑——Xcode 的 `vChewing`／`vChewingDebuggable`／`vChewingInstaller` 三個 scheme（Debug）與 `make debug`／`make release`——實測**皆不產出、亦不同捆** dylib：各自的 `.app` 皆 0 個 `.dylib`、無 `Contents/Frameworks`、執行檔 0 條 `@rpath`（`make release` 的兩 app 皆 universal，且安裝器內嵌的第二層 IME app 亦同）；根 `swift build` 從未建出該 product（SwiftPM 產物目錄清單可證），Xcode 亦只宣告 `MainAssembly4Darwin`／`InstallerAssembly4Darwin` 兩個 SPM product 依賴。另以正向探針把真正的 `libOSNeutralAssemblyDylib.dylib` 塞進插件輸入目錄後重跑 `bundle-apps`，`.app` 內仍 0 個 dylib——即 `Plugins/BundleApps/plugin.swift:102-104` 的 `pathExtension == "bundle"` 篩選確實會擋掉它。注意 `xcodebuild -derivedDataPath` 不會把產物導到該路徑（Xcode 產物落在倉庫自身的 `Build/Products/`）。**追加檢證之二（同日，已修）**：`Plugins/BundleApps/plugin.swift` 原以 `try? fm.removeItem(at: outputDir)` 清空整個 `Build/Products/<Config>`，而本機 Xcode **全域**偏好（`IDECustomBuildProductsPath = "Build/Products"`、`RelativeToWorkspace`）把 Xcode 的 `SYMROOT`／`OBJROOT` 指到同一棵樹（倉庫內無任何一處設定之），故 `make debug` 會連 Xcode 的中間物一起刪（修前：72 個 `.o`／`.swiftmodule` ＋ 2 個 `.app.dSYM` → 0）。改為只刪自己擁有的兩個 `.app`（`plugin.swift`）後，Xcode 的 69 個中間物 ＋ `vChewingDebuggable.app` ＋ dSYM 在 `make debug` 後全數存活、插件自有產物仍正常刷新，`make release` 照常通過。**兩條已證死路**：`project.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings` 的 `BuildLocationStyle`／`CustomBuildProductsPath` 不被 `xcodebuild` 採納；專案層級 `SYMROOT`／`OBJROOT` 會令 Xcode 退回 legacy 佈局而 SPM 整組失效。**追加檢證之三（同日，E8 落地嘗試）**：事主指示把 `OSNeutralAssemblyDylib` 變成 app target 的依賴——**照字面做被 SwiftPM 否決**（`duplication of library code`，8 個 product）；量測得 exe 側與 dylib 側的**交集恰為整個閉包**、「由外消費內」的邊 **15 條**，故落實須把 9 個 product 全改 `.dynamic` 並連帶改 `BundleApps`／`Makefile`／`BuildPKG.sh`／Xcode 三 scheme，且會動到 6 個套件的 Ubuntu／Windows CI 連結形態（本機無法驗證）。**已完成並實測的是管線**：`plugin.swift` 只同捆執行檔**實際連結**的 `*.dylib`（`otool -L` 取件、找不到即報錯）、建 `Contents/Frameworks`＋補 `@executable_path/../Frameworks` rpath、`codesign` 由內而外且移除 `--deep`；`Makefile` 增列 dylib 的逐架構 `lipo`／Nexus 鏡像。以暫翻 `Tekkon` 為 `.dynamic` 實測：真連結→同捆＋rpath＋三層簽章全過；未連結（含殘留）→不進 app。**關鍵量測**：改掉 bundle ID 的 app 副本實跑，dyld 報 `Library not loaded: @rpath/libTekkon.dylib`／`different Team IDs`、退出碼 134——**它精準找到我方同捆的檔案，唯一異議是 library validation**，即管線正確、卡點純為簽章政策（屬 P199 §八 E3 事主定調項）。）。**追加檢證之四（同日）：`OSNeutralAssembly` 全併包**——事主裁定「併包（這就是新的 LibVanguard）」，遂把 7 個套件（`vChewing_BPMFVS`／`BrailleSputnik`／`Homa`／`LexiconAssembly`／`Shared`／`SwiftExtension`／`Tekkon`）以**同名 target** 併入 `Packages/vChewing_OSNeutralAssembly`（`git mv`，152 筆 rename；原目錄刪除；13 份授權檔保全到該套件的 `LICENSES/`，含與眾不同的 `SwiftExtension-LICENSE.txt`＝MulanPSL-2.0 與 ButKo 世系的 `BPMFVS-LICENSE.txt`），新 manifest 以**單一** `.dynamic` product 涵蓋 10 個出貨 target（測試素材／測試輔助另立動態 product 不進出貨庫），並更新 11 個 exe 側套件的 manifest。**SwiftPM 一次就過、無任何 duplication error**；產物只有一個 `libOSNeutralAssembly.dylib`（6.8 MB），app 的動態依賴為 `@rpath/libOSNeutralAssembly.dylib`：`swift test` **505 項 0 失敗**、`make debug`／`make release` 皆 ✅（Release 為 universal）、**dyld 實測由前一輪的 exit 134（`different Team IDs`）轉為「程序進入自身啟動流程、dyld 錯誤 0」**。資源 bundle 名隨併包改變，已同步三處（其中 `BPMFVS` 那處不改屬**靜默失敗**；測試靶的硬編碼名不改則該靶失敗）。簽章政策落地為「本地 ad-hoc 條件式注入 `disable-library-validation`（僅在有同捆 dylib 時）＋ 分發走 Team ID」。過程中修掉一個**僅 universal 才暴露**的 bug：`otool -L` 對 fat binary 每架構各印一次依賴，導致 `make release` 重複複製 dylib 而失敗——**教訓：管線改動必須用 `make release` 行使**。連帶更新 `project.pbxproj`（移除 7 條 wrapper fileRef ＋ 7 條 group child，`plutil -lint` OK）、CI 的 `packages=(…)`、`AGENTS.md` §1/§2，以及 `CLAUDE.md`／`algorithm.md`／`README*.md`／`DevLab`／`copilot-instructions.md` 共 29 處路徑。**追加檢證之五（同日）：E1 有答案、Xcode 路徑的啟動回歸已修**——Xcode **會**自動嵌入（`OSNeutralAssembly.framework` 形態）；但 ad-hoc ＋ hardened runtime 令 app 啟動即死（`different Team IDs`、134），對照組實驗定位為 library validation。解法是 Xcode 內建設定 `RUNTIME_EXCEPTION_DISABLE_LIBRARY_VALIDATION = YES;`（兩 target × 兩 config 共四處）；該鍵由簽章時注入、**不寫進** `vChewing.entitlements`，故 `BuildPKG.sh` 發佈件仍嚴格、無須改動。三 scheme 皆 BUILD SUCCEEDED、皆實跑存活。另修真 bug：`make` 安裝器 app 因未同捆 dylib／補 rpath 而一直 134，已抽共用函式與主 app 共用；修後 `make debug`／`make release` 皆 0。**追加檢證之六（同日）**：安裝器探針 3 次 2 次 SIGTRAP（133）；一度改以 `DispatchQueue.main.sync` 交棒，**經實測無效**（Release 仍 SIGTRAP）。**真根因**（〈追加檢證之七〉）＝`InstallerAssembly4Darwin` 設 `.defaultIsolation(MainActor.self)`，令事件處理器閉包成 MainActor 隔離、計時器卻在私有佇列，故事件於**閉包入口**即隔離檢查 SIGTRAP（閉包本體未執行，故 `main.sync` 無效；TIS 亦斷言主佇列）。**真修法**＝計時器改建 `.main`、直接呼叫 `installInputMethod()`、移除死碼 `taskQueue`（兩倉同構）。**機制已釘死**（探針 A/B：133／0）、**端到端已由事主實測確認**（兩款 Release 安裝器皆正常、輸入法本體正常）。**Phase 210 結案（2026-09-14，事主驗收）**：`make pkg-signed` 全流程全綠（退出碼 0、156 秒，`notarytool` Accepted、`stapler validate` 通過），notarized 產物已實裝於系統並正常運作。唯一殘留：Windows `LNK4217` 噪音未解（屏蔽未生效、已撤、原因未查）。此前一段（**Phase 209 ＝「安裝程式的 App Translocation 偵測除役：改『直接安裝、失敗即退避重試』（兩倉）」**：該段邏輯溯源為 OpenVanilla `Source/Mac/Installer/AppDelegate.m`（Lukhnos Liu，2016-12-30 `db8548a6`），後抽出為 `Source/Mac/Installer/BundleTranslocate.{h,m}`，經 fork 承襲為唯音的 `Reloc.isAppBundleTranslocated` ＋輪詢流程。改後**不再偵測「是否被轉置」**——轉置映像為唯讀，寫入必然失敗，故「複製失敗」本身即為可靠的等待訊號：新流程＝killall → 掃描／改名／進垃圾桶 →（必要時提權改名）→ 立即 `FileManager.copyItem` 至同目錄隱藏暫名 `.vChewingInstallStaging-<UUID>.app` → `moveItem` 原子就位；失敗則清暫存、每 0.5 秒重試、首次失敗才亮面板、60 秒逾時方放棄。**落點**：`vChewing-macOS` 改 4 檔、刪 `RelocationDetector.swift` 與其測試、新增 `InstallerStagingTests.swift`（5 項）、i18n 4 檔；`vChewing-OSX-legacy` 改 4 檔、刪 `Installer/Modules/RelocationDetector.swift` 並退 `project.pbxproj` 4 處接線、i18n 4 檔。**識別子換軌**：`kTranslocationRemovalTickInterval`（實測死碼）與 `shouldWaitForTranslocationRemoval` 與 `InstallerUIConfig.isTranslocationFinished`（實測唯寫零讀取）刪除；`kTranslocationRemovalDeadline` → `kInstallRetryInterval`／`kInstallRetryTimeout`；`translocationTimer` → `installRetryTimer`；`installInputMethod(previousExists:previousVersionNotFullyDeactivatedWarning:)` → `installInputMethod()`；新增 `retryDeadline`／`retrySecondsRemaining`。**i18n**：新增 `i18n:Installer.OldVersionStillInUse`，`AlertType.postInstallAttention` → `.oldVersionStillInUse`。**未驗**：真實 Gatekeeper 轉置情境之端到端安裝。）。此前一段（**Phase 208 ＝「兩倉『剩餘部分』與 HomePage 之授權換軌 `MulanPSL-2.0`，暨 VanguardLexicon 由 3-Clause BSD 換軌（四倉）」**：（一）**標頭**：`vChewing-macOS` 179 檔、`vChewing-OSX-legacy` 114 檔，7 行 MIT-NTL banner（兩倉 216 處）＋`Copyright (c)` 變體 4＋3 行短版 1＋**vChewing 自有 plain MIT banner 12**（IMKSwift 族，兩倉各 6 檔）＋Jad 檔內嵌歸屬行 2＋縮排之 `TISInputSourceExtension` MARK 2，一律換為 3 行 `MulanPSL-2.0` 形制、年份 `2022`；第三方歸屬（Mzp／Isaac Xen／OpenVanilla／Nick Chen／Yahoo 奇摩／LibTaBE／Qwertyyb／OpenCC）不動。（二）**倉級授權檔**：`LICENSE.txt` 改持官方中英雙語全文（逐位元組沿用 P207 之 SwiftExtension LICENSE；**2026-09-14 追加任務已改為英文單語**，`LICENSE.txt`／`-CHS`／`-CHT`／`-JPN` 自此一語言一檔，見 `Reqs_0201-0210.md` Phase 208〈追加任務之二〉）、`LICENSE-CHS`／`LICENSE-CHT` 為官方簡中／OpenCC `s2t` 繁中、`LICENSE-JPN` 為本 phase 自撰之非官方日文譯本；`CREDITS`／`COPYING` 之 SPDX 改 `MulanPSL-2.0` 並更正 `./LICENSES.txt` 筆誤。（三）**README〈應用授權〉**三份改寫，併同更正鐵恨 Tekkon 之既有誤載（原 macOS 寫 `AGPL-3.0-or-later`、legacy 寫 `MIT-NTL`，實皆 `LGPL v3.0 or later, with Swift App Development exception`）。（四）**使用者可見授權散文**（P207 一度改寫後撤回之案）落地：`DISCLAIMER_TEXT` 16 檔、`LICENSE_TITLE` 16 檔、`CFEULAContent` 17 檔。（五）**VanguardLexicon** 原始碼標頭 31 檔由 `BSD-3-Clause` 改 `MulanPSL-2.0`（年份維持 `(c) 2021`）、三份 `LICENSE` 重建、`README.MD`〈授权〉段改寫；`Sources/VanguardTrieKit/`（LGPLv3）不動。（六）**HomePage.io** 3 檔（`README.md`／`manual/preferences.md`／`techdocs/TechnicalWhitePaper-AIWritten.md`）；`ReleaseNotes.md` 之歷史記述不動。**未動／待裁**：`vChewing-LibVanguard` 不在範圍，其 `BSD-3-Clause` 測試資料標頭與已換軌之上游不一致（跨倉同步債）。**〈追加任務 2026-09-13〉**：木蘭條款「如何將本條款應用到您的軟體」範例段之三個佔位符已於 P207／P208 全部受 MulanPSL-2.0 保護之內容中填補（共 14 檔 20 處：`SwiftExtension` 三份全文副本各 2 處取 `2022`／`The vChewing Project`／`SwiftExtension`；兩倉倉級授權檔 10 處取 `2021`／`The vChewing Project`／`vChewing for macOS`；VanguardLexicon 4 處取 `2021`／`The vChewing Project`／`Vanguard Lexicon`；`[Year]` 取各該檔自載之首次發表年，`SwiftExtension` 三份無自載著作權列故改取其標頭之 `(c) 2022 and onwards`），填後三份副本與兩倉倉級四檔仍各自逐位元組相同；**2026-09-14 追加任務之二／同日第二輪則把該 20 處佔位符整段換掉**——四份譯本刪去 How-to-Apply 段後，14 檔已無範例段（三份 `SwiftExtension` 副本自此與事主英譯本逐位元組相同），`LICENSE.txt` 等改為英文單語，日語稱謂統一為「木蘭寛容許諾文書」；**同日第三輪**再把兩倉倉級授權檔尾端之 `[The previous upstream EULA contents have been removed …]` 一段除役（事主：留著只會被解讀成蹭 OpenVanilla 的知名度），連帶把 App 端同文副本 `CFUpstreamEULAContent`（4 份 `Info.plist`）與其 8 檔 Swift 消費端（`eulaContentUpstream`）整條拆除。）。此前一段（**Phase 207 ＝「以 `OSNeutralAssembly` 為終末點之全部內容換軌 `LGPL-3.0-or-later`（兩倉）」**：閉包＝`OSNeutralAssembly` 及其 7 個遞移相依 SPM package（`LexiconAssembly`／`Homa`／`Shared`／`Tekkon`／`BrailleSputnik`／`BPMFVS`／`SwiftExtension`），即 `Packages/vChewing_OSNeutralAssembly/Package.swift` 依賴清單所界定者；閉包外（`Shared_DarwinImpl`／`OSFrameworkImpl`／`IMKUtils`／`MainAssembly4Darwin`／`SettingsUI`／`CandidateWindow` 等）一律維持 MIT-NTL。標頭統一為 3 行、年份 `2022`（事主更正：原誤作 `2025`），故既有 60 檔 `(c) 2025` 之 LGPL 標頭亦一併歸一（macOS：Homa 25／TrieKit 12；legacy：Homa 16／TrieKit 7）。落點：macOS 169 檔（165 `.swift` ＋ 4 `README.md`）、legacy 102 檔；`Package.swift` 8 檔、`BSD-3-Clause` 測試資料 2 檔（`TrieKit_TestData.swift`／`Homa_TestData.swift`）、3 檔第 2 行之 `StringView Ranges extension by (c) 2022 and onwards Isaac Xen (MIT License).` 第三方歸屬行均明列不動；3 檔原無標頭者（`BPMFVSTests.swift`、`LXAssemblyMaterials4Tests/{TestData,LXATestStringData}.swift`）補上。legacy 因 `Shared/` 係合併式目錄（`vChewingComponents/Shared/` 與 `Shared/Extensions/` 皆混裝閉包內外之檔案），改以「basename 是否存在於 macOS 閉包對應 package 的 `Sources/`」逐檔判定，留白 17 檔經反查全屬閉包外。**未動／待裁**：LibVanguard 側 TrieKit／Homa／BrailleSputnik 仍為 `(c) 2025`（與兩倉僅差標頭年份一行）；16 處使用者可見授權散文（`Localizable.strings`）與兩倉 `README.md`〈應用授權〉段、`LICENSE.txt` NOTE、`COPYING` 未動、現已不完整；新納入 LGPL 之 5 個 package 尚無 `LICENSE`／`CUSTOM_LGPLv3_EXCEPTION.md`。）。前一段（**Phase 206 ＝「LibVanguard 的 FCITX 相容項遷入兩倉（盤點補漏）＋ LibVanguard 切除 `SharedCore` 與 `CandidateKit` 兩模組」**：（一）事主已先將 `KBEvent_FCITX.swift`（101 條 Fcitx5 → Darwin 鍵碼對映 ＋ `FCITX5FlagsOfModifier`）遷入 macOS `Packages/vChewing_Shared/Sources/Shared/` 與 legacy `Shared/vChewingComponents/Shared/`（含 legacy `project.pbxproj` 之 3 處接線、鍵盤映射屬性由 LibVanguard 的 `mapTableDarwin` 改回兩倉的 `mapTable`、`ModifierFlags` 同步加 `Sendable`），本 phase 盤點後補齊三項：① 兩份新檔仍逐字帶 LibVanguard 的 LGPL 標頭 → 改掛兩倉通用的 7 行 MIT-NTL 標頭（起始年沿用 2025）；② legacy 檔內的 `import SwiftExtension` 為**致命錯誤**——legacy 係單一 Xcode target、倉內不存在該模組（`Shared/Extensions/SwiftExtension.swift` 只是同 target 的一個來源檔），實測 `make debug-core` 以 `error: no such module 'SwiftExtension'` 失敗（exit 65），已刪該行；macOS 端同名 import 則正確保留（該倉 `SwiftExtension` 是獨立 SPM 模組，`Shared.swift` 的 `@_exported` 僅在該檔內生效）；③ 對映表零測試覆蓋 → macOS `Tests/SharedTests/SharedTests.swift` 新增 `testFcitxKeyboardEventConversion`。（二）LibVanguard 以 `git rm -r` 刪 `Sources/_Modules/SharedCore/`、`Sources/_Modules/CandidateKit/` 與兩個零 `@Test` 的空殼測試靶（4 目錄 7 檔），`Package.swift` 退 6 處（2 個 `Product.library`、2 個 target、2 個 testTarget、`LibVanguard` 聚合 target 的 2 條依賴、及成孤兒的 `// Basic components ---` 註記）。動工前實測 `KBEvent`／`KBEventProtocol`／`KeyCode`／`KeyCodeBlackListed`／`CandidateCellData`／`CandidateKit` 於 LibVanguard 模組自身目錄以外**出現次數皆為 0**（`CandidateKit` 的全部內容就是 `@_exported import SharedCore` ＋ 空命名空間 `public enum CandidateKit {}` ＋ 無消費端的 `CandidateCellData` class；`Research/Phase137_PostResearch.md:179` 早有「全倉零消費端（死碼）」之紀錄），且 `CandidateKit → SharedCore` 是唯一的模組內依賴邊，故兩者必須同 phase 同進退。切除後模組集合為 LibVanguard／TrieKit／Tekkon／Homa／BrailleSputnik／LexiconKit／SwiftExtension，測試靶 8 → 6（合計 140 項）。`SharedCore` 的 `KBEvent`／`KBEventProtocol` 在 LibVanguard 原本就是**無抽象消費者的空殼**（對照 macOS 的 `InputSignalProtocol` 是整個 `InputHandler` 家族的事件抽象接縫）；兩倉各有獨立副本、與 LibVanguard 無 SPM 依賴關係，故下游零波及。**〔同日驗收追加〕**：另一輪 Deepseek 驗收後補四事——① **年份更正**：原把本即為 `(c) 2025 … LGPL` 的 `Homa`／`TrieKit` 一併歸一為 `2022`，與事主「`Homa` 的年份還是得 `2025`」之指示相違；已回復 `2025`（macOS 37 檔／legacy 23 檔），三倉 `TrieKit` 七檔 sha256 隨之**再度全等**。② **`SwiftExtension` 改掛 `MulanPSL-2.0`**（macOS／legacy／LibVanguard 三倉一體）：該模組係通用 Swift 工具函式，採 LGPL 將無法與其他專案混用；三倉各置授權全文（macOS `Packages/vChewing_SwiftExtension/LICENSE`、legacy 倉根 `LICENSE-MulanPSL-2.0.txt`、LibVanguard `LICENSES/preferred/MulanPSL-2.0`），LibVanguard `COPYING` 另記該例外。③ **補齊授權附帶檔**：macOS `OSNeutralAssembly`／`LexiconAssembly`／`BrailleSputnik`／`Shared` 補 `LICENSE`＋`CUSTOM_LGPLv3_EXCEPTION.md`、`BPMFVS` 補例外宣告；legacy `OSNeutralAssembly`／`LXAssembly`／`Tekkon`／`BrailleSputnik` 比照（其 `Homa` 原即已有此二檔；混授權目錄 `Shared/`／`Extensions/` 不置目錄級授權檔）。④ **使用者可見授權告示**：一度改寫 20 檔（16 處 `DISCLAIMER_TEXT`、兩倉 `LICENSE.txt` NOTE、兩倉 `README.md`〈應用授權〉段），惟事主複核裁定「確屬必要、但納入本 phase 會使手術範圍過大」，故**全數撤回、另案處理**。⑤ **事主第二輪複裁（標頭年份之最終定案，三倉一體）**：`Homa`／`TrieKit`／`BrailleSputnik` 維持 `2025`；`vChewing-LibVanguard` 其餘一律改 `2022`（15 檔）；兩倉 `BrailleSputnik` 由 `2022` 改 `2025`（新變更而非回復）；`BSD-3-Clause`（2021，語料出處）與 `E-TEN`（1985，Public Domain）兩種標頭不動。另修正 legacy 檔名筆誤 `lxPlainBopomopo` → `lxPlainBopomofo`。）。前一段（**Phase 205 ＝「`LangModel`／`langModel` 殘端清剿 ＋ Homa 測試 `TestLM` 族換軌」**：取代規則 `LangModel` → `Lexicon`、`langModel` → `lexicon`、`LM` → `LX`（限測試靶）。151 處符號名落網（三倉 35 檔）——`Shared.InputMode.langModel` → `.lexicon`（連帶 `mode.lexicon`／`inputMode.lexicon`／`IMEApp.currentInputMode.lexicon` 全體呼叫端）、`LangModelCache` → `LexiconCache`、`resetLangModelCache(forUnitTests:)` → `resetLexiconCache`、`initUserLangModels()` → `initUserLexicons()`、`targetLangModels` → `targetLexicons`、legacy `project.pbxproj` 群組註解 `LangModelManager` → `LXManager`；另 541 處測試符號（macOS／LibVanguard 42 檔）——`TestLM` → `TestLX`、`mockLM` → `mockLX`、`strLMSampleData*` → `strLXSampleData*`、`textMapTestCoreLMData` → `textMapTestCoreLXData` 等 22 名（限測試靶；動工前實查其在生產碼出現次數皆為 0）。**未動**：`currentLM`（生產屬性，macOS 434／legacy 86，列〈待定 F〉待裁）、`vCLMLog`、`scoreFromLM`、`syncLMPrefs` 族、`LMGetKbdType`、註解散文中的「LM」。前一段（Phase 201~204 ＝「`LM` 系命名全面換軌至 `LX`」，主戰場在 `vChewing-macOS` 與 `vChewing-OSX-legacy`：`Typewriter` 模組 → `OSNeutralAssembly`、`LangModelAssembly` 模組 → `LexiconAssembly`、`LMAssembly` 命名空間與其全體成員 → `LXAssembly.*`、`LMInstantiator`／`LMI` → `LXFacade`、`LookupHub` → `LXQuerier`（存取子 `lookupHub` → `lxQuerier`）、`LMMgr` → `LXMgr`（含 `LangModelManager/` → `LXManager/`）、小寫 `lm*` 家族 → `lx*`；清剿對象不限識別子，含註解、日誌字串、在地化鍵、測試標籤與兩份 `project.pbxproj`。**LibVanguard 本倉**除同步上述 3 處文字引用外，另有 `Lexicon.LMPlainBPMF` → `Lexicon.LXPlainBPMF`（檔名 `LX_LXPlainBPMF.swift`）與 `lmPlainBPMFData` → `lxPlainBPMFData`）。**同日第二輪（事主驗收後追加）**：① `LMPlainBPMF` → `LXPlainBPMF`（承前）；② macOS 兩個 Linux／Windows CI workflow 的 `name:` 與 job `name:` 改用 `OSNeutralAssembly`；③ **API 壓縮**——`LXFacade` 根型別 10 個查詢方法降為 internal、`LXQuerier` 增設 6 個公開查詢方法，生產碼 52 處改用 `lxQuerier`，使 `LXQuerier` 成為唯一公開查詢面（根型別 public func 24→14、querier 6→12、公開查詢方法總數 30→26）；④ 參數名／區域變數 `lm` → `lx`、`lmTest` → `lxTest`（兩倉 13 檔；Tekkon 測試資料表內的 `lm` 為鍵盤對映資料、明令排除）。前一段（Phase 200 ＝「徹底割除 SQLite 與 Plist 支援」）之記載：範圍涵蓋 `vChewing-LibVanguard` 與 `vChewing-VanguardLexicon` 兩倉：前者移除 TrieKit 的 `VanguardTrie.SQLTrie`／`TrieSQLScriptGenerator` 與本地 `CSQLite3` 套件，以及 `TrieIO` 的 plist 入口、`TrieHub` 的 `sqlTrieMap`／`plistTrieMap` 與其 update 介面、`Trie`／`TNode`／`Entry` 的 `Codable` 與只服務 plist 的讀音鍵加解密層；後者移除 SQL 與 plist 兩組建置目標、對應 BuildTool Plugin、data builder 與 `compileSQLite(...)`。**現存 Trie 資料來源與序列化格式僅餘 Vanguard Pragma TextMap 一途**，另有純記憶體型 `VanguardTrie.Trie`）。


---

## 十二、AI Agent 反應模式（Response Pattern）

> 本節供 AI Agent 參考，當用戶提出新的 Phase 開發任務時，應遵循以下標準流程。

### 12.1 工作流程（Workflow）

當用戶提出新的 Phase 需求時，按以下順序執行：

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: 讀取相關檔案                                           │
│  - 讀取用戶指定的 Phase 描述                                    │
│  - 讀取需要修改的原始碼檔案                                     │
│  - 確認現有實作與新需求的關聯                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: 程式碼實作                                             │
│  - 根據需求實作功能                                             │
│  - 遵循專案現有程式碼風格（Swift 6, @MainActor, 層級結構）      │
│  - 複用既有組件和工具函數                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: 編譯驗證                                               │
│  - swift build（於 vChewing-LibVanguard/）                      │
│  - 確保無錯誤、無警告                                           │
│  - 如有錯誤立即修復                                             │
│  - 然後用 Xcode build 觸發 localized strings key 生成           │
│  - 完成 localization 之後再次重試編譯                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: 文件同步（並行執行）                                   │
│  ├─ vChewing-DevLogs/Reqs4LLM: Phase 規格與實作備忘錄           │
│  ├─ vChewing-DevLogs/DevReqsHistory.md: 追加 Phase 歷史摘要     │
│  ├─ vChewing-DevLogs/KnowledgeMemo4LLM.md: 更新標記與近期摘要   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 5: 彙總報告                                               │
│  - 列出所有變更的檔案                                           │
│  - 說明核心實作邏輯                                             │
│  - 確認編譯狀態                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 12.2 文件更新規則

| 檔案 | 更新時機 | 內容格式 |
|------|---------|----------|
| **vChewing-DevLogs/Reqs4LLM/Archive_P201-P300/Reqs_0201-0210.md**（現行卷） | 每個 Phase 必須 | `# Phase XX` 標題 + 規格說明 + `## Phase XX 實作結果` 小節（歸檔卷不再增改，回填規則見 §12.5） |
| **vChewing-DevLogs/DevReqsHistory.md** | 每個 Phase 必須 | 追加一行 `\| Phase XX \| 簡短描述 \|` |
| **KnowledgeMemo4LLM.md** | 每個 Phase 必須 | 更新 `最後更新` 標記，並按需要補上近期 phase 摘要或修正過時指引 |
| **UserGuide (4語言)** | 影響使用者操作時 | 在對應章節添加功能說明（鍵盤熱鍵、滑鼠操作等） |

### 12.3 程式碼實作原則

1. **最小變更原則**：只做必要的修改，不改動無關程式碼
2. **風格一致性**：
   - **勿在 Swift 原始碼正文內提及 Phase 編號**（各 Phase 規格之既有要求；原「使用 `// Phase XX:` 註解標記新代碼」之指引與此相牴觸，已於 Phase 210 更正）
   - 遵循現有命名慣例（如 `handleXxx`, `onXxx`）
   - 保持縮排和空行風格
3. **平台相容性**：
   - (今後會按需求補充)
4. **狀態管理**：
   - (今後會按需求補充)

### 12.4 常見任務類型

| 任務類型 | 典型檔案 | 注意事項 |
|----------|----------|----------|

### 12.5 回報格式範本

> 注意**各個 Phase 檔案內部的標題層級**：一級標題（`#`）僅保留給 `# Phase NNN`；`--------------------` 大分隔線亦僅保留於 Phase 之間。

```markdown
## 變更總結

### 程式碼變更

**檔案名稱.swift**:
- 變更項目 1
- 變更項目 2

### 文件更新

| 檔案 | 更新內容 | 注意事項 |
|------|---------|---------|
| vChewing-DevLogs/Reqs4LLM/Archive_P201-P300/Reqs_0201-0210.md（現行卷） | 新增 Phase XX 規格與實作備忘錄 | 一律寫入**現行卷**；**歸檔卷**（已滿 10 個 Phase、或經事主裁定結案者）不再增改。分卷以「十年前綴 `0NN1`–`0NN0`」為界、每卷至多 10 個 Phase（如 `Reqs_0201-0210.md` 收 Phase 201~210）；**同一區間內的新 Phase 續寫同一卷，不另立新卷**。該區間寫滿或結案後始自立為一卷，並置於 `Archive_P{首 Phase 所屬百位段}/`（現行卷尚未寫滿，故雖置於 `Archive_P201-P300/` 內仍屬現行卷）。若本次 Phase 牽涉某歸檔卷所載之記錄，須於該歸檔處補記一行簡述（何時／哪個 Phase／何種修改）。 |
| vChewing-DevLogs/KnowledgeMemo4LLM.md | 根據專案實際情況更新內容（如適用） | 參考既往記錄的文書風格。 |
| vChewing-DevLogs/DevReqsHistory.md | 新增 Phase XX 到開發階段歷史 | 參考既往記錄的文書風格。 |

### 編譯狀態

✅ 編譯通過 / ❌ 有錯誤（說明）
```

### 12.6 工作細節附註

- 交差前注意與 L10n 有關的內容是否全部補齊。
- 如果要使用 tmp 目錄的話，請使用**所在倉庫根目錄**的 `tmp/` 或 `.tmp/`（例如 `vChewing-LibVanguard/tmp/`、`vChewing-DevLogs/tmp/`）、而非系統的 `/tmp/`。使用 `/tmp/` 這種 out-of-workspace 的路徑會迫使事主每次都得手動設定存取權限，非常麻煩。
- **腳本內的全形字元陷阱（2026-09-13 實錄）**：本工作區的 shell 常以 `LC_ALL=C` 執行，此時 bash 會把緊接在全形字元（如 `（`）之前的高位元組**一併視為變數名的一部分**——`log "…：$scheme（Debug）…"` 會以 `scheme\xef…: 未綁定的變數` 失敗（在 `set -u` 下直接中止腳本，且若已把 stderr 重導就完全看不出原因）。**凡變數緊鄰 CJK／全形字元者，一律寫成 `${var}`。** 同源的坑：Python `re` 的 `\b` 視中日韓字元為 word character，故 `\bTypewriter\b` 不會命中 `Typewriter的`（見 `DevReqsHistory.md` Phase 201 的〈方法學教訓〉）；腳本化取代 CJK 文本時請改用 `(?<![A-Za-z0-9_])`／`(?![A-Za-z0-9_])` 這類斷言。
- **跨平台 manifest 的 `platforms:` 陷阱（2026-09-14 實錄）**：`Package(platforms:)` 之參數型別是 **`[SupportedPlatform]?`**，故 **`platforms: []` 會讓 SwiftPM 報 `supported platforms can't be empty`**（Linux／Windows CI 當場炸），正解是 **`platforms: nil`**（＝不宣告平台限制）。要條件編譯分流者，須把變數宣告為 **optional**：`#if canImport(Darwin) let p: [SupportedPlatform]? = [.macOS(.v12)] #else let p: [SupportedPlatform]? = nil #endif`。**此誤在本機（Darwin）永遠測不出來**——務必以「把 `canImport(Darwin)` 反轉為假」的 scratch 套件預驗（見 `_MainWorkspace/tmp/p215LinuxSim` 之做法），或直接等 CI。
- 如果事主丟給您 Xcode Instruments Profile 檔案的話，可能會是二進位檔案，請先嘗試用 `xctrace export` 解讀成 XML 再分析。
- 每次交差之前「一次性運行」 `make lintFormatUncommitted` 這一條命令。如果事主明確要你對某些工作單獨 commit 的話，每次 commit 之前也請運行 `make lintFormatUncommitted`。該命令僅對 tracked 且未 commit 的 Swift 檔案下動作，且依序執行 SwiftLint 自動修正與 SwiftFormat（先 lint 後 format 的順序已內建）。但注意在運行之後也請 build 一次，因為這些自動修正對 `count == 0` 的範式會頑固地轉換成 `.isEmpty` 而不會檢查相關 type 是否有 `isEmpty` 這個 read-only property。發現這種情況的話，可以用 `.count * 1 == 0` 取代之。commit 的 title 格式是 `ModuleName // WhatHasBeenFixed`、且在 commit 的 description 的末尾單獨一行寫明 `(Phase XXX - Task YYY)`。Commit Titles 始終以 ASCII Period `.` 結尾，除非是疑問句時才使用 `?` 結尾。
- 給 ChatGPT、Codex、Raptor 的特殊指令：Do not intentionally stop with tasks incompleted to ask me whether you do the next step I already told you. Just do it. I have backups, hence no worry. // Claude、Kimi、GLM、QwenCoder 從來不用問這些問題，就能把這些事情全部做好。唯獨 ChatGPT、Codex、Raptor 不這樣，就很奇怪很令人窩火。

---

> ⚠️ **注意**: 本文檔需要定期更新以反映最新程式碼狀態。如有發現過期內容，請及時修正。
