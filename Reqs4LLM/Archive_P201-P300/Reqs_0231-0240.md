# Phase 231

Assignee: Deepseek-v4-flash（Codewhale harness）。

> **狀態：定案（2026-09-19；本機實測 ＋ 事主實機覆驗通過）。** GSI 捲動模式下「橫向排版、縱向展頁」的視口最大高度，其唯一來源是 `CandidatePool4AppKit.lineStep`；該值原為建池時自**靜態範本 cell** `shitCell.textDimension.height` 取得的快照，而範本 cell 的 `textDimension` 只在該靜態成員**初次初始化**時依當時字級定影一次——行程存活期間更動候選字字級不會更新它。故 `pageCandidateSize.height`（＝`lineStep × maxLinesPerPage`）連同 `maxScrollOffset`／`scrollerThumbRatio`／`snapScrollOffset`／`scrollByLines`／`scrollToMakeLineVisible` 全部停在舊字級。TDK（非捲動）分支量的是實際排版高度、縱向排版量的是實際排版寬度，故皆不受影響——此即事主所述「僅出現在 GSI 分支」與「vertical layout 橫向展頁無此故障」之由來。修法：把該快照改為即時讀取 `CandidateCellData4AppKit.unifiedTextHeight` 的計算屬性（單檔 ＋6／−1）＋回歸測試 1 支（＋42 行）。**已 commit（事主 finalize，2026-09-19）：`vChewing-macOS` `9fb6b94c`（`TDKCandidates // FIx ScrollView height issues with large font sizes.`，2 檔 ＋48／−1）。** 另註：同一內容之舊 hash `c91c4e06` 已經事主 rebase，現行 hash 為 `9fb6b94c`（兩者之 `--stat` 逐項相同）。

## 前導 Phase

無。落點（`lineStep`／`pageCandidateSize` 與其消費端）係 GSI 捲動模型之自有成員，自 `35668fde`（2026-05-25，〈GSICandidates // Initial implementation.〉）引入即如此；而 `cellTextHeight` 之快照式寫法可上溯至 `39bad0a2`（2026-02-09，〈TDKCandidates // Refactor delegation APIs & Simplify implementation.〉）。即本故障自 GSI 落地起即在，**故障窗口 4.5.1（`git tag --contains 35668fde` 之首個含它者）～ 4.8.2（現行 HEAD）**。另：`8d86632a`（新增未完成讀音專用 pane）與 `5c4060cc`（填充行寫回守衛）先後動過同一區段之鄰近邏輯，但均未觸及 `lineStep` 之取值來源。

## 手術範圍

- `vChewing-macOS`（單倉；`vChewing-OSX-legacy` 已封存且無此模組）：
  - `Packages/vChewing_CandidateWindow/Sources/TDK4AppKit/CandidatePool4AppKit.swift`
  - `Packages/vChewing_CandidateWindow/Tests/TDK4AppKitTests/TDK4AppKitTests.swift`

## 要解決的問題

事主原文：

> 選字窗在 horizontal layout 狀態下縱向展頁時的最大高度並沒有隨著 candidate font size 而遞增。
>
> (vertical layout 橫向展頁時沒有類似的故障。)

動工中事主補充定調：

> 這個問題很可能僅出現在 GSI 分支。

## 病灶定位（探針實證）

`CandidatePool4AppKit` 有兩個**行程級**的靜態範本 cell（`static var shitCell`／`static var blankCell`，用於量測最小 cell 尺寸與填充行）；`CandidateCellData4AppKit.init` 會依當下的 `unifiedSize`（＝偏好 `candidateListTextSize`）把 `textDimension` 算好**存進 struct**，此後 `updateMetrics` 只寫 `visualDimension`、不回寫 `textDimension`。於是範本 cell 一旦在字級 A 之下被初次存取，其 `textDimension` 便在該行程內定影於 A。

`lineStep`（「單行高度（horizontal matrix）」）原本寫成 `cellTextHeight + 2 * padding`，而 `cellTextHeight` 是 `let cellTextHeight = CandidatePool4AppKit.shitCell.textDimension.height`——**建池時的快照**。`lineStep` 只被橫向排版的捲動模型消費：

- `pageCandidateSize` 之橫向分支 → `lineStep × maxLinesPerPage`（＝捲動視口高度，即使用者所見的視窗最大高度）
- `maxScrollOffset` 之橫向分支 → `candidateOnlySize.height − lineStep × maxLinesPerPage`
- `scrollerThumbRatio`、`scrollByLines`、`snapScrollOffset`、`scrollToMakeLineVisible`

**橫向排版為何以快照式算式表達視口高度**（此即故障只落在此處之結構性原因）：

- TDK（非捲動）分支之視窗高度取自 `metrics.fittingSize`——由 `layoutCells` 逐一量測排出的 cell 高度相加，故必然跟隨字級。
- 縱向排版之捲動視口量的是 `candidateLines[page]` 各列**實測**寬度與高度，故亦必然跟隨字級。
- 唯獨橫向排版的捲動視口以 `lineStep` 這個**合成值**表達（設計意圖正確：視口恆為 `maxLinesPerPage` 行高，末頁不足行數時視窗不縮水），而該合成值取自定影快照 ⇒ 字級一變即與真實版面脫節。

**探針實證（35 個候選、`lines: 4`、`isExpanded: true`、`layout: .horizontal`；先在字級 16 之下定影範本 cell，再改字級）**：

- 字級 16：`unifiedTextHeight` 19.0、`lineStep` 23.0、`pageCandidateSize.height` 92.0、實測 cell 高 23.0、第二行原點 y 25.0 ✓ 一致。
- 字級 40（術前）：`unifiedTextHeight` 48.0（**已即時更新**）、`lineStep` **23.0（未動）**、`pageCandidateSize.height` **92.0（未動）**，而實測 cell 高 **42.0**、第二行原點 y **44.0**、`candidateOnlySize.height` **312.0** ⇒ 視口高度只有內容真實高度的三分之一弱，且 `maxScrollOffset` 反被算成 220.0（應為 104.0）——捲動範圍連帶失真。
- 字級 40（術後）：`lineStep` 52.0、`pageCandidateSize.height` 208.0、`maxScrollOffset` 104.0 ✓ 與實測行距 52.0 逐位吻合。

**GSI 視圖層之同一量測（`VwrCandidateGSI4AppKit.fittingSize`，`rendersInScrollMode = true`）**：字級 16 ⇒ `(350.0, 115.0)`；字級 40 術前 ⇒ `(746.0, 137.0)`（高度只多 22 行點——那 22 點是底部欄位隨字級長出來的部分，視口本身紋絲未動）；字級 40 術後 ⇒ `(746.0, **253.0**)`。此即事主所述「最大高度並沒有隨著 candidate font size 而遞增」之直接再現。

## 落點

`CandidatePool4AppKit.swift`：把 `cellTextHeight` 由 stored 快照改為計算屬性，即時讀取全域通用的字級換算（與 `CandidateCellData4AppKit.updateMetrics` 內 `cellDimension.height = unifiedTextHeight + 2 * padding` 同源），並在原地註明「不得自範本 cell 取值」之理由。

```swift
    /// 候選字 cell 的文字高度（跟隨偏好中的候選字字級即時浮動）。
    /// 不得自範本 cell（`shitCell`）取值：範本 cell 的 `textDimension` 只在該靜態成員初次
    /// 初始化時定影一次，行程存活期間更動字級不會更新它，一旦快照化即會讓行步進（連帶
    /// 捲動模式的視口高度）停在舊字級。
    var cellTextHeight: CGFloat { CandidateCellData4AppKit.unifiedTextHeight }
```

`lineStep`（`cellTextHeight + 2 * padding`）與其餘五個消費端一行未動——修在單一真源上，五處同步受惠。刻意**不**改 `pageCandidateSize` 之算式：`lineStep × maxLinesPerPage` 表達「視口恆為整頁高」這一設計意圖（末頁不足行數時不縮水）本身正確，病灶只在取值來源。

## Phase 231 實作結果

### 程式碼變更

**`Packages/vChewing_CandidateWindow/Sources/TDK4AppKit/CandidatePool4AppKit.swift`**（＋6／−1）：

- 移除 `let cellTextHeight = CandidatePool4AppKit.shitCell.textDimension.height`。
- 新增計算屬性 `var cellTextHeight: CGFloat { CandidateCellData4AppKit.unifiedTextHeight }` ＋ 定影陷阱之註記。

**`Packages/vChewing_CandidateWindow/Tests/TDK4AppKitTests/TDK4AppKitTests.swift`**（＋42）：

- 新增迴歸測項 `testGSILineStepFollowsCandidateFontSize`（置於「GSI 捲動模型測試」節之首），四條斷言：①行步進＝當前字級下之實際行高；②捲動視口高度＝該行高 × `maxLinesPerPage`；③行步進與**實測**行距（第二行原點 y − 第一行原點 y）一致；④捲至末行時 `scrollOffset` 恰等於「內容高 − 視口高」。
- 測項自身先在環境字級下定影範本 cell、再調高 24 級建池，以模擬「使用者於 IME 行程存活期間調大候選字字級」——此即病灶的觸發條件，故該測項不依賴測試執行順序。

## 驗證（2026-09-19，本機實測）

### 紅綠雙驗

- **紅（術前版原始碼，測項不動）**：`swift test --no-parallel --disable-sandbox --filter testGSILineStepFollowsCandidateFontSize` ⇒ **恰 4 筆失敗、逐筆命中病灶**——`lineStep` 23.0 對 52.0；`pageCandidateSize.height` 92.0 對 208.0；`lineStep` 23.0 對實測行距 52.0；`abs(scrollOffset − (candidateOnlySize.height − pageCandidateSize.height))` 差 **232.0**。
- **綠（修後）**：同一命令 ⇒ 1 支全過。

### 全靶測試與建置

- 套件測試（Swift 6.4）：`swift test --no-parallel --disable-sandbox` ⇒ **21 支 ／ 1 套全過**（術前 20 支；新增者即本 phase 之回歸測項）。
- 套件建置：`swift build --disable-sandbox` ⇒ `Build complete!`。
- 倉根建置（IME 全鏈）：`swift build` ⇒ `Build complete!`（既有之 `libswiftCore.dylib` 部署目標 ld 警告與本 phase 無關）。
- Swift 5.10 側：`cd Packages/vChewing_CandidateWindow && make build510`（5.10.1 toolchain ＋ `MacOSX13.3.sdk` ＋ `x86_64-apple-macosx10.9`）⇒ `Build complete!`（既有之 `_maxLinesPerPage` 併發警告與本 phase 無關）。

### 格式管線

`make lintFormatUncommitted`（倉根）`rc=0`：SwiftLint 對新碼補 `trailing_comma` 7 處（原始碼檔）與 8 ＋ `void_return` 1 處（測試檔），SwiftFormat 格式化 2／2 檔；**連跑第二次 md5 不變**，且格式化後之建置與全靶測試複跑仍全綠（無人為 `count == 0` 被誤改為 `.isEmpty` 之情事——本改動未含此範式）。受改兩檔 md5：`CandidatePool4AppKit.swift` `fe28e8ea05df93dc39e9708180e50de7`、`TDK4AppKitTests.swift` `39461169e510e3c8716359b1f1427878`。

## 未驗

- **真機 IME 目視**：**已閉合（2026-09-19，事主實機覆驗）**——事主原話：「問題成功解決，可以定案了。」即「GSI 選字窗於橫向排版、縱向展頁時，最大高度隨候選字字級遞增」已於真機確認；本機側之探針、回歸測試與 GSI 視圖 `fittingSize` 量測（字級 16 → 40：高 115.0 → 253.0）與此相符。
- **CI**：本倉之 `build_darwin_SPMTestsAndPackage.yml` 之測試清單**不含** `Packages/vChewing_CandidateWindow`（該包之測試靶自 `aa55c210`，2022-09-27 起即在，卻始終不在該步驟刻意維護的白名單內），故本 phase 之驗證全屬本機。**已於同日解決、且經 CI 實跑驗證**：見文末〈補記（2026-09-19）〉。

## 附註

- **相鄰未修之同源缺陷（已量測、未動；2026-09-19 經事主澄清，已與兩項刻意設計分離）**：範本 cell（💩）的 `textDimension.width` 同屬「初次初始化即定影」之物，故**字級調小**時，由它導出的最小寬度（`minimumCellDimension.width`；單行橫排另含 `cellWidth(_:)` 之 `minAccepted`）不會跟著縮，甚至會反過來蓋過刻意地板。

  **先排除兩項刻意設計（事主 2026-09-19 裁定）**：
  1. **單欄（vertical、單行）於字級 12 時之視窗寬約 76 為刻意設計**（`cellWidth` 之 `min = max(unifiedSize * 6, ceil(cell.size * 5.6))`＝72，加 `originDelta * 2` 之 4 ＝ 76）——太窄則選字窗不顯眼。實測複核：字級 12 ⇒ cellW 72.0／windowW 76.0、16 ⇒ 96.0／100.0、24 ⇒ 144.0／148.0——**隨字級等比、非定影物**。
  2. **範本 cell 用 💩、而其書寫出的字形比全形空白大**，係 macOS 對 emoji glyph 之既有渲染行為（**不進入寬度算式**：範本之 `textDimension.width` 走的是與任何單字 cell 相同的 `ceil(unifiedCharDimension * 1.4)` 公式，非量測 glyph 所得）。

  **隔離量測（唯一變因＝樣板何時定影；現行字級固定）**：

  | 現行字級 | 版面 | 樣板於同級定影 | 樣板於 40 定影 | 樣板於 196 定影 |
  |---|---|---|---|---|
  | 12 | 橫排單行 | 36.0 | **76.0** | **297.0** |
  | 12 | 橫排 4 行 | 45.0 | 45.0 | 45.0（惟視窗寬 94 → 156 → **598**） |
  | 12 | 直排單欄 | 72.0 | 76.0 | **297.0** |
  | 12 | 直排 4 列 | 45.0 | **76.0** | **297.0** |
  | 16 | 橫排單行 | 42.0 | **76.0** | **297.0** |
  | 16 | 直排單欄 | 96.0 | 96.0（被刻意地板遮蔽） | **297.0** |
  | 16 | 直排 4 列 | 56.0 | **76.0** | **297.0** |
  | 24 | 橫排單行 | 53.0 | 76.0 | 297.0 |
  | 24 | 直排 4 列 | 78.0 | 78.0 | 297.0 |

  即：**同一偏好下，僅換「樣板定影時的字級」即可讓 cell 寬與視窗寬數倍之差**（297 ＝ 字級 196 時 💩 樣板之 `ceil(206 * 1.4) + 8`；現實中多為 40 → 12／16 之 76.0）；**字級調大時此地板永不生效**（故日常使用看不出來），唯有「同一行程內把字級調小」方顯。橫排 4 行模式之 cell 寬不受影響，惟其視窗寬仍被該範本經空白 cell 補償（`maxLineCapacity * minDim.width`）撐寬。

  **事主之補充觀察**：vertical layout（橫向展頁）於日常使用看不出毛病——與本量測相符（該路徑之字級變大、變小皆走實測幾何與隨字級浮動之刻意地板，僅「樣板定影於更大字級、且該地板值超過其刻意地板」之組合方顯）。**處置待裁**：若要修，手法與本 phase 同款（把該地板的來源改為即時讀當前字級），`unifiedSize * 6` 等刻意地板不受影響；若裁定「接受現狀」（重開輸入法行程即復原），則本項自此結案。
- **相鄰未修之另一事項（僅盤點；已由 Phase 232 修復）**：GSI **捲動模式**不繪製頂部 pane（unfinished reading）——`VwrCandidateGSI4AppKit.drawScrollMode` 無該 pane 之繪製，且捲動模式只走 `computeCandidateOnlySize()`（不經 `updateMetrics()` 之 `topPaneShift`），故候選字溢成捲動模式時，該 pane 靜默消失（非本 phase 所致，亦非本 phase 所改）。
- **工具鏈教訓（Swift Testing）**：`#expect` 對**混型別**運算元之 `==` 會誤報失敗。實測：`let cgValue: CGFloat = 52`、`let doubleValue: Double = 52` ⇒ 直接比較 `cgValue == doubleValue` 為 `true`，但 `#expect(cgValue == doubleValue, …)` **報失敗**；寫成 `#expect(cgValue == CGFloat(doubleValue), …)` 即通過。故 `#expect` 內之浮點比較務必對齊型別（本 phase 之回歸測項即因此把 `expectedLineHeight` 顯式轉為 `CGFloat`）。
- **未觸及之面**：`pageCandidateSize` 之算式、TDK 分支之 `metrics` 路徑、縱向排版之捲動量測、底部欄位與頂部 pane 之量測（後二者本即隨字級即時浮動）；未新增偏好、未動 i18n、未動 `vChewing-OSX-legacy`（已封存）。
- **文書**：`vChewing-DevLogs/DevReqsHistory.md` 追加本 phase 一列；`vChewing-DevLogs/KnowledgeMemo4LLM.md` 更新「最後更新」標記並於 §A2.2 增列範本 cell 定影之施工注意事項。Swift 原始碼正文內未出現任何 Phase 編號。

## 補記（2026-09-19）：本包之單元測試納入 macOS CI

事主指示：「你把 CandidateWindow 的單元測試塞入 CI」。此即關閉上節〈未驗〉之 CI 缺口。

### 落點（單檔一行）

`.github/workflows/build_darwin_SPMTestsAndPackage.yml` 之「Run Package Tests (Selected)」步驟的 `packages=(…)` 清單，於 `'./Packages/vChewing_OSNeutral_LibVanguard'` 之後插入一行：

```bash
            './Packages/vChewing_CandidateWindow'
```

**已 commit（事主 finalize，2026-09-19）：`vChewing-macOS` `c26bb31c`（`CI // Also test TDKCandidates.`，1 檔 ＋1 行）；該筆已隨 `9fb6b94c` 推至 GitHub `main`（建 run `35375154246`）。**

**為何先前不在清單內**：該清單是**刻意維護的白名單**（`KnowledgeMemo4LLM.md` 首段載明 `./Packages/vChewing_BPMFVS` 為刻意不收者），非對 `Packages/*` 之全域掃描；本包之測試靶自 `aa55c210`（2022-09-27，〈Repo // Introducing CtlCandidateTDK (horizontal).〉，當時為 `Tests/CandidateWindowTests`）起即在，現行路徑為 `Tests/TDK4AppKitTests`（靶名 `TDK4AppKitTests`）；只是始終未被收進該清單。

### 驗證（2026-09-19，本機實測）

- **YAML 解析**：`YAML.load_file` 讀入該 workflow，實查該步驟之 `run` 內容已含新項。
- **逐字模擬 CI 之迴圈體**：以「清單僅置本包」＋與 CI 同構之目錄守衛（`[ -d "${pkg}/Tests" ]`、`find "${pkg}/Tests" -type f -name "*.*" | wc -l` > 0）實跑 ⇒ `swift test --no-parallel --package-path ./Packages/vChewing_CandidateWindow` **21 支／1 套全過**。腳本存 `vChewing-macOS/tmp/p231-cisim.sh`（`tmp/` 受 `.gitignore` 忽略）。**本機須追加 `--disable-sandbox`**（SwiftPM 之內層沙盒與本工作區之外層沙盒不相容）；CI runner 無此沙盒、**不得**把該旗標寫進 workflow。
- **冷啟動成本**：以全新 `--scratch-path`（連同其全部本地路徑相依）建置＋測試 ⇒ **52.4 s** wall（137.6 s user／290% CPU）。
- **時預算核對**：同一 CI 該步驟最近一次實跑（run `35365849276`，2026-09-18）起訖 **16:05:35 → 16:12:20＝6 分 45 秒**（`gh api repos/:owner/:repo/actions/runs/<id>/jobs --jq '.jobs[].steps[]'` 實查），對 `timeout-minutes: 15` 尚餘約 **8 分 15 秒**；本包之冷啟動增量（runner 較本機慢，通常 2～3 倍 ⇒ 約 2～3 分）應可容納。故 **`timeout-minutes` 未動**。
- **headless 安全性（為何本包可在無 GUI 之 runner 上跑）**：本包測試不觸及 `NSApplication.shared`——`NSApplication.isDarkMode`（`Packages/vChewing_OSFrameworkImpl/Sources/OSFrameworkImpl/AppKitImpl/AppKitImpl_Misc.swift:402`）係讀 `UserDefaults.current` 之 `AppleInterfaceStyle`，該實作原地即註明「`NSApp` can be nil during SPM unit tests」；測試亦不建立 `NSView`／`NSWindow`（本 phase 之探針曾建 `VwrCandidateGSI4AppKit`，惟探針未入庫）。

### 未驗（已閉合）

- **CI 實跑**：原為本補記之唯一未驗項，已於 **2026-09-19** 閉合——**三條 workflow 皆全綠**：`debug-macOS-SPMTestsAndPackage`（run `35375154246`）、`unit-test-LibVanguard-linux`（run `35375154153`）、`unit-test-LibVanguard-WinNT`（run `35375154338`），同 SHA `9fb6b94c`、即 GitHub `main` 之現行 HEAD。其中 macOS 該步驟之日誌實查含 `Checking tests for ./Packages/vChewing_CandidateWindow`、`Running tests for ./Packages/vChewing_CandidateWindow`、`✔ Suite TDK4AppKitTests passed after 0.399 seconds.`、`✔ Test run with 21 tests in 1 suite passed after 0.399 seconds.`（即含本 phase 新增之 `testGSILineStepFollowsCandidateFontSize`）。**該步驟耗時自 6 分 45 秒（run `35365849276`）增至 8 分 05 秒**（17:41:55 → 17:50:00Z），即本包之增量約 **80 秒**（含建置；較本機冷啟動之 52.4 s 高約五成，仍遠低於 `timeout-minutes: 15` 之餘裕——推估無需調整）。

### 未動

- **清單未改為 glob 掃描**：該清單是刻意維護之白名單，改為掃描 `Packages/*` 會把刻意不收者（如 `vChewing_BPMFVS`）與無 CI 靶者一併納入——屬語意變更，非本案所求。
- **`timeout-minutes` 未動**（餘裕估算見上）；其餘 7 個套件之項次、步驟內容與其他 workflow 皆未動。

--------------------

# Phase 232

Assignee: Deepseek-v4-flash（Codewhale harness）。

> **狀態：完工（2026-09-19，本機實測 ＋ 離屏渲染覆驗；第二項之設計讀法已由事主定調）。** 兩項：（一）**GSI 捲動模式補繪頂部 pane（未完成讀音）**——該 pane 先前於捲動模式下從未被繪製、其空間亦未從捲動幾何中讓出，故候選字一溢出成捲動模式即靜默消失；（二）**選字鍵標籤之顯示區域改為正方形**——該標籤之呈現盒本為「窄高」形（寬＝字寬、高＝行高），顯示區域卻只依「0.6 × 字級」給寬，字級越大越顯窄。落點：`vChewing-macOS` 之 `CandidateWindow` 模組 3 檔（＋74／−22）＋回歸測試 3 支（＋154）。**已 commit（事主 finalize）：`vChewing-macOS` `ade56ecc`（`TDKCandidates // Patch unreasonable behaviors.`，4 檔 ＋228／−22）。**事後事主複驗另回報「標籤於其區域內偏 center-trailing」，該追加修正見文末〈補記（2026-09-19）〉。

## 前導 Phase

- 第一項係兌現 Phase 231〈附註〉所列「相鄰未修」之第二條（GSI 捲動模式不繪製頂部 pane）。
- 第二項係本 phase 新提。
- 相關沿革：未完成讀音之頂部 pane 係 `8d86632a`（〈TDKCandidates // Add a dedicated pane for unifnished reading.〉）引入，當時只做了 TDK（非捲動）路徑與 GSI 之普通模式；GSI 之捲動模式（`35668fde` 引入之 `drawScrollMode`）自始未納入該 pane。選字鍵之橫向留白 `ceil(size * 0.6)` 自 `d7ca7a45`（2023-07-16，〈CtlCandidateTDK // Rewrite.〉）即有。

## 手術範圍

- `vChewing-macOS`（單倉；`vChewing-OSX-legacy` 已封存且無此模組）：
  - `Packages/vChewing_CandidateWindow/Sources/TDK4AppKit/VwrCandidateGSI4AppKit.swift`（＋54／−19）
  - `Packages/vChewing_CandidateWindow/Sources/TDK4AppKit/CandidateCellData4AppKit.swift`（＋18／−1）
  - `Packages/vChewing_CandidateWindow/Sources/TDK4AppKit/VwrCandidateTDK4AppKit.swift`（＋2／−2）
  - `Packages/vChewing_CandidateWindow/Tests/TDK4AppKitTests/TDK4AppKitTests.swift`（＋154）

## 要解決的問題

事主原文：

> 1. GSI 捲動模式不繪製頂部 pane（unfinished reading）：溢成捲動模式時該 pane 靜默消失。
> 2. 候選字的 candidate key label 的顯示區域得設法保持成一個正方形的面積。不然的話，Candidate Font Size 越大，這 Candidate Key Label 越是顯得窄得誇張。

## 病灶

### 一、GSI 捲動模式之頂部 pane

非捲動模式（TDK 與 GSI 之普通模式）之 pane 走 `CandidatePool4AppKit.updateMetrics()`：該函式量出 pane 尺寸 `dimensionUnfinishedReading`、算出 `topPaneShift = 該高 + padding`、把**全體 cell 之 visualOrigin.y 下移**並把 `fittingSize.height` 加高，`metrics.unfinishedReading` 則記下 pane 之矩形。**三件事在捲動模式全都不成立**：

1. `VwrCandidateGSI4AppKit.drawScrollMode(...)` 從未繪製 `attributedDescriptionUnfinishedReading`（該函式只畫候選格、scroller 與底部欄位）。
2. 捲動模式之視窗高度走 `fittingSize` 之另一分支（`pad + page.height + extraHeight + bottom.height`），未含 pane。
3. 捲動模式之候選區幾何——`clipOrigin`、`drawOffsetX/Y`、`scrollerTrackRect`、`drawBottomFields(topY:)`、`findCell` 之命中點與視埠矩形——全以 `originDelta` 為基準，未計 pane。

且捲動模式之 cell 幾何來自 `computeCandidateOnlySize()`（自 `(0,0)` 起排、不經 `updateMetrics` 之 `topPaneShift`），故**連「cell 被下移」這個副作用都不會發生**：pane 就這樣無聲無息地消失。

### 二、選字鍵標籤之顯示區域

標籤以 `selectionKeyFont()`（monospaced 系統字、字級 `fontSizeKey = max(ceil(0.6 × 候選字字級), 11)`）繪製，其**呈現盒**＝字寬 × 行高（數字之 advance 約 0.6 em、行高約 1.17 em）——即盒之兩邊比約 **1 : 1.95**（窄高）。而顯示區域之橫向留白原為 `phraseDrawXOffset = ceil(size * 0.6)`（＝標籤字級、亦即其字寬），故**顯示區域恰好只夠容納「字寬」，比標籤自身之行高窄上一倍**：字級越大，這個「窄得誇張」的比例越顯眼。

實測（本機，非高亮格）：字級 12 ⇒ 盒 8.0 寬 × 13.0 高；24 ⇒ 15.0 × 17.7；40 ⇒ 24.0 × 28.3；96 ⇒ 58.0 × 68.3；196 ⇒ 118 × 139。

**耦帶發現（本 phase 一併收緊）**：既有之 cell 寬度算式（`textDimension.width + 4 × padding`）在**極大字級**下本就容不下「標籤 ＋ 候選字詞」——以字級 196 為例，候選字詞之起點（`2 × padding + 118`）＋字詞寬（測得 184.9）＝ 306.9 > 該格寬 297，即術前即已超格約 10 點（該算式之「餘裕」約 14 點，係按 1 字型紋理而非實測 advance 估的）。

## 落點

### 一、GSI 捲動模式之頂部 pane（`VwrCandidateGSI4AppKit.swift`）

新增兩個成員，把 pane 之幾何收攏成單一真源：

```swift
  private var topPaneDimension: CGSize {   // 空字串 ⇒ .zero（pane 隱藏）
    let strPane = thePool.attributedDescriptionUnfinishedReading
    guard rendersInScrollMode, !strPane.string.isEmpty else { return .zero }
    let rawDim = strPane.getBoundingDimension(forceFallback: true)
    return CGSize(width: ceil(rawDim.width), height: ceil(rawDim.height))
  }

  private var topPaneShift: CGFloat {      // pane 高 + padding；0 ⇒ 無 pane
    let dimension = topPaneDimension
    guard dimension.height > 0 else { return 0 }
    return dimension.height + thePool.padding
  }
```

六處幾何一併以 `topPaneShift` 下移：`fittingSize`（高＋shift；寬納入 pane 寬）、`drawScrollMode` 之 `clipOrigin` 與 `drawOffsetY`、`scrollerTrackRect` 之 y、`drawBottomFields(topY:)`、`findCell` 之命中點（`clickPoint.y -= topPaneShift`）與其視埠矩形（連同 cell 之 view 座標推算）。**軌道高度須與 pane 無關**（＝候選區高 − 上緣圓角留白）：軌道之頂端隨 pane 下移、底端仍貼齊候選區下緣——首版誤將高度亦減去 shift，令軌道短掉一個 paneShift（直立軌道；見〈驗證〉之複核記錄）。pane 本身繪於 `(originDelta, originDelta)`（與 `updateMetrics` 之 `rectUnfinishedReading` 同座標）——**不隨捲動移動**。另：clip path 之快取鍵補上 `lastClipOrigin`（pane 之出現／消失會平移裁切矩形，只比對尺寸會取到過期路徑）。

### 二、選字鍵標籤之正方形顯示區域（`CandidateCellData4AppKit.swift` ＋ 兩視圖）

```swift
    /// 選字鍵標籤之顯示區域：正方形，邊長取其自身之行高。
    var keyLabelBoxSide: CGFloat {
      let keyFont = selectionKeyFont()
      return ceil(keyFont.ascender + abs(keyFont.descender) + keyFont.leading)
    }
```

`updateMetrics` 內：`phraseDrawXOffset = keyLabelBoxSide`（候選字詞自正方形右緣起排）、`headerDrawXOffset = ceil((keyLabelBoxSide - 標籤字寬) / 2)`（標籤水平居中於其盒內）；並新增「標籤盒 ＋ 候選字詞」之硬約束：

```swift
      let keyAndPhraseWidth = keyLabelBoxSide
        + makeAttributedStringPhrase(isMatrix: false).size().width
      cellDimension.width = max(cellDimension.width, keyAndPhraseWidth + 2 * padding)
```

四處繪製點（TDK 1 處、GSI 3 處：捲動模式兩分支與普通模式）改用 `visualOrigin.x + 2 * padding + cell.headerDrawXOffset`；TDK 之候選字詞偏移由行內算式 `ceil(size * 0.6)` 改讀 `cell.phraseDrawXOffset`（與 GSI 一致）。

## Phase 232 實作結果

### 程式碼變更

- **`VwrCandidateGSI4AppKit.swift`**（＋54／−19）：`topPaneDimension`／`topPaneShift`（對內開放以便測試斷言）；`fittingSize`、`drawScrollMode`、`scrollerTrackRect`、`findCell` 六處幾何；`lastClipOrigin` 快取鍵。
- **`CandidateCellData4AppKit.swift`**（＋18／−1）：`keyLabelBoxSide`、`headerDrawXOffset`；`phraseDrawXOffset` 改義；cell 寬度之硬約束。
- **`VwrCandidateTDK4AppKit.swift`**（＋2／−2）：標籤與候選字詞改用上述兩個偏移。

### 新增回歸測試（`TDK4AppKitTests.swift`，＋154）

- `testCandidateKeyLabelDisplayAreaIsSquare`：於字級 12／16／24／40／96／196 逐級斷言——盒邊長＝標籤自身行高；候選字詞自盒右緣起排；標籤水平居中（以其實測字寬計）；「標籤盒 ＋ 候選字詞」不超出該格。
- `testKeyLabelFitsInsideHorizontalMatrixCells`：橫排多行（matrix）模式之 cell 寬度由 `cellWidthMultiplied` 覆寫、不經上述硬約束，故逐字級另鎖「標籤盒 ＋ 候選字詞仍放得進該格」之不變式。
- `testGSIScrollModeShowsTopPane`：**幾何**斷言——`topPaneShift` 恰為 `ceil(pane 高) + padding`（無 pane 時為 0）；有 pane 與無 pane 之視窗高差即該值；寬度足以容納 pane；捲動軌道底端貼齊候選區下緣（`maxY ＝ originDelta + shift + 頁高`）。**繪製**實查則以離屏點陣圖掃描斷言首行候選字因 pane 而下移（不可繪製之環境下該段自動略過，幾何斷言仍為有效鎖）。

## 驗證（2026-09-19，本機實測）

### 紅綠雙驗

- **紅（撤下原始碼改動、測試不動）**：`testCandidateKeyLabelDisplayAreaIsSquare` **13 筆失敗**（六個字級之「盒邊長 vs 偏移」「標籤居中」全數命中）、`testGSIScrollModeShowsTopPane` **2 筆失敗**（視窗高差、首行下移）；恢復後全綠。另：捲動軌道高度之修正亦經紅綠鎖定——以首版算式（高度亦減 shift）**恰紅 1 筆**（`track.maxY` 實測 114.0 對 137.0，即短掉一個 paneShift 之 23），修正後全綠。
- **綠**：`swift test --no-parallel --disable-sandbox` ⇒ **24 支／1 套全過**（術前 21 支）。

### 幾何實測

- **標籤盒（新制）**：字級 12／16／24／40／96／196 ⇒ 邊長 **13／13／18／29／69／139**，逐級皆等於該標籤自身之行高；候選字詞起點＝盒右緣（4 + 邊長）；標籤於盒內居中（如字級 196：偏移 34 ＝ (139 − 72.9)/2）。
- **cell 寬度**：字級 24／40／96 之單字格寬 **53／81／166 不變**（新約束未觸發）；字級 196 者 **297 → 356**（新約束觸發：128 ≥ 標籤盒 139 ＋ 字詞 184.9 ＋ padding 4；順帶修掉上揭「術前即超格約 10 點」）。
- **GSI 捲動模式之視窗**（40 個候選、4 行／頁、字級 20）：無 pane ⇒ `(416.0, 137.0)`；有 pane「ban」（pane 高 21）⇒ `(416.0, **160.0**)`，即高差 23 ＝ 21 ＋ padding 2 ✓。

### 離屏渲染覆驗（人眼可查之證據）

以 `cacheDisplay(in:to:)` 離屏繪製並存 PNG（`vChewing-macOS/tmp/`，受 `.gitignore` 忽略）：

- 前後對照（標籤盒）：`p232-key-before-{12,16,24,40,96}.png` ↔ `p232-key-after-{12,16,24,40,96,196}.png`。
- 捲動模式之頂部 pane：`p232-pane-none-137.png`（無 pane）↔ `p232-pane-with-160.png`（有 pane）。
- 點陣圖掃描（原始緩衝、避開 `colorAt` 之逐像素配置）用於回歸測試內之「首行有墨跡之列」差分斷言。

### 全靶測試與建置

- 套件測試：**24 支／1 套全過**；套件與倉根 `swift build` 皆 `Build complete!`。
- Swift 5.10 側：`make build510` ⇒ `Build complete!`。
- `make lintFormatUncommitted`（倉根）`rc=0`；受改四檔 md5：`CandidateCellData4AppKit.swift` `9457d96fc6575c56a6fa1cd7890ec112`、`VwrCandidateGSI4AppKit.swift` `b1fd266d873aaad0de44bdf6b14f2603`、`VwrCandidateTDK4AppKit.swift` `d4e358401b2c4724b69b5603983cd2aa`、`TDK4AppKitTests.swift` `19ba779274b03e83122fca9fa1da5eae`。

## 未驗

- **真機 IME 目視**：未以實裝輸入法在真機上目視兩項變更（尤其大級字下之標籤盒比例、以及捲動模式下 pane 與 scroller／底部欄位之相對位置）。本 phase 之證據止於套件層之幾何斷言、離屏渲染 PNG 與點陣圖掃描。
- **CI**：本包之測試現已納入 macOS CI（Phase 231 補記），惟本次尚未推送，故未經 CI 實跑。

## 附註

- **第二項之設計讀法（已定調，2026-09-19）**：事主裁定採「**顯示區域變寬至標籤自身行高**」（即本實作）——標籤字級不動、候選字詞右移約 0.1 × 字級；另一種讀法（縮小標籤字級以求其盒在既有欄寬內成正方形）**不予採用**，日後不得改按該讀法之取值來源。
- **標籤盒之垂直擺位未動**：`headerDrawYOffset = ceil(cell 高 × 0.2)` 原樣保留；本 phase 只令其**橫向**區域成正方形（寬＝行高），未動其縱向起點。
- **微小字級下之既有地板**：標籤字級本有 `fontSizeKey = max(ceil(0.6 × 字級), 11)` 之下限，故字級 ≤ 18 時盒邊長定影於 ≈ 13（12 級與 16 級皆 13）——正方形仍成立，只是不再隨字級等比縮放。此係既有設計（避免選字鍵過小），未動。
- **獨立複核**：本 phase 完工前另遣一員（唯讀靜態審查）複核變更集。其無法執行 `swift test`／`swift build`（該角色之執行面被封），但在靜態審查中**發現並回報了一個真缺陷**——即上揭之捲動軌道高度（首版誤減 shift）；該項已修正並補上紅綠鎖定。其餘回報（橫排多行路徑不經硬約束、繪製斷言於不可繪製時自動略過）分別以「新增 matrix 專測」與「以幾何斷言為主鎖、繪製實查為輔」處置。
- **`ShitCell`／`BlankCell` 之定影陷阱未涉**：本 phase 未動 Phase 231〈附註〉所述之範本 cell 寬度定影項（該項之處置仍待裁定）。
- **文書**：`vChewing-DevLogs/DevReqsHistory.md` 追加本 phase 一列；`vChewing-DevLogs/KnowledgeMemo4LLM.md` 之 A2.2 新增「GSI 捲動模式之幾何須為頂部 pane 讓位」一條施工注意事項。Swift 原始碼正文內未出現任何 Phase 編號。

### 補記（2026-09-19）：標籤之墨跡光學居中（事主複驗回報之追加修正）

事主回報原文：

> 空間倒是騰出來了，但這個 candidate key text glyph 的位置看上去卻是在這個空間的 center-trailing 位置對齊的。換言之，center-leading 過大、center-trailing 過小。我在懷疑是不是需要從 NSAttributedString 組裝的角度干涉這個：candidate key text 左側的 space 的 fontsize 變小一些、candidate key text 右側的 space 的 fontsize 變大一些。

**量測（先確認現象，再動手）**：以離屏渲染掃描非高亮格之標籤墨跡，量其於正方形區域內之左右留白：

| 字級 | 區域邊長 | 左留白 | 右留白 | 差（正＝偏右） |
|---|---|---|---|---|
| 24 | 18.0 | 5.5 | 4.5 | **+1.0** |
| 40 | 29.0 | 9.5 | 7.5 | **+2.0** |
| 96 | 69.0 | 20.5 | 20.0 | +0.5 |

即現象屬實、方向與事主所述一致（偏 center-trailing）。

**根因（與事主之猜測不同）**：並非 bearing 不對稱。以 CoreText 實測該標籤字型：墨跡盒（`CTLineGetBoundsWithOptions(line, .useGlyphPathBounds)`：`minX` ＝ 左 side bearing、寬 ＝ 墨跡寬）之中點與 advance 中點**逐字級相差 ≤ 0.01 點**（如字級 40：advance 14.84／墨跡 `minX 1.61, w 11.61` ⇒ 中點 7.41 對 7.42）——墨跡本就居中於 advance。真正的病灶是**取整方向**：舊制 `headerDrawXOffset = ceil((區域邊長 − advance) / 2)` 恆**向上**取整，令偏移恆偏正向、誤差 0.4～0.9 點（於十餘點寬的區域上即 3～7%）——故字級 40 之留白落差達 2.0 點。

**落點**：`CandidateCellData4AppKit` 新增 `keyLabelInkBounds`（＝ `CTLineGetBoundsWithOptions(line, .useGlyphPathBounds)`；**注意**：`[.useGlyphPathBounds, .useOpticalBounds]` 會回傳 advance 盒而非墨跡盒，兩者不可混用——此坑已於探針中踩過並記下）與 `keyLabelHorizontalCenteringOffset`（＝ `區域邊長 / 2 − 墨跡盒中點`，**不取整**）；`updateMetrics` 改採後者。文字以分數座標繪製本屬常態，換得兩側留白精確相等。

**何以不由字串層干涉（對事主原提議之回應）**：事主提議「左側 space 字級小些、右側 space 字級大些」——方向正確、效果等價，但屬字串層：需為該二空格各留寬、且其位移量取決於空格之字型度量（換字型即失準）。本實作改在**版面層**以墨跡盒直接算出偏移：不動字串、不佔版面寬、且對任何字型皆成立。

**複驗（同法重測）**：

| 字級 | 修後左留白 | 修後右留白 | 差 |
|---|---|---|---|
| 24 | 5.0 | 5.0 | 0.0 |
| 40 | 8.5 | 8.5 | 0.0 |
| 96 | 20.0 | 20.5 | −0.5（像素網格量化之殘差） |

**回歸測試**：新增 `testCandidateKeyLabelInkIsCenteredInItsBox`（三字級：位移須恰為 `區域邊長 / 2 − 墨跡中點`〔不得取整〕；並離屏渲染實掃兩側留白差 ≤ 1.0 點）。**紅綠雙驗**：舊制（`ceil`）下該測 **5 筆失敗**（位移非恰中 ×3、留白 6.0／4.5 與 9.5／7.5），修後全綠。套件測試 **25 支／1 套全過**。

**可目視之證據**：`vChewing-macOS/tmp/p232b-key-{before,after}-{24,40,96}.png`（前後對照；`tmp/` 受 `.gitignore` 忽略）。

**未動（另備一議，待事主裁定）**：本補記只動**橫向**居中。同一量測另顯示**縱向**留白亦有偏（上／下留白差：字級 24 ⇒ +1.5、40 ⇒ 0.0、96 ⇒ +2.0 點，即標籤於其行盒內偏低）——該偏係字型 ascent/descent 之固有度量，且現行縱向擺位（`headerDrawYOffset = ceil(cell 高 × 0.2)`）係既有設計；若要一併光學居中，可同法以墨跡盒之縱向中點校正（一處常數，未動）。

**未 commit（待事主 finalize）**；本補記之動工面：`CandidateCellData4AppKit.swift`（＋22／−1，md5 `1cfd039f6e9f08bceb638df56b5243c1`）＋ `TDK4AppKitTests.swift`（＋62／−5，md5 `29b45894835922fad5c4dbf262675ee8`）。建置與格式管線：套件與倉根 `swift build`、5.10 側 `make build510` 皆 `Build complete!`、`make lintFormatUncommitted` rc=0。

### 補記（2026-09-19 之二）：CI 紅燈之修復——離屏點陣圖斷言改為與通道序無關

事主回報「遠端 CI 炸了」（run `35383442184`／job `105724789141`，SHA `ade56ecc`），並要求在本機複跑 `CandidateWindow` 之單元測試。

**CI 之失敗**：`testGSIScrollModeShowsTopPane` 恰 1 筆失敗——`firstRowWithPane > firstRowWithoutPane` 為 false，且兩側實測值**皆為 0**（CI log 逐行實查）；本機同一測項全綠 ⇒ 屬**環境相依**。

**根因（測試自身之取樣法，非繪製邏輯）**：該測項（與 `testCandidateKeyLabelInkIsCenteredInItsBox`）原本自行讀 `NSBitmapImageRep.bitmapData` 之**原始位元組**、假設通道序為 R,G,B，且以「圖中某一像素」為底色基準。CI runner 上該假設不成立——底色取樣點非代表值，遂令**每一列都被判為「有墨跡」**（故兩側皆得 0）。本地之所以綠燈，純係本機之點陣圖格式恰合該假設。

**修法（`TDK4AppKitTests.swift`）**：新增檔內工具 `OffscreenInkScan`——① 一律以 `colorAt(x:y:)` 取值（**與通道序無關**）、並除以 alpha 還原非預乘值；② 底色改取「整張圖取樣後最常見之亮度」（眾數），不再賭單一像素；③ 深淺色外觀皆適用。兩處斷言隨之改寫：

- `testGSIScrollModeShowsTopPane`：改為**帶域式**判讀——頂部 pane 所在之列帶內**有**墨跡（證明 pane 確實被繪出），且該帶之墨跡行數**遠少於**未提供 pane 時者（後者之頂帶裝的是整行候選字，橫向墨跡近乎滿版面）。
- `testCandidateKeyLabelInkIsCenteredInItsBox`：墨跡範圍改由上開工具取得。

**驗證（本機）**：

- **重跑命令**（事主所求）：於 `Packages/vChewing_CandidateWindow` 執行 `swift test --no-parallel --disable-sandbox` ⇒ **25 支／1 套全過**。
- **外觀雙態複驗**（模擬 runner 常處之淺色外觀）：以行程內強制 `AppleInterfaceStyle` ＝ `Light`／`Dark` 各跑一次 ⇒ pane 帶之墨跡行數：淺色 **31**（有 pane）對 **400**（無 pane）、深色 **32** 對 **167**——兩態皆滿足「> 0 且遠小於無 pane 者」；標籤留白兩態皆 8.5／8.5（差 0.0）。探針跑畢即刪。
- 套件與倉根 `swift build`、5.10 側 `make build510` 皆 `Build complete!`；`make lintFormatUncommitted` rc=0。

**未驗**：~~CI 實跑~~ — **已閉合（2026-09-19，事主推送後實查）**：`debug-macOS-SPMTestsAndPackage` run `35422339046`（SHA `84b2e578`）**全綠**，該步驟日誌實查 `◇ Suite TDK4AppKitTests started.` → `✔ Suite TDK4AppKitTests passed after 1.726 seconds.` → `✔ Test run with 26 tests in 1 suite passed`（步驟耗時 8 分 59 秒）；同 SHA 之 linux（`35422339054`）與 WinNT（`35422339047`）亦皆綠。本機無法複現 runner 之點陣圖格式差異，故本次係**由構造上排除該依賴**、而非在其上打補丁。本補記之動工面：`TDK4AppKitTests.swift`（＋106／−55，md5 `035f498eeaa3a0d34edbd38e98f8232f`），已由事主 finalize。

### 補記（2026-09-19 之三）：選字窗之外觀可覆寫（jailed；寄存於 pool，隨系統自動更新）

事主定調（三則）：① 「允許選字窗被指定使用 light mode 還是 dark mode 還是 auto。但這次就將這個特性 jail 在 candidate window 及 view 內。至於『是否對外開放給使用者決定』，現在先不用考慮。」② 「default: auto，但在 macOS 10.13 為止的系統下解讀時一律解讀成 light。」③ 「candidateAppearance 屬性可以寄存在 pool 內，且隨著系統的 appearance 自動更新。」

**落點（源碼 7 檔 ＋149／−11；測試 1 檔 ＋243／−64）**：

- `_TDK4AppKit.swift`（＋55／−1）：新增 `TDK4AppKit.CandidateAppearance: Int, Sendable`——`auto = 0`／`light = 1`／`dark = -1`；`isDarkModeResolved`（`.auto` 以 `#available(macOS 10.14, *)` 守衛——**10.13 及以前一律解析為淺色**，其後跟隨 `NSApplication.isDarkMode`）；`nsAppearance`（`.auto` 為 nil＝跟隨系統；`.dark` 之 `.darkAqua` 亦以 10.14 守衛，之前之系統回 nil）。**jailed**：`current` 僅模組內可見、不對外開放；是否給使用者決定留待日後裁定。
- `CandidatePool4AppKit.swift`（＋51／−1）：新增 **`candidateAppearance`（寄存在池內，預設 `.auto`）**——`didSet` 同步模組層之現行值（供 cell 之靜態色解析）並清「依外觀而異」之屬性字串快取；新增 `isDarkModeResolved`；`init` 時同步並起**系統外觀觀察者**（`DistributedNotificationCenter` 之 `AppleInterfaceThemeChangedNotification` → 清快取，令下一次重繪即反映新外觀）；`deinit` 移除該觀察者（令牌以 `nonisolated(unsafe)` 標記，以配合 nonisolated `deinit`）。
- `CtlCandidateTDK4AppKit.swift`／`CtlCandidateGSI4AppKit.swift`（各 ＋13／−0）：`appearanceOverride`（public）改為**代理池內之 `candidateAppearance`**——設定時一併覆寫 `window?.appearance`（其下之視覺效果視圖隨 effective appearance 自動跟隨）並 `updateDisplay()`。
- `VwrCandidateTDK4AppKit.swift`（＋2／−2）／`VwrCandidateGSI4AppKit.swift`（＋12／−6）／`CandidateCellData4AppKit.swift`（＋3／−1）：五處「問 `NSApplication.isDarkMode`」一律改問 `CandidateAppearance.isDarkModeResolved`（＝池之現行值）；GSI 之高亮行底色快取改以**明暗狀態為鍵**（系統外觀變更後不得沿用舊色）。

**驗證（2026-09-19 本機實測）**：

- 新增 `testCandidateAppearanceOverride`：rawValue 語義（0／1／−1）、`auto` 隨系統、`light`／`dark` 各自解析、`nsAppearance` 之 nil／aqua／darkAqua、**池內設定會同步至模組層**、且 `absoluteTextColor` 與兩視圖之底色確實隨覆寫切換。
- 兩個繪製型測項（標籤墨跡居中、捲動模式頂部 pane）改為**強制 light／dark 各跑一次**（經池設定），使斷言不再隨 runner 之系統外觀而異。
- 套件測試 **26 支／1 套全過**；倉根 `swift build` 與 5.10 側 `make build510` 皆 `Build complete!`；`make lintFormatUncommitted` rc=0（連跑 md5 不變）。
- **順帶修掉一個測試自身之缺陷（本次除錯實錄）**：`OffscreenInkScan.pixelDifference` 原把「兩邊皆透明」之像素（視窗圓角外之裁切區）計為差異，令「差異必集中於 pane 橫向範圍」之斷言誤紅（實測 maxX 竟達全寬）；改為**僅「單邊透明」方算差異**後即恢復正常。

**未驗**：~~真機目視；CI~~ — **皆已閉合（2026-09-19）**：事主實機確認「測試成功」；CI 亦全綠（`debug-macOS-SPMTestsAndPackage` run `35422339046`／SHA `84b2e578`，該步驟日誌實查 `✔ Test run with 26 tests in 1 suite passed`；linux `35422339054`、WinNT `35422339047` 同綠）。**事主裁定（同日）**：「所有選字窗的 appearance 都只可能雷同」——故模組層 `CandidateAppearance.current` 之共用解析通道係設計如此（非缺陷），日後無需為「多窗各自不同外觀」而改造。已 commit：`vChewing-macOS` **`84b2e578`**（`TDKCandidates // Allow overriding dark / light scheme.`）。

**未動**：未新增偏好、未動設定介面（**jailed**）、未動 `vChewing-OSX-legacy`（已封存）。
**未 commit（待事主 finalize）**。
