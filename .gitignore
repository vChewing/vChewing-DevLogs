# ── vChewing-DevLogs ─────────────────────────────────────────────────────────
# 本倉庫為純文書倉庫（內容僅 markdown），無任何建置產物，故不套用 sibling 倉庫
# 那份 Visual Studio 制式 .gitignore；此處只列真正會在本倉庫出現的東西。

# ── 暫存目錄 ─────────────────────────────────────────────────────────────────
# 臨時檔案一律放在倉庫根目錄的 tmp/ 或 .tmp/（勿用系統的 /tmp/）。
# 不錨定根目錄，讓任何層級的暫存目錄都一併涵蓋。
tmp/
.tmp/
*.tmp

# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
._*
.AppleDouble
.LSOverride

# ── 編輯器備份 ───────────────────────────────────────────────────────────────
*~
