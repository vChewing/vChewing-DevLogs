# Phase 221

Assignee: Deepseek-v4-flash（Deepseek harness）。

> **狀態：完結（2026-09-16）。** 中英混打（mixedAlnum）模式下，令 TooltipUI 的**第二行**顯示「游標最前方正在組裝的注音讀音」預覽。動刀次序照事主建議：先 `vChewing-LibVanguard`（打字閉包正本），再同步 `vChewing-macOS` 與 `vChewing-OSX-legacy`。現行 commit：`vChewing-LibVanguard` `7135400`、`vChewing-macOS` `b971af6b`、`vChewing-OSX-legacy` `290faa6b`（三倉同訊息 `InputHandler // MixedAlnum: Show reading preview in tooltip.`）。已推送 `origin/main`；**CI（linux／darwin／WinNT）三平台全綠**（見〈驗證〉）。

## 前導 Phase

無。落點是 Phase 53（Typewriter 層中英文混輸）與 Phase 59（Shift+Space early-exit）所立之混輸 Tooltip 區塊；讀音之呈現規則則沿用標記狀態 Tooltip 的既有那一套。

## 手術範圍

- `vChewing-LibVanguard`（先行，5 檔）：`Sources/LibVanguard/InputHandler/InputHandler_HandleStates.swift`、`…/InputHandler_CoreProtocol.swift`、`Sources/LibVanguard/Session/IMEStateParsed.swift`、`Tests/LibVanguardTests/InputHandlerTests_Cases4.swift`、`Tests/LibVanguardTests/SessionTests_Cases2.swift`
- `vChewing-macOS`：前列五檔之對位。
- `vChewing-OSX-legacy`：前三檔之對位（該倉不承擔單元測試）。

## 要解決的問題

事主原文：「mixedAlnum 模式下需要用 tooltipUI 的第二行顯示游標最前方組出來的注音讀音 preview。」

混輸模式原本恆久顯示一枚 Tooltip，內容**僅**為使用者敲下的 ASCII 原文——因為組字區顯示的是注拼槽轉寫出來的讀音，ASCII 原文反而無處可見。缺口在於：使用者無從得知該 ASCII 字串被注音排列解析成什麼讀音。組字區固然在同一位置插入了讀音，但那段讀音會被組字區既有內容擠壓、且在安全強化組字區等客體下根本不顯示。

## 落點

### 一、Tooltip 改為兩行（`InputHandler_HandleStates.swift`）

`generateStateOfInputting()` 內的混輸 Tooltip 區塊改為：首行 ASCII 原文、次行讀音；**讀音為空時不附加第二行**（否則 TooltipUI 之行高量測會多算一行空白、視窗多出一段下緣留白）。

```swift
if prefs.mixedAlphanumericalEnabled, !mixedAlphanumericalBuffer.isEmpty {
  let readingPreview = inlineReadingPreview
  result.tooltip = readingPreview.isEmpty
    ? mixedAlphanumericalBuffer
    : mixedAlphanumericalBuffer + "\n" + readingPreview
  result.tooltipDuration = 0 // 設為 0 使 Tooltip 恆久顯示，直到混打模式結束。
}
```

TooltipUI 本身無須改動：`TooltipContentView` 本以 `.usesLineFragmentOrigin` ＋ `.usesFontLeading` 繪製與量測，`\n` 之多行形制自標記狀態（`.ofMarking`）的既有兩行 Tooltip 起即在用。

### 二、讀音之取用與呈現（`InputHandler_CoreProtocol.swift` ＋ `IMEStateParsed.swift`）

`readingForDisplay`（組字區用）與 Tooltip 第二行的需求不同，故拆為三支：

```swift
var readingForDisplay: String { // 組字區：原行為不變
  let currentReading = inlineReadingForCompositionBuffer
  guard !prefs.cassetteEnabled else { return currentReading }
  return currentReading.isEmpty ? mixedAlphanumericalBuffer : currentReading
}

var inlineReadingPreview: String { // Tooltip 第二行
  guard !prefs.cassetteEnabled, !composer.isPinyinMode else {
    return inlineReadingForCompositionBuffer
  }
  return IMEStateParsed.convertReadingForTooltip(composer.getComposition(isHanyuPinyin: false))
}

private var inlineReadingForCompositionBuffer: String { …原 getInlineCompositionForDisplay 分支… }
```

本值是 Tooltip 的一部分，故**呈現規則沿用 Tooltip 自家既有的那一套**。倉內 `showHanyuPinyinInCompositionBuffer` 有兩套呈現、不可混用：

| 呈現位置 | 實作 | 拼音形態 |
|---|---|---|
| 組字區 | `composer.getInlineCompositionForDisplay(isHanyuPinyin: 偏好)` | 數字標調式（`mo3`） |
| Tooltip | `IMEStateParsed.readingThreadForDisplay`（標記狀態既有） | 注音**教科書式**（輕聲前置）；偏好啟用**且該 Tooltip 為橫排**時，改漢語拼音**教科書式標調**（`mǒ`） |

後者之「橫排」判準（`alwaysShowTooltipTextsHorizontally || !InputSession.isVerticalTyping`）與 `showTooltip(...)` 決定 Tooltip 排版方向的判準同源。故把該規則自 `readingThreadForDisplay` 抽成共用成員 `IMEStateParsed.convertReadingForTooltip(_:)`（逐字等義之重構），第二行呼叫同一支——偏好與直排兩個閘門一次到位，日後 Tooltip 讀音規則若有更動，兩處也不會各走各的。

**一項必須踩準的細節**：該轉換函式的輸入**恆為注音原字串**，故第二行的取值來源固定為 `getComposition(isHanyuPinyin: false)`。若圖省事餵入組字區那套「隨偏好變動」的取值（偏好開啟時已是 `mo3`），`restoreToneOneInPhona` 會替它補出 `mo31`，終得 `mǒ1`——`test218` 即為釘死此點而設。

## 驗證（2026-09-16，本機實測）

**新增測項**：

- `test_IH435_MixedTooltipInlineReadingPreview`（`InputHandlerTests_Cases4`）：注拼槽有內容時 Tooltip 恰為兩行（首行 ASCII 原文、次行讀音）、注拼槽為空時恰為單行。
- `test218_InputHandler_MixedTooltipReadingPreviewStyle`（`SessionTests_Cases2`；真 `InputSession`，可控 `InputSession.current` 與 `isVerticalTyping`）：偏好關＋橫排 → `ㄇㄛˇ`；偏好開＋橫排 → `mǒ`（並斷言組字區自身仍為數字標調式 `mo3`，證明兩者走不同規則）；偏好開＋直排 → 退回 `ㄇㄛˇ`；直排但強制橫排 → `mǒ`。

**全量測試**：`vChewing-LibVanguard` 與 macOS 之 `Packages/vChewing_OSNeutral_LibVanguard` 各跑 `swift test --no-parallel` → **538 項 0 失敗**（9 靶：18／37／7／6／206／193／61／3／7）。**CI**：三平台（linux／darwin／WinNT）已驗全綠——該批 run 之 HEAD 為含本 phase 之 `99f986a`，詳見 P222〈驗證〉。`vChewing-OSX-legacy` 之 `make debug-core` → `** BUILD SUCCEEDED **`（全 log 0 筆 `error:`）。

**格式**：`swiftformat --lint`（LibVanguard／macOS `--swiftversion 6.0`、legacy `--swiftversion 5.5`，皆 `--indent 2`）對 P221＋P222 之受改 9 檔報 `0/9 files require formatting`；兩倉 `make lintFormatUncommitted` rc=0。

**兩倉 byte-sync（`md5`）**：受改五檔於 LibVanguard 與 macOS 逐一同值——`InputHandler_CoreProtocol.swift` `2a68e38f…`、`InputHandler_HandleStates.swift` `86131679…`、`IMEStateParsed.swift` `a99ec6b1…`、`InputHandlerTests_Cases4.swift` `83c37d42…`、`SessionTests_Cases2.swift` `fe9fbad3…`。`vChewing-OSX-legacy` 三檔與 LibVanguard 之差異僅餘兩處 P213 併包時即有之模組名散註（`LibVanguard 通道` ↔ `OSNeutralAssembly 通道`、`LibVanguard 僅負責` ↔ `OSNeutralAssembly 僅負責`），即本 phase 之新增段落逐字元相同。

## 未驗

- **真機 GUI 目視**：兩行 Tooltip 之行距、視窗高度與直排情境未經人力目視——本 phase 只驗到 `IMEState.tooltip` 字串層級，未在實際 IMK 客體中開窗觀察。
- **磁帶模式**：`inlineReadingPreview` 之磁帶短路分支不可達（混輸與磁帶互斥），純屬結構對稱。

## 附註

- 本 phase 未新增使用者可見文字，故未動任何 `.strings`（`vChewing-LibVanguard` 本即無此類資源）。
- `readingForDisplay` 與 `readingThreadForDisplay` 兩處改寫皆逐分支等義，由既有測試「不改一行即全綠」反向印證；新測項測的是新行為。
- **建置環境（供日後取用）**：本工作區之 SwiftPM 需 `--disable-sandbox` ＋ 三條 in-workspace 之 cache／config／security 路徑（否則撞上巢狀沙盒 `sandbox-exec: sandbox_apply: Operation not permitted`）；`vChewing-OSX-legacy` 之 `make debug-core` 則須完整檔案權限——`xcodebuild` 需寫 `~/Library/Developer/Xcode/DerivedData`，且專案內之「Build Dictionary Data」run-script 會自行再起一次 SwiftPM。
- Swift 原始碼內未出現任何 Phase 編號。

--------------------

# Phase 222

Assignee: Deepseek-v4-flash（Deepseek harness）。

> **狀態：完結（2026-09-16）。** 新增偏好 `SpecifyShiftSpaceKeyBehavior4EmptyState`：一般打字且輸入狀態為 `ofEmpty` 時，Shift+空格鍵敲出全形（預設）或半形空格。三倉同步；現行 commit：`vChewing-LibVanguard` `99f986a`、`vChewing-macOS` `58622b3b`、`vChewing-OSX-legacy` `8ed18d04`（同訊息 `InputHandler // Add Shift+Space width preference for the empty state.`）。已推送 `origin/main`；**CI（linux／darwin／WinNT）三平台全綠**（見〈驗證〉）。

## 前導 Phase

P221（同卷前一 phase，同屬「空格鍵／混輸」家族）。直接模板為既有之 `kSpecifyShiftSpaceKeyBehavior4CandidateWindow`——它示範了「`Bool` ＋ `options` 兩選項」之 UserDef 形制，本 phase 逐項照抄。

## 手術範圍

- `vChewing-LibVanguard`（先行，5 檔）：`Sources/Shared/UserDef/UserDef.swift`、`Sources/Shared/Protocols/PrefMgrProtocol.swift`、`Sources/Shared/PrefMgr_Core.swift`、`Sources/LibVanguard/InputHandler/InputHandler_TriageInput.swift`、`Tests/LibVanguardTests/SessionTests_Cases2.swift`
- `vChewing-macOS`（11 檔）：前列五檔之對位 ＋ `Packages/vChewing_SettingsUI/…/SettingsUI/VwrSettingsPaneBehavior.swift`（SwiftUI）＋ `…/SettingsCocoa/VwrSettingsPaneCocoaBehavior.swift`（Cocoa）＋ `Sources/vChewingIME_macOS/Resources/{en,ja,zh-Hans,zh-Hant}.lproj/Localizable.strings`
- `vChewing-OSX-legacy`（9 檔）：前四檔之對位（該倉不承擔單元測試）＋ 該倉 Cocoa 設定面板 ＋ `vChewing/Resources/{en,ja,zh-Hans,zh-Hant}.lproj/Localizable.strings`

## 要解決的問題

事主原文：「得允許使用者指定一般打字『輸入狀態為 empty』時的 Shift+Space 的行為是敲出全形還是半形。（預設仍是全形）」「可能得需要調整既有的內容的文字說明，不然容易與 `(Shift+)空格鍵 (選字窗):` 混淆。」

原本 `InputHandler_TriageInput.swift` 之 `triageByKeyCode()` 內、`.kSpace` 之 `case .ofEmpty:` 寫死 `input.isShiftHeld ? "　" : " "`，即「不帶 Shift 者恆半形、帶 Shift 者恆全形」，無偏好可調。此即本 phase 唯一要開放的維度。

## 落點

### 一、新偏好 `kSpecifyShiftSpaceKeyBehavior4EmptyState`（三處一體，缺一即編不過）

| 檔案 | 追加內容 |
|---|---|
| `UserDef.swift` | 列舉項 `kSpecifyShiftSpaceKeyBehavior4EmptyState = "SpecifyShiftSpaceKeyBehavior4EmptyState"`；`dataType` 之 `.bool(false)`（**預設關＝全形＝舊行為**）；`metaData` 之兩 `options` |
| `PrefMgrProtocol.swift` | `var specifyShiftSpaceKeyBehavior4EmptyState: Bool { get set }` |
| `PrefMgr_Core.swift` | 對位之 `@AppProperty` 屬性 |

形制逐項照抄 `kSpecifyShiftSpaceKeyBehavior4CandidateWindow`（`Bool` ＋ `options`）：`Bool` 型偏好一旦帶 `options`，SwiftUI 與 Cocoa 兩側之控件即自動由勾選項改繪為二選一（`0`↔`false`、`1`↔`true`），無須為本項另寫控件。`validNumeralValueRange` 只服務 `Int` 型故未動；本項為全新鍵、無舊鍵對位，故亦未動 `fixOddPreferencesCore()` 與 `migrateDeprecatedSettings()`。

### 二、分診行為（`InputHandler_TriageInput.swift`）

```swift
case .ofEmpty:
  if !input.isHoldingAny([.option, .control, .command]) {
    // 一般打字且組字區為空時：不帶 Shift 的空格鍵恆插入半形空格；
    // 帶 Shift 者之寬度由偏好決定（預設全形，與舊行為一致）。
    let wantsHalfWidth = !input.isShiftHeld || prefs.specifyShiftSpaceKeyBehavior4EmptyState
    session.switchState(State.ofCommitting(textToCommit: wantsHalfWidth ? " " : "　"))
    return true
  }
```

不帶 Shift 的空格鍵**不受此偏好影響**、恆半形；作用域僅 `.ofEmpty`（`.ofInputting` 之輪替與狂拼固化語意未動）。

### 三、設定介面與 i18n

macOS SwiftUI、macOS Cocoa、legacy Cocoa 三處同構：與既有之選字窗項**同一 Section、新項在上**，令兩者對比一目瞭然。i18n 為四語系 ×2 倉 ＝ 8 檔，新增 4 鍵，並依事主指示**改寫既有 `kSpecifyShiftSpaceKeyBehavior4CandidateWindow.description`**——各語系末尾追加一條「作用域僅限選字窗、一般打字之寬度見上方該項」之互指註記（新項描述則反向指回），兩者自此不再混淆。

用詞一律以倉內既有語料之「英文 ↔ 目標語言」對位定案，不取自創譯法：

| 概念 | en（既有鍵） | ja | zh-Hant | zh-Hans |
|---|---|---|---|---|
| 組字緩衝區 | `compositor` / `composition buffer` | `入力緩衝列` | `組字區` | `组字区` |
| 緩衝區為空 | `only if the compositor is empty` | `入力緩衝列が空の時` | `組字區為空` | `组字区为空` |
| 空格（字元） | `Insert space` | `空白` | `空格` | `空格` |
| 全形／半形 | `full-width` / `half-width` | `全角`／`半角` | `全形`／`半形` | `全形`／`半形` |
| 僅對某範圍有效 | `Only takes effect…` | `…にのみ有効` | `僅對…生效` | `仅对…生效` |

四語系 `shortTitle`：`Shift+空格鍵 (組字區為空時):`／`Shift+空格键 (组字区为空时):`／`Shift+Space (Empty Compositor):`／`Shift+Space (入力緩衝列が空の時):`。**`zh-Hant` 一律用 `僅`**（不用港式康熙之 `祗`）；**`zh-Hans` 依 zh-Hans-TW 慣例——僅做字元簡化、詞彙沿用台灣**（`字元` 而非 `字符`、`設定` 而非 `设置`、`半形` 而非 `半角`、`選字窗` 而非 `候选窗`）。

## 驗證（2026-09-16，本機實測）

**新增測項**：`test219_InputHandler_ShiftSpaceEmptyStateWidth`（`SessionTests_Cases2`；真 `InputSession` ＋ 真 `InputHandler` ＋ `MockClientProxy`）三路逐條斷言——① 不帶 Shift（即使偏好為 `true`）→ 客戶端收到 `" "`；② 帶 Shift 且偏好 `false`（預設）→ `"　"`；③ 帶 Shift 且偏好 `true` → `" "`。走完整分診鏈，非僅測組態讀寫。

**全量測試**：同 P221——`vChewing-LibVanguard` 與 macOS 側各 **538 項 0 失敗**（9 靶：18／37／7／6／206／193／61／3／7）。

**建置**：macOS 之 `swift build --package-path Packages/vChewing_SettingsUI` 與倉根 `swift build` 皆 `Build complete!`（含遠端 lexicon 之 build plugin 全程）；legacy `make debug-core` → `** BUILD SUCCEEDED **`（0 筆 `error:`）。

**偏好中繼資料之自動查核**：`swift run vChewingSharedCLI list-pending-userdef` 僅列三項既存者（`kKanjiConversionPreferences`／`kCandidateListTextSize`／`kPopupCompositionBufferTextSize`），**新鍵不在列**——即其 `shortTitle`／`description`／`options` 六個欄位皆已直接以 `i18n:` 鍵書寫，未留待人工遷移。

**i18n 檔案**：8 檔 `plutil -lint` 全過；另以倉內 `LocalizableFileSorter.swift` 對 8 檔各跑一次乾跑比對，**8 檔皆未被改動**，即鍵序仍符合本倉自己的排序器。

**格式**：`swiftformat --lint` 對 P221＋P222 之受改 9 檔報 `0/9 files require formatting`；兩倉 `make lintFormatUncommitted` rc=0。

**CI（2026-09-16，事主推 `origin/main` 後三平台全綠）**：推送之 HEAD 即本 phase 之 `99f986a`（內容含 P221），故三條 run 同時涵蓋兩 phase 之全部改動：

| 平台 | workflow | run ／ job |
|---|---|---|
| linux | `test_ubuntu_LibVanguard.yml` | `35112264660` ／ `104848750617` |
| darwin（Swift OpenSource） | `test_darwin_LibVanguard_SwiftOpenSource.yml` | `35112264678` ／ `104848750669` |
| WinNT | `test_winnt_LibVanguard.yml` | `35112264645` ／ `104848750000` |

（第四條 `test_darwin_LibVanguard_Xcode.yml` CI Workflow 已由事主於 GitHub 端**停用**、故不產生 run：GitHub CI 現無 Xcode 27，而 Xcode 26 內建之 Swift 6.3.3 恰落於本專案以封堵 manifest 刻意拒絕的 6.0～6.3 之內——該條要跑得等 CI 供得起 Xcode 27。該「停用」之決策或在今後 GitHub CI 提供 Xcode 27 的時候結束，屆時會擇日重啟該 CI Workflow。）

## 未驗

- **真機 GUI 目視**：三處設定介面新增之二選一下拉（其繪製路徑與既有選字窗項完全相同，屬既有機制）、四語系描述在 Cocoa 固定寬度欄位內之排版。
- **舊使用者升級後之預設值**：新鍵不存在於 `UserDefaults` 時由 `AppProperty` 之預設值機制取 `false`（＝全形），屬既有機制，本 phase 未另驗。

## 附註

- 本工作區現無 `UserGuide*` 文件可更新（已全倉搜尋確認）；`vChewing-HomePage.io` 不在事主指定之影響範圍內，未動。
- 本 phase 未新增任何 Swift 檔案（新檔僅存在於 `.strings`，非 Swift，故不涉版權 banner）。
- 與 P221 之關係：兩者同屬「空格鍵／混輸」家族，但 P221 動的是 Tooltip 之顯示內容、本 phase 動的是 `.ofEmpty` 之按鍵語意與偏好體系，**落點互不重疊**，可各自獨立回退。
- Swift 原始碼內未出現任何 Phase 編號。

--------------------

# Phase 223

Assignee: Deepseek-v4-flash（Kimi Code harness）。

> **狀態：完結（2026-09-17，本機實測）。** 為 legacy 發行版補上 `make archiveLegacy`，產出一份 Xcode Organizer 認得的 `.xcarchive`，供事主在 Xcode 內手動以 Developer ID 簽名與公證。**未 commit（待事主 finalize）**，動工面為 `vChewing-macOS` 六檔（`Makefile`、`Plugins/BundleAppsLegacy/plugin.swift`、`AGENTS.md`、`CLAUDE.md`、`README.md`、`.github/copilot-instructions.md`）；`vChewing-OSX-legacy` **未動**（該倉之 `archive` 走 Xcode 專案、本即有之）；`vChewing-LibVanguard` 未動。

## 前導 Phase

P218（Swift 5.10 legacy 路徑之終點推進：倉根兩支執行檔 ＋ `bundleLegacy` 之 app 組裝）。**對位目標是現代側早已存在的 `make archive`**（`swift package … bundle-apps --archive`，其實作見 `Plugins/BundleApps/plugin.swift` 之 `assembleXcarchive`）；本 phase 即把那條路原樣複製到 legacy 側，兩側自此在「產出 archive」上同形。

## 手術範圍

- `vChewing-macOS/Makefile`：新增 `archiveLegacy` 目標（並補進 `.PHONY`）。
- `vChewing-macOS/Plugins/BundleAppsLegacy/plugin.swift`：新增 `--archive` 旗標、`.xcarchive` 組裝（新 `// MARK: - .xcarchive Assembly` 一節，兩個新成員），並把 `assembleAppBundles` 之回傳值自 `URL` 改為「組裝目錄 ＋ 版本對」。
- 指引檔四份：`AGENTS.md`（§2 之 legacy 入口清單、`Plugins/BundleAppsLegacy` 條目）、`CLAUDE.md`、`README.md`、`.github/copilot-instructions.md`。

## 要解決的問題

事主原文：「vChewing-macOS 似乎沒有 archiveLegacy 命令（用以生成 xcarchive 手動給 Xcode 簽名公證所用）。你實作一下。」

現代側早有一條 `make archive`，把 SwiftPM 產物包成 `.xcarchive` 並搬進 `~/Library/Developer/Xcode/Archives/<date>/`，於是「Xcode Organizer → Distribute App → Developer ID」那條簽名公證流水線走得通。legacy 側卻只有 `debugLegacy`／`releaseLegacy`／`bundleLegacy`／`cleanLegacy`，產物止於 `Build/Products/Legacy/` 的兩份 `.app`——沒有 archive，也就沒有那條流水線的入口。

## 落點

### 一、`.xcarchive` 之形制（照現代側逐項對位）

```
vChewingInstallerLegacy-YYYY-M-D-HHMMhrs.xcarchive/
  Info.plist
  Products/Applications/vChewingInstallerLegacy.app/
  dSYMs/
    vChewing.app.dSYM/
    vChewingInstallerLegacy.app.dSYM/
```

- **`Info.plist`**：`ArchiveVersion 2`／`CreationDate`／`Name`／`SchemeName`／`ApplicationProperties`（`ApplicationPath`／`Architectures`／`CFBundleIdentifier`／`CFBundleShortVersionString`／`CFBundleVersion`／`SigningIdentity`／`Team`）。`SigningIdentity` 與 `Team` 依現代側之例留空：archive 內之 app 是 ad-hoc 簽章，Developer ID 身分屬於匯出端那台機器。
- **`Products/Applications/` 放的是安裝程式**，不是 IME——IME 以 `Contents/Resources/vChewing.app` 棲身其內。這不是取捨而是對位：現代側之 archive 同樣只放 `vChewingInstaller.app`（實查 `Build/Products/Debug/vChewingInstaller.app/Contents/Resources/` 亦為此形），且 legacy 的那支 Xcode 專案 archive 的本來就是這個 scheme（`xcodebuild archive -scheme vChewingInstallerLegacy`）。
- **`dSYMs/`** 對兩支執行檔各出一份（`xcrun dsymutil`，非 AppleScript 式之猜測）；release 產物仍帶 debug map，故 dSYM 為實物（實測 35 MB ＋ 7.2 MB）。
- **刻意不含 `SwiftSupport/`**：那一區是 App Store 上傳用的（供審查端重簽內嵌之 Swift 執行期），本 archive 之用途是 Developer ID 簽名與公證——與現代側一致。
- 檔名沿用現代側 `…-YYYY-M-D-HHMMhrs.xcarchive` 之節奏，legacy 側冠以 `vChewingInstallerLegacy-`。

### 二、開關與分工

- **plugin 端**新增 `--archive`（與現代側同名）。`assembleAppBundles` 之回傳值自 `URL` 改為 `(appsDir: URL, version: (marketing: String, build: String))`，讓 archive 直接沿用剛剛組 bundle 時讀到的 `Release-Version.plist` 版本對，不必再讀一次。
- **archive 只寫在套件目錄內**（`Build/Products/`）：command plugin 之寫入權止於 `--allow-writing-to-package-directory`，故搬進 `~/Library/Developer/Xcode/Archives/<date>/` 是 `make` 的事——與現代側 `archive` 目標以 glob 搬走同一手法，連「搬而非複製、`Build/Products/` 下不留 `.xcarchive`」之行為也一致。
- **`Makefile` 端**：`archiveLegacy: LEGACY_CONFIG := release` 且前置為 `releaseLegacy`——archive 是發行產物，單架構 debug 的 archive 既未最佳化又只有 x86_64。`LEGACY_CONFIG` 之 target-specific 值亦令 `$(LEGACY_PRODUCTS_DIR)` 指向 release 產物目錄（否則會落到 debug 那一份，這正是 `bundleLegacy` 之預設 config 踩過的坑）。
- `Architectures` 由 `lipo -archs` 現讀而非寫死：legacy 之 universal 是兩次 `swift build`（`x86_64-apple-macosx10.9` 與 `arm64-apple-macosx11.0`）各自出 slice 後再 `lipo` 併起來的，與一次建置出 universal 的現代側不同路。

## 驗證（2026-09-17，本機實測）

- **端到端**：`make archiveLegacy` 全程 **rc=0**（含 `releaseLegacy` 之兩支 triple 建置、`lexiconLegacy`、plugin 之兩份 bundle 組裝與 archive 組裝），產物 `Build/Products/vChewingInstallerLegacy-2026-9-17-0028hrs.xcarchive` 隨即搬進 `~/Library/Developer/Xcode/Archives/2026-09-17/`。
- **結構**：`Info.plist` ＋ `Products/Applications/vChewingInstallerLegacy.app` ＋ 兩份 dSYM；總 **101 MB**（dSYM 35 MB ＋ 7.2 MB）。
- **archive `Info.plist`**（`plutil -p` 節錄）：`ArchiveVersion 2`、`Name`／`SchemeName` = `vChewingInstallerLegacy`、`ApplicationPath = Applications/vChewingInstallerLegacy.app`、`Architectures = [x86_64, arm64]`、`CFBundleIdentifier = org.atelierInmu.vChewing.vChewingInstallerLegacy`、`CFBundleShortVersionString = 4.7.4`／`CFBundleVersion = 4740`（皆取自 `Release-Version.plist`）；archive 之 `Info.plist` 與其內 app 之 `Info.plist` 皆 `plutil -lint` OK。
- **dSYM 與執行檔之 UUID 逐支相同**（`dwarfdump --uuid`）：`vChewing.app.dSYM` = `4DC5240F-0C79-335F-87B8-71153EB2549E`（x86_64）／`1C134D15-C926-3D22-9365-EF967E794437`（arm64），與安裝程式內 `Contents/Resources/vChewing.app/Contents/MacOS/vChewing` 完全一致；`vChewingInstallerLegacy.app.dSYM` = `578C3B81-1971-346E-917C-A54486D2DD22`／`4B36E21C-DF93-35E3-A407-B1FFBBCFF2B6`，與 `Contents/MacOS/vChewingInstallerLegacy` 一致。
- **簽章**：`codesign --verify --deep --strict` 對 archive 內之安裝程式**通過**；`Identifier = org.atelierInmu.vChewing.vChewingInstallerLegacy`、`flags=0x10002(adhoc,runtime)`、`Signature=adhoc`（正式之 Developer ID 簽章待事主於 Organizer 匯出時為之）。
- **巢狀 IME 仍在**：`Contents/Resources/vChewing.app` 完整；`Build/Products/Legacy/` 之二份 `.app` 照舊留存。
- **格式管線之後之復跑**：`make lintFormatUncommitted`（rc=0）對本 phase 之新碼補了 **19 處 `trailing_comma`**（SwiftLint）並經 SwiftFormat 重排——該管線謀求的本就是兩者順序運行後的最終結果；復跑 `make archiveLegacy` 仍 **rc=0**、產出第二份 archive（`vChewingInstallerLegacy-2026-9-17-0030hrs.xcarchive`），結構與簽章複查同前。附帶證明：SwiftPM 編譯 command plugin 用的是**驅動它的那支 toolchain**（此處即 Swift 5.10.1），故該次復跑同時證明新碼——含 SwiftLint 補上的那些尾逗號——在 5.10 下可編。

## 未驗

- **Xcode Organizer 之載入與匯出**：本 phase 之驗證止於 archive 之形制本身——照現代側 `assembleXcarchive` 逐項對位，再以 `plutil`／`codesign`／`dwarfdump` 三方實查；Organizer 端之載入、以及匯出時對「巢狀於 `Resources/` 之 IME」之重簽是否順遂，未經本 phase 實跑（該巢狀形制與現代側完全相同，非本 phase 引入）。
- **公證本身**（`notarytool` 上傳與 `stapler`）。
- **arm64 slice 於真機之執行**：與 P218 之同項未驗事項相同，非本 phase 新增。

## 附註

- **未動 `vChewing-OSX-legacy`**：該倉之 archive 走 `xcodebuild archive -project vChewing.xcodeproj -scheme vChewingInstallerLegacy`，早已存在且有自己的一套 `ARCHIVE_NAME`；本次只在主倉補上對位的 `.xcarchive` 產出。
- **未新增 Swift 檔案**：plugin 之新碼全落在既有檔內（新 extension 一節）。
- **指引檔四處同步**：`AGENTS.md` §2 之「Entry points」清單與 `Plugins/BundleAppsLegacy` 條目（含 `--archive` 之產物形制與「寫入權止於套件目錄、搬移由 `make` 負責」之理由）、`CLAUDE.md`、`README.md`、`.github/copilot-instructions.md`。
- 本 phase 未新增測試：archive 之正確性屬產物形制，已由上述結構／plist／UUID／簽章四項實查涵蓋。
- Swift 原始碼內未出現任何 Phase 編號。

--------------------

# Phase 224

Assignee: Deepseek-v4-flash（Kimi Code harness）。

> **狀態：完工（2026-09-17，本機實測，**已 commit**）。** 修復 POM（漸退記憶）持久化層的**取鎖順序倒置**（ABBA）死鎖。正本為 `vChewing-LibVanguard`（`ce62f75`），`vChewing-macOS` 之巢狀副本逐位元組同步（`b2bbbe61`，同 commit 另含 `LXMgr_Core.swift` 之註解更正與新測試檔）；本卷文書入庫為 `db09b24`。

## 前導 Phase

P223。**病灶之來歷**：`vChewing-macOS` issue #616〈[功能問題提報] 加詞／濾除與漸退記憶背景存檔並行時死鎖，輸入法永久無回應〉（回報版 **4.7.4**）。工單樓主之 `sample` 抓到兩條執行緒皆 889/889 個樣本停在同一處：

- 主執行緒：`performUserPhraseOperation(addToFilter:)` → `bleachSpecifiedPOMSuggestions(targets:saveCallback:)` → `LXPerceptor.bleachSpecifiedSuggestions(candidateTargets:saveCallback:)` → `PerceptionPersistor.markKeyForUpsert(_:)` → `__psynch_mutexwait`
- `LXAssembly_POM` 佇列：`savePerceptionOverrideModelData(_:coordinator:)` → `savePOMData(toURL:)` → `PerceptionPersistor.saveData(dataProvider:mapProvider:keyValidator:toURL:)` → `preparePendingJournalRecords(mapProvider:keyValidator:)` → `LXPerceptor.saveData(toURL:skipDebounce:)` 之 closure → `__psynch_mutexwait`

症狀：完全打不出字、CPU 0%、無 crash report、不會自行恢復，只能強制結束 process。機率隨 POM 資料量上升（該機 POM 累積 2,218 筆時一天內四次）。

## 手術範圍

- `vChewing-LibVanguard/Sources/LexiconAssembly/SubLMs/lxPerceptionPersistor.swift`：`saveData` 之取鎖結構重寫、`preparePendingJournalRecords` 改收狀態快照、`shouldCompactJournal(for:)` 之鎖歸屬變更、類別 doc 新增〈取鎖紀律〉、新增僅供測試之 `isLockHeld` 探針。
- `vChewing-LibVanguard/Tests/LexiconAssemblyTests/POMLockDisciplineTests.swift`：**新檔**（3 支測試）。
- `vChewing-macOS/Packages/vChewing_OSNeutral_LibVanguard/`：上列兩檔逐位元組同步（`Sources/` 與 `Tests/` 實查 `diff -rq` 無差異）。
- `vChewing-macOS/Packages/vChewing_MainAssembly4Darwin/Sources/MainAssembly4Darwin/LXManager/LXMgr_Core.swift`：一行殘留註解更正（見〈附註〉）。
- `vChewing-OSX-legacy`：**未動**（該倉自 4.8.0 起已封存，且已不在本工作區內）。

## 要解決的問題

**兩把鎖、兩個相反順序。** issue 的兩份堆疊各持一把鎖、各等對方那把：

一、**`LXPerceptor` 側（LXPerceptor.lock → persistor.lock）**：`LXPerceptor` 的五個改寫入口（`memorizePerception`、三個 `bleachSpecifiedSuggestions`、`bleachUnigrams`）都在 `lock.withLock { … }` 之內呼叫 persistor 的 pending-key 標記：

```swift
keysNeedingUpsert.forEach { persistor.markKeyForUpsert($0) }        // LXPerceptor.swift:804
keysToRemoveCompletely.forEach { persistor.markKeyForRemoval($0) }  // LXPerceptor.swift:805
```

同型者另有 687、707、709、753、754、838、863。這一側是**寫在檔內的既有契約**——persistor 檔內那一節的標題即 `// MARK: - Pending Key Tracking (called by LXPerceptor under its lock)`。

二、**persistor 側（persistor.lock → LXPerceptor.lock）**：`PerceptionPersistor.saveData` 在持自有鎖時呼叫回呼，而回呼之一正是 `LXPerceptor.saveData` 傳進來的 `mapProvider`：

```swift
let records: [JournalRecord] = lock.withLock {
  preparePendingJournalRecords(mapProvider: mapProvider, keyValidator: keyValidator)  // 舊版
}
```

```swift
mapProvider: { [self] in lock.withLock { mutLRUMap } },   // LXPerceptor.swift:928
```

兩序相反 ⇒ 主執行緒持 `LXPerceptor.lock` 等 persistor 鎖、POM 佇列持 persistor 鎖等 `LXPerceptor.lock` ⇒ 永久互鎖。

**為何 4.8.0 已不復現、而本 phase 仍必須動它**（此為本 phase 之核心判斷）：

- macOS 倉 `60eb5f33`（2026-09-15，`LXMgr // Call savePOMData on MainActor.`，已是 tag `4.8.0` 之祖先）把 coordinator 裡真正落盤的那句包進 `asyncOnMain`；**4.7.4 時同一句是裸跑在 `LXAssembly_POM` 佇列上的**——那正是 issue 堆疊第二段的來源執行緒。
- 4.8.0 之 LibVanguard 以 Swift 6.4 建置，`Package.swift` 對含 `LexiconAssembly` 在內的 12 個 target 下 `.defaultIsolation(MainActor.self)`，故 `LXPerceptor`／`PerceptionPersistor` 本身即 main-actor-isolated，編譯器不允許非主執行緒呼叫 `saveData`；全倉實查 `savePOMData` 亦僅兩處引用（`LXFacade_POMRepresentable.swift:49` 之門面、與 `LXMgr_Core.swift` 內 `asyncOnMain` 之呼叫）。
- **但倒置本身仍逐字寫在原始碼裡**，且 5.10／legacy 側沒有 `defaultIsolation`（那兩個型別在那裡是帶**真** `NSLock` 的 non-isolated 型別，編譯器不會攔非主執行緒的落盤）。即：4.8.0 之所以不炸，是**靠呼叫端把落盤挪回主執行緒兜住的**，不是靠鎖的順序本身正確；任何未來在背景佇列上觸發 POM 存檔或改寫的呼叫端都會把這個坑重新打開。

## 沿革追溯（2026-09-17 追加）

**病灶自 vChewing 4.4.0 起；引入者為 `a7cab4e0`（2026-04-18，`Perceptor & Megrez // API upgrades for future purposes.`）。**

| 日期 | commit | 事件 | 首個含此 commit 之發行版 |
|---|---|---|---|
| 2025-10-29 | `afcd05ac` | POM 存檔改由背景 debounce 佇列落盤（`LMMgr.POMSavingCoordinator` 之雛形）——**第二條執行緒登場** | 4.0.5 |
| 2025-11-07 | `dd0fa4d3` | POM 持久化改為 WAL 日誌格式：`markKeyForUpsert(_:)`／`preparePendingJournalRecords(…)` 問世，但該檔**全無鎖** | 4.1.0 |
| 2025-11-08 | `610fc45f` | `LMAssembly // POM: Use NSLock for thread safety.`——perceptor 與 persistor 同處一類、**共用同一把鎖**，且 `saveData` 呼叫的是無參數的 `preparePendingJournalRecords()`（無跨物件回呼） | 4.1.0 |
| **2026-04-18** | **`a7cab4e0`** | **persistor 自 `lmPerceptionOverride.swift` 析出為獨立類別 `lmPerceptionPersistor.swift`（自帶 `private let lock = NSLock()`），並改用 `mapProvider`／`dataProvider`／`keyValidator` 閉包回頭取 perceptor 的鎖** ⇒ **兩把鎖、兩個相反順序於此成形** | **4.4.0**（2026-04-27 發版） |
| 2026-09-15 | `60eb5f33` | coordinator 之落盤包進 `asyncOnMain`——第二條執行緒消失、**故障被掩蓋** | 4.8.0 |
| 2026-09-17 | `ce62f75`／`b2bbbe61` | 本 phase 之修復 | （未發版） |

佐證：

- `git log -S 'mapProvider' --all` 全史只命中兩處：`a7cab4e0`（首次出現）與本 phase 之修復 commit。即「persistor 持鎖呼叫回呼」這個形制是 `a7cab4e0` 造出來的，此前不存在。
- `610fc45f` 當時之 `saveData`：`preparePendingJournalRecords()` 不帶任何參數，`markKeyForUpsert(_:)`／`markKeyForRemoval(_:)`／`preparePendingJournalRecords()` 三個私有方法**自身皆不取鎖**（由持鎖的呼叫端負責），而 `dataProvider`／`mapProvider` 這類閉包名在該檔內一次都沒出現——故 4.1.0～4.3.x 的 POM 只有「一把鎖 ＋ 無跨物件回呼」，**無從構成倒置**（該期間的問題是資料競爭與否，不是互鎖）。
- `a7cab4e0` 當時三個要件俱在：perceptor 在 `lock.withLock { … }` 內呼叫 `persistor.markKeyForUpsert`／`markKeyForRemoval`（`lmPerceptionOverride.swift` 行 510／511／561／562／595／620，另有 444／464／466）；persistor 在 `lock.withLock { preparePendingJournalRecords(mapProvider:keyValidator:) }` 內呼叫回呼（`lmPerceptionPersistor.swift:170-171`）；而那個 `mapProvider` 正是 perceptor 傳進來的 `{ [self] in lock.withLock { mutLRUMap } }`（同檔行 685）。
- 第二條執行緒於 4.4.0 確實在場：`LMMgr_Core.swift` 於 4.4.0 之 `targetLangModels.forEach { $0.savePOMData() }` 仍裸跑在 `pomDebounceQueue` 上（該版第 544 行，其上註解即 `// Disk I/O runs on pomDebounceQueue to avoid blocking MainActor.`）。

**故障窗口：4.4.0（2026-04-27 發版）～ 4.7.4（2026-09-08）**，約四個半月；issue #616 之回報版 4.7.4 正在窗內。

## 落點

### 一、`PerceptionPersistor` 之鎖改為嚴格葉鎖（單邊消滅倒置）

- **`saveData` 不再於持鎖狀態下呼叫任何回呼**：新增 `private func pendingKeys() -> PendingKeys` 與 `private struct PendingKeys { upserts, removals, needsFullSnapshot }`——鎖內只做「排序鍵集 ＋ `needsFullSnapshot`」的**狀態快照**，出鎖後才跑 `mapProvider`／`keyValidator` 組 records，最後回鎖清 pending。`preparePendingJournalRecords` 改收該快照；其內原有的 `if needsFullSnapshot { return [] }` 早退改由呼叫端之 `guard !pending.needsFullSnapshot` 承接（同一時間點、同一語意，行為不變；該 guard 與其後 `records.isEmpty` 之 guard 共用同一句日誌）。
- **`shouldCompactJournal(for:)` 之鎖歸屬反轉**：舊版是呼叫端持鎖呼叫它（函式內含 `attributesOfItem` 之檔案 I/O）；改為函式自行在需要讀 `journalEntriesSinceLastCompaction` 時取鎖，呼叫端不再持鎖。此處非死鎖來源，改的是「持鎖期間不做 I/O」這條同族的紀律。
- **類別 doc 新增〈取鎖紀律〉**：明寫本類別的鎖恆為葉鎖、取用期間不得呼叫 `dataProvider`／`mapProvider`／`keyValidator`／`loadCallback`／`replayApplicator`，並寫出失敗模式（與 `LXPerceptor` 之持鎖路徑互為倒置即成永久死鎖）。
- **新增 `isLockHeld`（internal，標註僅供測試）**：實作為 `NSLock.try()`——同一執行緒在持鎖狀態下重入取鎖時亦回 `false`，故可把上述紀律釘成可斷言的性質。此探針置於類別本體之 `// MARK: Internal` 段（不另開 extension：該檔已有一個以上同型 extension，另開會多出一筆 `no_grouping_extension`）。

**`LXPerceptor` 側刻意一行不動**：該側「持鎖時呼叫 persistor 之 pending-key 標記」是既有契約（見上引 `MARK` 標題），且那些呼叫是**葉操作**（只取一把鎖、不呼叫任何回呼）。倒置的成因在 persistor 側那一處嵌套，故單邊消滅即足；動 `LXPerceptor` 只會平白改動與 macOS 倉逐位元組同步的檔案。

### 二、回歸測試（新檔，3 支，皆單執行緒可確定判定）

死鎖本身是時序性的、且 6.4 建置下此二型別為 main-actor-isolated（寫不出真正的兩執行緒競態測試，硬寫也只能靠 `assumeIsolated` 之類的不安全手段）。故改以**探針斷言不變式**：回呼被呼叫的那一刻，鎖不得被持有。

- `testSaveDataJournalCallbacksRunOutsidePersistorLock`：快照檔先存在（否則 `saveData` 會走完整快照分支而提前返回）＋ 標記一筆待 upsert 鍵，走「追加日誌」分支；斷言 `mapProvider`／`keyValidator` 執行時鎖未被持有。末以「日誌檔確實落地」佐證該二回呼真的被走過，避免斷言空轉為真。
- `testSaveDataSnapshotCallbackRunsOutsidePersistorLock`：快照檔不存在 → 走完整快照分支，斷言 `dataProvider` 亦然。
- `testLoadDataCallbacksRunOutsidePersistorLock`：造一份快照 ＋ 一行合法日誌記錄，斷言 `loadCallback`／`keyValidator`／`replayApplicator` 三者亦然。

## 驗證（2026-09-17，本機實測）

- **紅綠雙驗（本 phase 之關鍵證據）**：把新測試檔對 **HEAD 版**的 persistor（僅就地補進 `isLockHeld` 探針、其餘原樣）跑 —— `testSaveDataJournalCallbacksRunOutsidePersistorLock` **失敗 2 筆**，失敗點正是 `!lockHeldDuringMapProvider` 與 `!lockHeldDuringKeyValidator`（即：`mapProvider`／`keyValidator` 是在**持鎖狀態**下被呼叫的，與 issue 病灶同一件事）；另兩支照過（快照與載入路徑本即未嵌套）。換上修後版本則 **3/3 全綠**。
- **`LexiconAssemblyTests` 全靶：196 tests / 31 suites 全過**（含 `POMA*` 之存檔、日誌重播、損毀日誌處置、LRU、衰減、容錯查詢等既有覆蓋）。
- **`vChewing-LibVanguard` 之全靶 release 測試**（`swift test -c release --no-parallel`）：**9 次靶級測試、541 支全過**（18／37／7／6／206／196／61／3／7），零失敗、零 `error:`。
- **`vChewing-macOS` 之端到端建置**：`swift package --allow-writing-to-package-directory bundle-apps -- --debug`（即 `make debug`）全程通過，`✓ vChewing.app assembled`／`✓ vChewingInstaller.app assembled`／`✅ Done`——證明逐位元組同步後之巢狀副本可在主倉的實際組裝路徑下編譯並組出兩份 app bundle。
- **`vChewing-macOS` 側之巢狀副本**：逐位元組同步後，同組 3 支測試在同倉建置並通過。
- **格式管線**：兩倉之 `swiftlint --fix --autocorrect` ＋ `swiftformat` 對新碼就地跑過（SwiftLint 一度「Corrected `void_return` 3 times／`trailing_comma` 1 time」，經 SwiftFormat 斧正後最終碼內無實質差異——與 P216 記下的那條觀察一致）。改正後之 persistor 檔，其 SwiftLint 違規集合與 HEAD 版**逐條相同**（僅既有 `file_length` 之計數自 529 → 572）；新測試檔 `rc=0`。

## 未驗

- **真機 10.9／10.10／11.x 之運行時**：與 P218 同項未驗事項，非本 phase 新增。
- **對 4.7.4 版二進位的端到端復現**：本 phase 之證據是「同一份原始碼在 HEAD 版下違反不變式、在修後版下滿足之」，非「在舊二進位上真的把輸入法打掛」。後者需部署一份 4.7.4 並以 2,000 筆以上之 POM 競態觸發，未做。
- **兩執行緒競態之壓力測試**：見〈落點〉第二節之說明（該模組在 6.4 下為 main-actor-isolated，寫不出乾淨的競態測試）。

## 附註

- **`LXMgr_Core.swift` 之殘留註解**：`// Perform disk I/O on this background queue – avoids blocking MainActor.` 與其下已改為 `asyncOnMain { … }` 的事實相反——`60eb5f33` 只改碼未改註解。本 phase 一併改正為「POM 落盤改在主執行緒執行（非主執行緒只負責調度）…本佇列僅負責 debounce 計時」。
- **為何未動 `LXPerceptor`**：見〈落點〉第一節末。另註：`LXPerceptor` 之 `alternateKeys(for:)` 有一支 `alternateKeysForTesting` 未持鎖，屬既有狀況、與本 phase 無關，未動。
- **未 commit**：兩倉之變更留待事主 finalize。
- Swift 原始碼內未出現任何 Phase 編號。

--------------------

# Phase 225

Assignee: Deepseek-v4-flash（Kimi Code harness）。

> **狀態：完工（2026-09-17，本機實測）。** 修復 `vChewing-macOS` 之 macOS CI（run `35195546476`／job `105117792777`）在 test 步驟之最後一包 `./Packages/Jad_BookmarkManager` 上於 link 期報出的未定符號（`SwiftExtension.mainSync`）。真因不在原始碼、亦與 P224 之改動無關——是 **SwiftPM 6.4 起之預設建置引擎（Swift Build）不為 dynamic 產品傳導其動態依賴**。動工面一檔：`vChewing-macOS/Packages/Jad_BookmarkManager/Package.swift`（＋5 行；**已 commit**：`ca99c17e`，`BookmarkMgr // Add missing deps.`）；`vChewing-LibVanguard` **未動**（該套件不在該倉）。**第二輪（同日，事主追加）**：同一宣告補進該包之 `Package@swift-5.10.swift`（對位），並全倉盤點出同型之 `vChewing_FolderMonitor`／`vChewing_UpdateSputnik` 兩家、一併補齊 6.4 與 5.10 兩側——該輪動工面共 5 檔。**兩輪文書入庫（本卷）與第二輪施工尚未 commit，待事主 finalize。**

## 前導 Phase

P224（POM 持久化層之取鎖順序倒置死鎖之修復）。本 phase 即 P224 推送後（`vChewing-macOS` `b2bbbe61`）該倉 macOS workflow 之紅燈排查。

## 手術範圍

- `vChewing-macOS/Packages/Jad_BookmarkManager/Package.swift`：`dependencies` 增一條 `path:` 依賴，兩個靶各增一條 `VanguardSwiftExtension` 產品宣告（＋5 行，含一行說明註解）。
- 其餘全部未動：`vChewing-LibVanguard`（無此套件）、`vChewing-OSX-legacy`（已封存、亦不在本工作區）、四份封堵檔（`Package@swift-6.0`／`6.1`／`6.2`／`6.3`）。5.10 側之對位 manifest 於**第二輪**補（見該節）。

## 要解決的問題

**症狀**：CI 之 test 步驟先前六包皆過，跑到最後一包時 link 期報未定符號：

```
Undefined symbols for architecture arm64:
  "SwiftExtension.mainSync<A>(execute: @Swift.MainActor @Sendable () throws -> A) throws -> A", referenced from:
      BookmarkManagerTests.BookmarkManagerTests.deinit in BookmarkManagerTests.o
      BookmarkManager.BookmarkManager.deinit in BookmarkManager.o
ld: symbol(s) not found for architecture arm64
```

**真因（三條實查事實相乘）**：

1. `SwiftExtension.mainSync` 只由 `libVanguardSwiftExtension.dylib` 定義——`nm -gU libVanguard.dylib` 查無此符號。
2. `libVanguard.dylib` 對該 dylib 只有 `LC_LOAD_DYLIB`、**不是 `LC_REEXPORT_DYLIB`**（`otool -l` 實查；`LibVanguard` 之 `@_exported import SwiftExtension`（`LibVanguardSPM.swift:11`）只影響模組名查找、不生成鏈接期的 re-export）⇒ ld 不藉這條間接 dylib 解析未定符號。
3. 失敗那條 linker 命令行上，Swift 庫只有 `…/Products/Debug/libVanguard.dylib` 一顆——**Swift Build 引擎不把 dynamic 產品（`Vanguard`）之動態依賴轉給下游**；舊的 llbuild 引擎會。（對照組見〈驗證〉第一節。）

`Jad_BookmarkManager` 之 manifest 只宣告了 `Vanguard` 這一 dynamic 產品、從未宣告 `VanguardSwiftExtension`，而該套件之 `Sources/` 與 `Tests/` 都直接 `import SwiftExtension` 並呼叫 `mainSync`（`BookmarkManager.swift` 之 `deinit`、`BookmarkManagerTests.swift`）⇒ SUT 之 object 與測試 object 皆帶此引用，test bundle 卻沒有定義它的那顆庫。

**為何是現在才炸**：

- 最後一次 macOS 綠燈（run `34852818242`，09-14）用 `Xcode_26.6.app`、佈局為舊引擎之 `Packages/Jad_BookmarkManager/.build/arm64-apple-macosx`；今回紅燈（run `35195546476`）用 `Xcode_27_beta_6.app`、佈局為 `.build/out/Products/Debug`（新引擎）。`swift test --help` 現載 `--build-system` 之 default 已是 `swiftbuild`（`native` 標 deprecated）。
- `mainSync` 早在 2026-02-06（`5a744b72`，`SPM(macOS) // Use Swift 6.2 with default MainActor isolation.`）即進入 `BookmarkManager.swift` 之 `deinit`——**即「manifest 少宣告一個產品」這個缺口是既有的，只是舊引擎代為兜住**；引擎一換，這條傳導才斷。
- **為何別的套件不受影響**：`SettingsUI`／`MainAssembly4Darwin`／`CandidateWindow`／`NotifierUI` 雖然同樣沒宣告該產品，卻（直接或間接）都鏈了**有宣告它**的**靜態**產品（`IMKUtils`／`OSFrameworkImpl`）——靜態產品會把自身的動態依賴往下帶，`Vanguard` 這種 dynamic 產品不會。`vChewing_UpdateSputnik` 則是根本沒有 `Tests/` 目錄、每輪皆被 CI 靜默跳過。全倉實查下來，`Jad_BookmarkManager` 是唯一裸露者。

**排除項**：錯誤訊息裡的 `@Sendable` 不是線索——那是 `-default-isolation MainActor` 之下 `@MainActor () throws -> T` 的 mangling 形態。以最小兩模組重現驗過：定義側與引用側同形（`def`／`ref` 皆 `… mainSync<A>(execute: @Swift.MainActor @Sendable () throws -> A) throws -> A`），名字本身沒問題。

## 沿革追溯

**兩件事相乘，缺一不成紅燈**：一是「`SwiftExtension` 之符號自 `libVanguard.dylib` 內獨立出去」（dylibify，P216～P218 階段），二是「預設建置引擎翻轉為 Swift Build、不再代傳 dynamic 產品之依賴」（Swift 6.4／Xcode 27）。

| 時點 | 事件 |
|---|---|
| 2026-02-06 | `5a744b72`：`BookmarkManager.swift` 之 `deinit` 開始呼叫 `mainSync`——新引用登場（manifest 未同步補宣告） |
| 2026-09-14 | `LibVanguard // Dylibify VanguardSwiftExtension.`：`SwiftExtension` 之符號自此不在 `libVanguard.dylib` 內（該輪 CI 仍綠，見下） |
| 2026-09-14 | run `34852818242`（`Xcode_26.6.app`）：**最後一次 macOS 綠燈**——傳導尚由舊 llbuild 引擎兜住 |
| 2026-09-17 | run `35195546476`（`Xcode_27_beta_6.app`）：新引擎 ⇒ `Jad_BookmarkManager` 之 test bundle link 失敗 |
| 2026-09-17 | 本 phase 修復 |

## 落點

單檔、＋5 行——`vChewing-macOS/Packages/Jad_BookmarkManager/Package.swift`：

```diff
   dependencies: [
     .package(path: "../vChewing_OSNeutral_LibVanguard"),
+    // `Vanguard` 是 dynamic 產物：Swift Build 引擎（Swift 6.4 起之預設）不會代它把 SwiftExtension
+    // 的動態庫轉給下游，故凡直接使用 SwiftExtension 符號（含經 `@_exported` 轉出者）的靶一律自行宣告本產品。
+    .package(path: "../vChewing_OSNeutral_LibVanguard/Deps/VanguardSwiftExtension"),
   ],
   targets: [
     .target(
       name: "BookmarkManager",
       dependencies: [
         .product(name: "Vanguard", package: "vChewing_OSNeutral_LibVanguard"),
+        .product(name: "VanguardSwiftExtension", package: "VanguardSwiftExtension"),
       ],
```

測試靶同構補上同一條產品宣告。此形制與倉內三家既有者**逐字同構**（`vChewing_IMKUtils:20,43`、`vChewing_OSFrameworkImpl:16,29`、`vChewing_InstallerAssembly4Darwin:18,25`）——它們在 dylibify 當時即已如此宣告，故從未踩此坑。

**5.10 側不動**：該側 `VanguardSwiftExtension` 之產物是 `.static`（`Deps/VanguardSwiftExtension/Package@swift-5.10.swift:29`），靜態鏈接本即帶得過去，無此傳導缺口。

## 驗證（2026-09-17，本機實測）

- **重現**：`swift test --no-parallel --package-path ./Packages/Jad_BookmarkManager --scratch-path <tmp>` 於 HEAD 版重現同一未定符號與**同一行** linker 指令（`.build/out/Products/Debug/BookmarkManagerTests.xctest` 那條，逐項與 CI 相符）。
- **裁決性對照（單一變因）**：同一份原始碼改跑 `--build-system native`（舊引擎）⇒ **10 tests／1 suite 全過、rc=0**。即引擎是唯一變因，原始碼無咎。
- **修後複驗**：預設引擎下同一條命令 ⇒ **10／10 綠、rc=0**。
- 全程以 `--scratch-path` 導向倉外 tmp，未汙染各套件之 `.build`；`git status` 僅 `M Packages/Jad_BookmarkManager/Package.swift` 一項。
- 6.4 側與 5.10 側之既有建置路徑皆未受影響（本改動只增一筆既有依賴之明文宣告）。

## 第二輪（2026-09-17，事主追加：5.10 對位 manifest 與全倉盤點）

**追加一：`Jad_BookmarkManager` 之 `Package@swift-5.10.swift`。** 同型補上 `VanguardSwiftExtension` 之套件依賴與產品宣告（＋4 行、含註解）。**此側並非必需**——5.10 之該產品是 `.static`（`Deps/VanguardSwiftExtension/Package@swift-5.10.swift:29`），靜態鏈接本即帶得過去；此宣告是**拓樸對位**（免日後改動時漏一側），manifest 內之註解亦如此寫明。

**追加二：全倉盤點。** 判準＝「該靶**直接使用 SwiftExtension 符號**（含只靠 `@_exported import` 轉出者）」× 「其**靜態**依賴鏈是否抵達一個已宣告 `VanguardSwiftExtension` 者」——靜態產品會把自身動態依賴往下帶、dynamic 產品不會（機理見〈驗證〉第一節之引擎對照）。

| 套件 | 用 SE 符號 | 自宣告 VSE | 判定 |
|---|---|---|---|
| `vChewing_IMKUtils`／`vChewing_OSFrameworkImpl`／`vChewing_InstallerAssembly4Darwin` | ✓ | ✓ | 即「宣告者」本身（本即無事） |
| `vChewing_OSNeutral_LibVanguard` | ✓ | ✓（其自身 12 靶） | 無事 |
| `Jad_BookmarkManager` | ✓ | 已補 | 本 phase 主體（CI 曾紅） |
| **`vChewing_FolderMonitor`** | ✓ | **已補** | **裸露形狀**（只鏈 dynamic 的 `Vanguard`）；惟無 `Tests/`、亦無執行檔產物 ⇒ 從未有 link 步驟、故未曾顯形 |
| **`vChewing_UpdateSputnik`** | ✓ | **已補** | 同上（且 CI 因無 `Tests/` 目錄而靜默跳過） |
| `vChewing_SettingsUI` | ✓ | ✗ | 安全：靜態鏈 `OSFrameworkImpl`／`IMKUtils`（＋`BookmarkManager`）——CI 綠燈佐證 |
| `vChewing_MainAssembly4Darwin` | ✓ | ✗ | 安全：靜態鏈 `IMKUtils`——CI 綠燈佐證 |
| `vChewing_Shared_DarwinImpl` | ✓ | ✗ | 安全：靜態鏈 `IMKUtils`／`OSFrameworkImpl` |
| `vChewing_CandidateWindow` | ✓ | ✗ | 安全：**兩層**靜態鏈 `Shared_DarwinImpl`→`IMKUtils`——本機實測（見下） |
| `vChewing_NotifierUI`／`vChewing_PopupCompositionBuffer`／`vChewing_TooltipUI` | ✓ | ✗ | 安全：同 `Shared_DarwinImpl` 那條 |
| `vChewing_Uninstaller` | ✓ | ✗ | 安全：靜態鏈 `OSFrameworkImpl` |
| `vChewing_Hotenka`／`vChewing_OtherIMEDataReader`／`vChewing_ModifierKeyHitChecker`／`HangarRash_SwiftyCapsLockToggler` | ✗ | ✗ | 不涉 |

**第二輪之驗證（2026-09-17，本機實測）**：

- **5.10 側逐包編譯測試**（各包 `make build510`，Swift 5.10.1 ＋ `MacOSX13.3.sdk` ＋ `x86_64-apple-macosx10.9`）：`Jad_BookmarkManager`／`vChewing_FolderMonitor`／`vChewing_UpdateSputnik` 三者皆 **rc=0、`Build complete!`**（各出 `lib*.a`）。
- **6.4 側**：`swift build --package-path` 對 `vChewing_FolderMonitor` 與 `vChewing_UpdateSputnik` 皆 **rc=0**（`Build complete!`）。
- **「靜態鏈是否安全」之裁決性對照**：`vChewing_CandidateWindow`（**未**宣告 VSE、且與宣告者隔兩層靜態產品）跑 `swift test --no-parallel` ⇒ **20 項測試全綠、rc=0**——即靜態產品之傳導確實成立，盤點表之「安全」欄非徒託推論。
- **legacy 根建置之回歸**（本輪新增了 VSE 之依賴邊，須確認根套件之 link 不因此生重複或未定符號）：倉根 `make debugLegacy` 全程 **rc=0、`✅ Done.`**，兩份 `.app` 照常組出（`Build/Products/Legacy/`），內嵌執行期之清單與 minOS 10.9 標註不變。

**盤點之附帶結論**：全倉 19 個 subpackage 中，**除本 phase 已修之三者外，無第四個會當場失敗者**——`FolderMonitor`／`UpdateSputnik` 同屬「裸露形狀」卻不產出可 link 之物（無測試靶、無執行檔），故此類缺口只在「日後為其新增測試靶」時才會顯形。其餘八家之安全性**寫在鄰居的 manifest 內**（不在自己的），移除任一家的產品宣告都會連帶打斷這條鏈——此即 KM 附錄二 A2.2 第 8 條所載之通則。

## 未驗

- **未重跑 CI**：本 phase 止於本機複驗（含裁決性對照）；止紅須再推一次 CI 方能確認。
- **未重跑 `make archive`**：該步在本 run 本即通過（失敗者唯 test 步驟），且本改動只是把一條本已在場的 dylib 補進 `Jad_BookmarkManager` 之 link 行。
- **未逐一實跑每一個套件之測試靶**：第一輪由 CI 代跑了七家（前六家皆過）；第二輪補跑了 `vChewing_CandidateWindow` 一家（用以驗「靜態鏈安全」）；其餘未跑者仍屬推論（機制與已驗者相同）。

## 附註

- **CI workflow 之兩處小瑕疵（本輪順帶發現，未動）**：① `Run Package Tests (Selected)` 之 `code=$?` 是**死碼**——該 step 為 `bash -e`，`swift test` 一失敗整個 step 即中止，故「`code == 1` ⇒ 當作沒測試、continue」那條分支永遠走不到；② `packages=(…)` 內之 `./Packages/vChewing_UpdateSputnik` 沒有 `Tests/` 目錄，每輪皆被靜默跳過（該包之測試靶並不存在）。
- **通則（今後之施工注意事項）**：凡直接使用 SwiftExtension 符號（**含經 `@_exported import SwiftExtension` 轉出者**）之靶，其 manifest 必須**自行宣告** `VanguardSwiftExtension` 產品，或至少鏈一個「已宣告它」的**靜態**產品；只鏈 `Vanguard` 這一 dynamic 產品不足。舊引擎代為兜住、故 6.3 世代不顯此坑。
- 本 phase 未動任何 Swift 原始碼，故「Swift 原始碼內不得出現 Phase 編號」一條無從觸犯。

--------------------

# Phase 226

Assignee: Deepseek-v4-flash（Codewhale harness）。

> **狀態：完工（2026-09-17，本機實測 ＋ 事主實機複驗）。** 修復「就地加詞之後無法立刻在當前的 Homa 組字器實例內生效」。正本為 `vChewing-LibVanguard`（3 檔原始碼 ＋ 2 檔測試），`vChewing-macOS` 之巢狀副本逐位元組同步（同五檔）。**已 commit**：`vChewing-LibVanguard` `c560ba4`、`vChewing-macOS` `15bcd292`（同訊息 `LibVanguard // Fix the new-userphase-hotload failure.`，各 5 檔 ＋223／−32）；本卷文書入庫為 `vChewing-DevLogs` `eec2495`（`P226.`）。**實機測試已確認該故障成功修復**（事主複驗）。`vChewing-OSX-legacy` 未動（已封存、亦不在本工作區）。

## 前導 Phase

P88（就地加詞脫手化：`writeUserPhrasesAtOnce` 尾端之資料重載改 `async: true`——自此「寫檔後的就地重載」不再於同一輪主執行緒內完成）與 P46〈補遺〉（其修者正是「`insertTemporaryData` 等資料變更入口未同步使 LRU 快取失效」）。事主之補充定調：「就地加詞在寫入硬碟之前的生效機制是 userphrase lexicon 的『臨時資料插入』機制。」本 phase 即令該機制在當前組字器內真正生效。

## 手術範圍

- `vChewing-LibVanguard`（先行，5 檔）：`Sources/LibVanguard/Session/InputSession_Delegates.swift`、`Sources/LexiconAssembly/LXFacade.swift`、`Sources/LexiconAssembly/vChewingLXAssembly_Common.swift`、`Tests/LibVanguardTests/SessionTests_Cases5.swift`、`Tests/LexiconAssemblyTests/LXFacadeTests.swift`
- `vChewing-macOS`：前列五檔於 `Packages/vChewing_OSNeutral_LibVanguard/` 之對位（逐位元組同步；五對 md5 全同）。
- `vChewing-OSX-legacy`：未動。

## 要解決的問題

事主原文：「就地加詞之後得立刻在當前的 homa assembler instance 內生效。該故障在 4.8.0 復發。」

就地加詞（marking state 之 Enter／Shift+Command+Enter／Backspace）之即時生效鏈路為：寫檔之後，靠「臨時資料插入」（`LXFacade.insertTemporaryData`）令新詞立刻可被查得；再以 `InputHandler.updateUnigramData()`（`assembler.assignNodes(updateBehavior: .refreshExisting)`：清空組字器查詢快取、就地重查所有既有節點）把該變更灌進**當前**組字器。三層缺陷令該鏈路斷裂，三者皆先以探針釘死（見〈驗證〉之紅綠雙驗）：

**（一）次序倒置**：`SessionProtocol.performUserPhraseOperation` 先 `updateUnigramData()`、後 `insertTemporaryData`——熱重載跑在臨時資料入庫之前，本次重查的結果自然不含剛寫入的詞。對照：選字窗右鍵選單路徑 `candidatePairManipulated` 之次序本即正確（先插臨時資料、後熱重載），故該路徑之既有測試從未顯病。

**（二）快取失效不全**：`insertTemporaryData` 只清 `keyChain\tPM0`／`PM1` 兩枚快取鍵；替代讀音路徑（`[PossibleKey]` 含 `.multipleKeys`，如拼音無調、狂拼桶）另以 `ALT\t<鍵陣列雜湊值>` 定址，該條目不受波及——同一讀音若在插入前已被查過，插入後之重查仍拿到過時結果。此與 P46〈補遺〉屬同一故障類別（資料變更入口未使查詢快取失效）在新增查詢路徑上的復發。

**（三）非同步重載之空窗期**：`LXFacade.loadUserPhrasesData` 於非同步路徑之入口即同步執行 `lxUserPhrases.clear()`（且無論 `filterPath` 是否為 nil，一律連 `lxFiltered.clear()` 一併執行），而重載本身非同步——「舊資料已清空、新資料尚未讀入」之空窗期若被就地加詞觸發的熱重載撞上，組字器會被重建成**既沒有使用者詞語（除臨時資料外）、濾除表亦被清空**的狀態。生產環境（`writeUserPhrasesAtOnce` → `loadUserPhrasesData(async: true)`）恆落此窗。

## 落點

### 一、次序：先插臨時資料、後熱重載（`InputSession_Delegates.swift`）

`performUserPhraseOperation` 內兩塊互調，並於熱重載處補上「次序不得顛倒」之註記：

```swift
    // 因為上述操作要等語言模組資料重載完成才會生效（除非遞交組字區），
    // 所以先暫時塞入臨時資料記錄，令當前組字器立刻用得上剛寫入的內容。
    inputHandler.currentLM.insertTemporaryData(…)

    // 更新組字器內的單元圖資料。……
    // 次序不得顛倒：該更新會清空組字器的查詢快取、並就地重查所有既有節點，
    // 臨時資料記錄必須先入庫，否則本次重查的結果不含剛寫入的詞、也就無從立刻生效。
    _ = inputHandler.updateUnigramData()
```

### 二、臨時資料插入改為全量失效查詢快取（`LXFacade.insertTemporaryData`）

原 `unigramLRUCache.removeValue(forKey: "\(keyChain)\tPM0")` ＋ `PM1` 兩行者，改為 `unigramLRUCache.removeAll(keepingCapacity: true)`。理由寫入註解：`ALT\t…` 條目無法以 keyChain 枚舉，而臨時資料變更屬人肉操作頻率、全量作廢之代價可忽略。

### 三、重載之清除時序（`LXFacade.loadUserPhrasesData` ＋ `vChewingLXAssembly_Common.swift`）

- `loadUserPhrasesData`：清除動作僅在**同步路徑**逕行（且「只清本次將重新載入者」——`filterPath` 為 nil 時不動 `lxFiltered`）；非同步路徑之清除延後——成功時由 `replaceData` 全量取代（臨時資料亦於該處一併清除）、失敗時由新增之失敗回呼清除。新資料落地之際另即時使 LRU 快取失效（空窗期內產生之條目不得殘留）。
- `readFileContentAsync`：新增可選之 `onFailure` 參數（`#if compiler(>=6.2)` 與 `#else` 兩支同構、預設 nil，既有五處呼叫端零改動），讀檔失敗時於 MainActor 回呼之——藉此保住「無論新檔案是否可讀，都必須先清除舊資料」之原語義。目錄切換情境之殘留防護本即由 `purgeUserData()` 於呼叫端（`LXMgr.initUserLexicons()`）負責，不受本改動影響。

### 四、回歸測試（3 支）

- `test515_InPlaceUserPhraseOperationsApplyToCurrentAssemblerImmediately`（`SessionTests_Cases5`；真 `InputSession` ＋ 真 `InputHandler`）：以較低權重預置「年中」後敲 `su065j/ `，進入 marking state 走完三路——① 升權（Enter）後當前節點內「年中」權重立刻為 `0`；② 降權（Shift+Command+Enter）後立刻為 `-114.514` 且組句立刻改用「年終」；③ 過濾（Backspace）後「年中」立刻自節點內除名、組句改用「年終」。三者皆**未遞交組字區**。
- `testTemporaryDataInsertionInvalidatesAllQueryCaches`（`LXFacadeTests`）：單鍵路徑與替代讀音路徑各先查一輪坐實快取，插入臨時資料後二者皆須立刻查到新詞。
- `testAsyncUserPhrasesReloadKeepsStoreUntilReplacementLands`（`LXFacadeTests`）：非同步重載之空窗期內既有片語與濾除表皆須留著；同步載入時則照舊清空（語義保留）。

## 驗證（2026-09-17，本機實測 ＋ 事主實機複驗）

**實機複驗（2026-09-17，事主）**：實機測試確認該故障已成功修復——就地加詞之後於當前組字器內即時生效。

**紅綠雙驗（本 phase 之關鍵證據）**：把三支原始碼改動暫時收起（`git stash push -- Sources/…`）後跑三支新測試——`test515` 於五處斷言失敗（升權權重、降權權重、降權後組句、過濾除名、過濾後組句）、`testTemporaryDataInsertionInvalidatesAllQueryCaches` 於替代讀音路徑失敗 1 筆（單鍵路徑本即無病）、`testAsyncUserPhrasesReloadKeepsStoreUntilReplacementLands` 失敗 2 筆（空窗期內片語被清、濾除表亦被清）；恢復修復後三支全綠。

- **`vChewing-LibVanguard` 全靶 release 測試**：9 次靶級測試、**544 支全過**（18／37／7／6／207／198／61／3／7），零失敗、零 `error:`；格式管線之後復跑一次，仍全綠。
- **`vChewing-LibVanguard` 之 5.10 側**：`make build510` **rc=0、`Build complete!`**、0 筆 `error:`（涵蓋本次改動之 `#if compiler(>=6.2)` `#else` 分支）。
- **`vChewing-macOS` 之巢狀套件**（逐位元組同步後）：`swift test --no-parallel` **544 支全過**（同上九靶）；`make build510` **rc=0、`Build complete!`**。
- **`vChewing-macOS` 之宿主整合靶**：`Packages/vChewing_MainAssembly4Darwin` 之 `swift test --no-parallel` ⇒ **28 項全過**（4 suites）、rc=0——該靶直接驅動真 Session 與宿主接線，為本改動在 macOS 側之整合層複驗。
- **端到端組裝（現代＋legacy 兩路）**：`vChewing-macOS` 之 `make debug`（`bundle-apps --debug`）全程 **rc=0、`✅ Done.`**，兩份 app bundle 照常組出；`make debugLegacy`（5.10 側）亦 **rc=0、`✅ Done.`**、0 筆 `error:`（僅既有之「dependency … left to the system runtime」提示）。
- **格式管線**：兩倉 `make lintFormatUncommitted` 皆 **rc=0**；格式之後兩倉五檔 md5 逐一同值——`LXFacade.swift` `4095c685…`、`vChewingLXAssembly_Common.swift` `fdab5fb4…`、`InputSession_Delegates.swift` `15b8d07e…`、`LXFacadeTests.swift` `d68eaf30…`、`SessionTests_Cases5.swift` `7456d841…`。
- **施工前之病灶探針**：三支病灶探針（組字器層、快取層、重載層各一）在改前全紅、改後全綠，作為病灶定位之證據；探針檔已於交差前刪除，其斷言由上述永久測試承接。

## 未驗

- **`onFailure` 之非同步觸發**：其時序為「fileHandleQueue → MainActor 任務」，單元測試無法確定性等待，故僅鎖定**同步路徑**之清除語義（見第三支測試）；非同步失敗之清除屬推理。
- **Linux／WinNT CI**：本 phase 止於本機（macOS）複驗；跨平台須推送後由 CI 判定。

## 附註

- **為何採「延後清除」而非「即時重載」**：P88 之脫手化是硬需求（防止 ~1 秒 MainActor 卡頓），故即時生效只能靠臨時資料；本 phase 未改動非同步重載之策略，只改「清除時機」與「快取失效範圍」。
- **沿革**：次序倒置自 `26252e14`（2022-12-01，`SessionCtl // Do currentLM.insertTemporaryData() when necessary.`）起即如此——惟當時寫檔後的重載尚為同步，熱重載看得到新詞、缺陷被掩蓋；P88（macOS `20205f73`，2026-05-26）把該重載改非同步後，臨時資料成為唯一橋樑，次序缺陷遂顯。替代讀音快取（`ALT\t…`）自 `564ce05d`（2026-06-07，首見於發行版 4.5.3 起）引入時即漏了 `insertTemporaryData` 之失效——即 P46〈補遺〉所修故障類別之復發。**本 phase 未能指認單一 4.8.0 引入點**；可確認者為：三者疊加，至 4.8.0（動工基底 `vChewing-LibVanguard` `8319e25`／`vChewing-macOS` `690a5fb2`）仍復現，且已由上述紅綠雙驗釘死。
- **未動之相鄰物**：`Homa`（組字器）零改動——`assignNodes(.refreshExisting)` 本即清空自身查詢快取並重查，問題全在上游的資料可見性；`LXPerceptor`／POM 未動；`candidatePairManipulated` 之既有次序（本即正確）未動；macOS 宿主側（`LXMgr_Utilities.swift` 之 `writeUserPhrasesAtOnce` 與其註解）經實查無需改動——`make debug` 與巢狀套件全量測試為證。
- Swift 原始碼內未出現任何 Phase 編號。

--------------------

# Phase 227

Assignee: Deepseek-v4-flash（Codewhale harness）。

> **狀態：完工（2026-09-18，本機實測）。** 中英混打（mixedAlnum）模式新增按鍵語意：**Option+BkSp 一次清空「尚待辨識的英文緩衝區」（混輸暫存 ASCII 緩衝區 `mixedAlphanumericalBuffer`）之全部內容**。正本為 `vChewing-LibVanguard`（1 檔原始碼 ＋ 1 檔測試），`vChewing-macOS` 之巢狀副本逐位元組同步（同兩檔）；`vChewing-OSX-legacy` 未動（已封存、亦不在本工作區）。**未 commit（待事主 finalize）。**

## 前導 Phase

P221（混輸 Tooltip 之第二行讀音預覽——同一枚 `mixedAlphanumericalBuffer` 在 Tooltip 首行之呈現）與 P53（Typewriter 層中英文混輸——`composer`（注音）＋ `mixedAlphanumericalBuffer`（ASCII）雙軌模型之立項）。按鍵語意之對照組則在碼內既有、非屬特定 phase：`handleBackSpace` 之 codePoint／romanNumerals 兩支。

## 手術範圍

- `vChewing-LibVanguard`（先行，2 檔）：`Sources/LibVanguard/InputHandler/InputHandler_HandleStates.swift`、`Tests/LibVanguardTests/InputHandlerTests_Cases4.swift`
- `vChewing-macOS`：前列兩檔於 `Packages/vChewing_OSNeutral_LibVanguard/` 之對位（逐位元組同步；兩對 md5 全同）。
- `vChewing-OSX-legacy`：未動。macOS 倉除鏡像外**零額外改動**（查核見〈附註〉）。

## 要解決的問題

事主原文：「MixedAlnum 模式新增特性：以「Option+BkSp」刪掉「尚待辨識的英文 buffer 的全部內容」。」

混輸模式下，使用者敲下的 ASCII 原文恆先暫存於 `mixedAlphanumericalBuffer`——該段內容尚「待辨識」：可能經 auto-split 被改判為注音前綴、亦可能整段作 ASCII 遞交。改前之行為（HEAD 探針實證）：`handleBackSpace(input:)` 開頭之混輸分支**無條件** `dropLast()`、根本不讀修飾鍵，故 Option+BkSp 與普通 BkSp 是同一個手勢——整段英文緩衝區只能逐字元刪。

**「原因」之定位（正本倉內）**：同函式內處理其餘兩支暫存緩衝區者皆有「整段作廢」語義——codePoint／romanNumerals 分支以 `input.commonKeyModifierFlags == .option` 把整個 `strCodePointBuffer` 清空（經 `revolveTypingMethod(to:)` 之 switch 內 `strCodePointBuffer.removeAll()`），前向刪除（`handleDelete`）於混輸緩衝區非空時亦整段清空（`isComposerOrCalligrapherEmpty == false` ⇒ `clearComposerAndCalligrapher()`）。唯獨混輸分支漏了這枚手勢；本 phase 即補上該缺口，使「BkSp 側的整段作廢」與兩個鄰居對齊。

## 落點

### 一、`handleBackSpace` 之混輸分支（`InputHandler_HandleStates.swift`）

```swift
    if currentTypingMethod == .vChewingFactory, !mixedAlphanumericalBuffer.isEmpty {
      // 混輸 ASCII 緩衝區的內容皆屬「尚待辨識」（尚未定案為英文抑或注音）：
      // 不帶 Option 者僅刪除最末字元；帶 Option 者（Option+BkSp）一次清空整段內容——
      // 此與內碼／數字模式之 Option+BkSp 語義（清空整個緩衝區）對齊。
      if input.commonKeyModifierFlags == .option {
        mixedAlphanumericalBuffer.removeAll()
      } else {
        mixedAlphanumericalBuffer = mixedAlphanumericalBuffer.dropLast().description
      }
      syncComposerWithMixedAlphanumericalBuffer()
      switch isConsideredEmptyForNow {
      case false: session.switchState(generateStateOfInputting())
      case true: session.switchState(State.ofAbortion())
      }
      return true
    }
```

三點設計說明：

- **修飾鍵判準為「恰為 Option」**（`commonKeyModifierFlags == .option`）——與同函式 codePoint／romanNumerals 兩支逐字一致；`commonKeyModifierFlags` 已剔除 capsLock／function／numericPad／help。故 Shift+Option+BkSp、Cmd+Option+BkSp 等更複雜組合不受本特性影響（行為與改前相同，仍為刪末字元）。
- **清空後續走既有收尾**：`syncComposerWithMixedAlphanumericalBuffer()`（緩衝區為空 ⇒ 注拼槽同清）與 `isConsideredEmptyForNow` 之狀態收尾（組字區尚有中文 ⇒ `generateStateOfInputting()` 續留 inputting；全部皆空 ⇒ `State.ofAbortion()` → session 端歸為 Empty 並 `clear()`）——此段一行未動，與「刪末字元」路徑共用。
- **組字區已組好的中文不受波及**：本特性刪的是「尚待辨識的英文緩衝區」、不是組字內容；選此語義亦即不把 Option+BkSp 做成第二個 Esc。

### 二、回歸測試（`test_IH436_MixedOptionBackspaceClearsWholeBuffer`）

三路斷言（`InputHandlerTests_Cases4`，沿用該檔既有之 `prepareMixedModeHandler()`／`injectTemporaryGrams(...)`／`typeSentence(...)` 基座）：

1. **組字區為空、緩衝區為 `abc`** → Option+BkSp ⇒ 緩衝區與注拼槽皆空、狀態歸 Empty。
2. **對照組：不帶 Option 的 BkSp** ⇒ 僅刪最末字元（`abc` → `ab`）——既有行為不得回歸。
3. **組字區已有中文**（注音 `ㄧㄡ`→「優」）＋緩衝區為 `gr` → Option+BkSp ⇒ 緩衝區清空、注拼槽清空、**組字區之 `ㄧㄡ` 原樣留存**、狀態續為 inputting、`committableDisplayText(sansReading: true)` 仍為「優」、混輸 Tooltip 一併消失（Tooltip 之混輸區塊本以「緩衝區非空」為前提，緩衝區清空即自然隱沒）。

## 驗證（2026-09-18，本機實測）

**施工前之探針**（暫存測試檔已於交差前刪除）：混輸模式下 `triageInput(optionBackspaceEvent)`——緩衝區 `abc` 得 `ab`；「組字區＝〔ㄧㄡ〕＋緩衝區＝`gr`」得緩衝區 `g`。即 Option 完全未獲理會、與普通 BkSp 無異，此為病灶之直接實證。

**紅綠雙驗（本 phase 之關鍵證據）**：把原始碼改動暫時收起（`git checkout -- Sources/…`、其後自暫存副本復原）後跑 `test_IH436`——於 HEAD 版為**紅**：恰 5 筆失敗，全數落在新行為之斷言（路徑一之「緩衝區未清空」「注拼槽未同清」「狀態未歸 Empty」三筆＋路徑三之「緩衝區未清空」「Tooltip 未消失」兩筆），而**對照組（普通 BkSp 只刪末字元）與其餘斷言全過**——證明失敗確由新特性之缺席所致、而非測試自身有誤。換上修後版本則**全綠**。

**全量測試**：

- `vChewing-LibVanguard` 之 `swift test -c release --no-parallel` ⇒ **9 靶 545 支全過**（18／37／7／6／208／198／61／3／7；其中 `LibVanguardTests` 由 207→208，即本 phase 之新測項），零失敗、`EXIT=0`。
- `vChewing-macOS` 之巢狀套件（逐位元組同步後）`swift test -c release --no-parallel` ⇒ **9 靶 545 支全過**（同上）。

**`vChewing-macOS` 側之整合與端到端**：

- 宿主整合靶 `Packages/vChewing_MainAssembly4Darwin`：`swift test --no-parallel` ⇒ **28 項（4 suites）全過**、rc=0。
- `make debug`（`bundle-apps --debug`）：全程 rc=0、`✓ Build succeeded.`，`vChewing.app` 與 `vChewingInstaller.app` 兩份 bundle 照常組出。
- `make debugLegacy`（5.10 側）：全程 rc=0、`✅ Done.`、兩份 legacy bundle 組出；全 log **0 筆 `error:`**（僅既有之「dependency … left to the system runtime」提示）。

**5.10 側編譯**：`vChewing-LibVanguard` 之 `make build510` 與 macOS 側巢狀套件之 `make build510` 皆 rc=0、`Build complete!`。

**格式管線**：兩倉 `make lintFormatUncommitted` 皆 **rc=0**；連跑後受改檔之 md5 逐次不變（穩定）；SwiftLint 之 `trailing_comma`／`void_return` 修正與 SwiftFormat 之重排之拉鋸為本倉已知現象（P216／P224 已記），最終結果即兩者順序運行後之定態。

**兩倉 byte-sync**：受改兩檔於 LibVanguard 與 macOS 逐一同值——`InputHandler_HandleStates.swift` `090dab2d7d4636f93c67cd6108d6a860`、`InputHandlerTests_Cases4.swift` `389ab9c3cbd1dc5096eedd5aecf43328`；`diff -q` 無差異。

## 未驗

- **真機 GUI 實按**：未在實際 IMK 客體中按 Option+BkSp 目視——本 phase 之驗證止於 LibVanguard 之 `triageInput` 層與 macOS 側之組裝／宿主整合靶。
- **CI（linux／darwin／WinNT）**：本 phase 止於本機複驗；跨平台須推送後由 CI 判定。

## 附註

- **未動之相鄰物**：`handleBackSpace` 之其餘分支、`handleDelete`、`Esc` 路徑、`MixedAlphanumericalTypewriter`（BkSp 本即不經 Typewriter 路由——`triageInput` 之 `.kBackSpace` 直接呼叫 `handleBackSpace`）皆一行未動。另記（讀碼所見、未另立測項）：`handleDelete`（前向刪除）於混輸緩衝區非空時本即整段清空，本 phase 未動它。
- **macOS 倉除鏡像外零額外改動之查核**：`handleBackSpace` 之實作唯存於巢狀 LibVanguard 套件（全倉實查）；`Sources/`、`LegacyZone/`、`Plugins/` 與其餘 subpackage 之既有碼皆無 BkSp／混輸處理之第二份；混輸偏好之設定介面（`VwrSettingsPaneBehavior`／`VwrSettingsPaneCocoaBehavior`）係對 `UserDef` 中繼資料自動繪製，其描述文未列舉按鍵行為——無使用者可見文字新增，故未動任何 `.strings`、未動任何偏好（本特性無開關）。
- **未 commit**：兩倉之變更留待事主 finalize。
- Swift 原始碼內未出現任何 Phase 編號。

--------------------

# Phase 228

Assignee: Deepseek-v4-flash（Codewhale harness）。

> **狀態：完工（2026-09-18，本機實測）。** 新增偏好 `SuppressTooltipForIntonationKeyOverrideEvents`（Swift 側 `kSuppressTooltipForIntonationKeyOverrideEvents`）：開啟後，以聲調鍵覆寫游標身後的漢字的音調時，不再顯示「已覆寫游標身後的漢字的音調」內文提示（預設關＝照常提示＝舊行為）。正本為 `vChewing-LibVanguard`（4 檔原始碼 ＋ 1 檔測試），`vChewing-macOS` 之巢狀副本逐位元組同步（同五檔）＋ 設定介面 2 檔 ＋ 四語系 `.strings` 4 檔；`vChewing-OSX-legacy` 未動（已封存、亦不在本工作區）。**未 commit（待事主 finalize）。**

## 前導 Phase

Issue `vChewing/vChewing-macOS#615`（標籤 Q&A / DISCUSSION、milestone 4.8.1；工單樓主為 kckhchen；事主於該串裁示「此乃合理之訴求。先備忘。可能 4.8.1 版會引入這個特性，看具體情況」）。原始碼面之前導為 **Phase 49**（後置聲調覆寫路徑補朗讀——同一 `.success` 分支的既有近鄰；本 phase 即在該分支上再加一道偏好閘門）。文書與施工體例之直接模板為 **P222**（同卷；「新增 Bool 偏好 ＋ 雙設定介面 ＋ 四語系 i18n」之完整形制；其落點在「(Shift+)空格鍵」區塊，與本 phase 之「聲調鍵」區塊不同，但逐項照抄其形制）。

## 手術範圍

- `vChewing-LibVanguard`（先行，5 檔、＋66／−3）：`Sources/Shared/UserDef/UserDef.swift`、`Sources/Shared/Protocols/PrefMgrProtocol.swift`、`Sources/Shared/PrefMgr_Core.swift`、`Sources/LibVanguard/Typewriter/Typewriter_BPMFFullMatch.swift`、`Tests/LibVanguardTests/InputHandlerTests_Cases5.swift`
- `vChewing-macOS`（11 檔、＋79／−3）：前列五檔於 `Packages/vChewing_OSNeutral_LibVanguard/` 之對位（逐位元組同步）＋ `Packages/vChewing_SettingsUI/Sources/SettingsUI/SettingsUI/VwrSettingsPaneBehavior.swift`（SwiftUI）＋ `…/SettingsCocoa/VwrSettingsPaneCocoaBehavior.swift`（Cocoa）＋ `Sources/vChewingIME_macOS/Resources/{en,ja,zh-Hans,zh-Hant}.lproj/Localizable.strings`
- `vChewing-OSX-legacy`：未動。

## 要解決的問題

工單樓主原文（issue #615 之〈痛點〉）：「已經使用唯音輸入法一段時間，非常享受唯音帶來的效率提升，不過由於我打開了「嘗試對游標正後方的字音覆寫聲調，且重設前選字狀態」功能，每次打字無論是意外或是有意改變前字聲調時，總會看到內文提示 tooltip 顯示「已覆寫游標身後的漢字的音調」。即便不影響功能性使用，頻繁提醒有時卻會打斷工作心流。願意進入設定打開此選項的人應該已經 explicitly 了解並同意此種聲調修正行為，無須另外提供內文提示。」（工單樓主之〈功能說明〉另提議「或可只給小型聲音提示」——該維度未採納為本 phase 範圍，理由見〈附註〉。）

**病灶定位（LibVanguard 倉內全倉實查）**：`i18n:StateOfInputting.Tooltip.PreviousIntonationOverridden` 之生成處全倉**唯有一處**——`Typewriter_BPMFFullMatch.swift` 之 `performRearIntonationOverrideIfNeeded(_:inputText:)` 的 `.success` 分支（改前為第 526～528 行）。該三行**無條件**把提示字串、`tooltipDuration = 2`、`tooltipColorState = .normal` 寫進 `refreshedState` 之後才 `session.switchState(refreshedState)`。而該路徑之准入條件（注拼槽為空 ＋ 輸入鍵確為聲調鍵 ＋ 存在前一字讀音快照 ＋ 覆寫請求可生成）正與偏好 `kSpecifyIntonationKeyBehavior` 之 0／1 兩值語意對應——即：**有偏好可決定整條覆寫行為是否啟用，卻無任何偏好可單獨管這條提示**。此即缺口。

附帶查核（供劃界）：同檔 `handleStandaloneIntonation` 之 `IntonationMarkInstruction` 提示屬「獨立聲調鍵」的另一事件（提示「ENTER 遞交／SPACE 插入」之行為說明），**非**覆寫事件，故不在本偏好管轄範圍、一行未動。

## 落點

### 一、新偏好 `kSuppressTooltipForIntonationKeyOverrideEvents`（三處一體，缺一即編不過）

| 檔案 | 追加內容 |
|---|---|
| `UserDef.swift` | 列舉項 `kSuppressTooltipForIntonationKeyOverrideEvents = "SuppressTooltipForIntonationKeyOverrideEvents"`（緊隨 `kSpecifyIntonationKeyBehavior`，第 65 行）；`dataType` 之 `.bool(false)`（**預設關＝照常顯示提示＝舊行為**，第 489 行）；`metaData` 之 `shortTitle` ＋ `description`（第 823 行起） |
| `PrefMgrProtocol.swift` | `var suppressTooltipForIntonationKeyOverrideEvents: Bool { get set }`（第 99 行，緊隨 `specifyIntonationKeyBehavior`） |
| `PrefMgr_Core.swift` | 對位之 `@AppProperty(userDef:)` 屬性（第 282 行起） |

`metaData` **不帶 `options`**——`Bool` 型一旦無 `options`，SwiftUI 側（`UserDefRenderableImpl` 之 `_UDAutoViewBool` ⇒ `Toggle`）與 Cocoa 側（`UserDefRenderableCocoa` 之 `.bool where optionsLocalized.isEmpty` ⇒ `NSSwitch`／`NSButton(.switch)`）即自動繪為開關勾選項，與工單樓主所援引之「以 Shift 切換輸入法/中英模式時顯示通知」同款，**無須為本項另寫任何控件代碼**。`validNumeralValueRange` 只服務 Int 型故未動；本項為全新鍵、無舊鍵對位，故亦未動 `fixOddPreferencesCore()`。

### 二、提示之閘門（`Typewriter_BPMFFullMatch.swift`，第 526～531 行）

```swift
      var refreshedState = handler.generateStateOfInputting()
      refreshedState.textToCommit = textToCommit
      // 是否顯示「已覆寫游標身後的漢字的音調」提示由偏好決定；抑制時不留殘餘的提示與其色彩狀態。
      if !handler.prefs.suppressTooltipForIntonationKeyOverrideEvents {
        refreshedState.tooltip = "i18n:StateOfInputting.Tooltip.PreviousIntonationOverridden".i18n
        refreshedState.tooltipDuration = 2
        refreshedState.data.tooltipColorState = .normal
      }
      session.switchState(refreshedState)
```

三點設計說明：

- **閘門只包「提示」三行**：`textToCommit`、朗讀（`narrateTheComposer`，Phase 49）與 `session.switchState`（組字區更新——聲調覆寫之可見結果）皆在閘門外——覆寫行為本身與提示徹底分離，抑制提示不影響功能。
- **三行同進同退**：`tooltipDuration` 與 `tooltipColorState` 留在閘門內，以免「不顯示提示」時仍留 `2` 秒之殘值；「無提示」即空字串，session 端 `InputSession_HandleDisplay.swift` 之 `setTooltip` 對空字串一律 `tooltipUI?.hide()`（既有行為），故不開窗。
- **不越界**：`generateStateOfInputting()` 之其餘 tooltip 來源（混輸 ASCII 緩衝區、狂拼暫存拼音）不受本偏好觸碰。

### 三、設定介面與 i18n（只落於 `vChewing-macOS`）

- `VwrSettingsPaneBehavior.swift`（SwiftUI，第 73 行）：`UserDef.kSuppressTooltipForIntonationKeyOverrideEvents.renderUI()` 置於 `kSpecifyIntonationKeyBehavior.renderUI()` **正下方**（同 Section）。
- `VwrSettingsPaneCocoaBehavior.swift`（Cocoa，第 138 行起）：`renderCocoa(fixWidth:prefUITab:)` 置於 `kSpecifyIntonationKeyBehavior.renderCocoa(…)` **正下方**（同 `NSStackView.buildSection`）。
- 四語系 `.strings`：各檔新增兩鍵（第 543～544 行；依 `LocalizableFileSorter` 之排序落於 `kSuppressFactoryUnigramsOfKanaSyllables` 與 `kSymbolInputEnabled` 之間）：
  - `…shortTitle`——zh-Hant：「停用「已覆寫游標身後的漢字的音調」內文提示」；zh-Hans：「停用「已覆写游标身后的汉字的音调」内文提示」；en："Suppress the tooltip when overriding the previous reading's tone"；ja：「後ろ側の漢字の音調を書き直した時のツールチップを表示しない」。
  - `…description`——zh-Hant：「啟用後，以聲調鍵覆寫游標身後的漢字的音調時，將不再顯示內文提示。若您覺得該提示干擾工作心流，可考慮開啟這個選項。」（其餘三語系同構，見檔案）。

### 四、回歸測試（`test_IH511_RearIntonationOverrideTooltipSuppression`，`InputHandlerTests_Cases5.swift` 第 142 行起）

兩路斷言（同一測試內先後為之，基座沿用同檔 IH503 之一套——臨時語料「ㄋㄧˇ→你測」「ㄋㄧˋ→逆測」＋拼音輸入 `ni3` 後以聲調鍵 `4` 觸發覆寫）：

1. **預設（偏好關閉）**：覆寫成功後 `testSession.state.tooltip == "i18n:StateOfInputting.Tooltip.PreviousIntonationOverridden".i18n`、`tooltipDuration == 2`——既有行為釘死。
2. **偏好開啟**：覆寫**照常生效**（`assembler.actualKeys.last` 之讀音確實被改寫）但 `testSession.state.tooltip.isEmpty`——抑制生效且不傷功能。

## 驗證（2026-09-18，本機實測）

**紅綠雙驗（本 phase 之關鍵證據）**：把 Typewriter 閘門暫時收起（`git stash push -- Sources/LibVanguard/Typewriter/Typewriter_BPMFFullMatch.swift`）後跑 `test_IH511`——於「偏好開啟」段之 `testSession.state.tooltip.isEmpty` **恰紅 1 筆**（該處得值恰為 `i18n:StateOfInputting.Tooltip.PreviousIntonationOverridden`，即殘餘之既有提示），而「偏好關閉」段之兩筆斷言與「覆寫照常生效」斷言全過——證明紅點確由閘門之缺席所致、非測試自身有誤。恢復閘門後全綠。

**全量測試**：

- `vChewing-LibVanguard`：`swift test -c release --no-parallel --disable-sandbox` ⇒ **9 靶 546 支全過**（18／37／7／6／209／198／61／3／7；其中 `LibVanguardTests` 由 208→209，即本 phase 之新測項），零失敗、零 `error:`、`EXIT=0`。
- `vChewing-macOS` 之巢狀套件（逐位元組同步後）：同命令 ⇒ **9 靶 546 支全過**（同上九個數字）。
- macOS 宿主整合靶：`Packages/vChewing_MainAssembly4Darwin` 之 `swift test --no-parallel --disable-sandbox` ⇒ **28 項（4 suites）全過**、`EXIT=0`。
- `Packages/vChewing_SettingsUI` 之 `swift test --no-parallel --disable-sandbox` ⇒ **1 項全過**（`testSettingsWindowDoesNotLeak`）——該測試實際開／關 SwiftUI 設定窗（macOS 14+），故設定面板（含本 phase 之新控件行）在真視窗組裝路徑上被執行過。

**端到端組裝**：

- `vChewing-macOS` 之 `make debug`（`bundle-apps --debug`）⇒ `EXIT=0`、`✅ Done.`、0 筆 `error:`，兩份 bundle 照常組出。
- `make debugLegacy`（5.10 側）⇒ `EXIT=0`、`✅ Done.`、0 筆 `error:`（僅既有之「dependency … left to the system runtime」提示）。
- **已實查**：兩份 bundle 之四語系 `Localizable.strings` 均含新鍵（`Build/Products/Debug/vChewing.app/Contents/Resources/zh-Hant.lproj/Localizable.strings:543-544`；`Build/Products/Legacy/vChewing.app/…` 同）。

**5.10 側編譯**：`vChewing-LibVanguard` 之 `make build510` 與 macOS 側巢狀套件之 `make build510` 皆 `Build complete!`、0 筆 `error:`。

**`.strings` 語法與排序**：四檔 `plutil -lint` 皆 `OK`；以倉內自己的 `LocalizableFileSorter.swift` 於複本上重跑排序 ⇒ 四檔逐位元組不變（插入位置即 canonical 位置）。

**格式管線**：兩倉 `make lintFormatUncommitted` 皆 `rc=0`；各自連跑兩次後受改檔 md5 逐次不變（LibVanguard 5 檔、macOS 11 檔）。

**兩倉 byte-sync（`md5 -q`）**：受改五檔於 LibVanguard 與 macOS 逐一同值——`UserDef.swift` `92d8b3812c24bf663d0548d0d59e83a2`、`PrefMgrProtocol.swift` `9bd87551a6ea7f0f96d211ffc3e233bf`、`PrefMgr_Core.swift` `626d1e66849aa222e7ff854850206295`、`Typewriter_BPMFFullMatch.swift` `6673b9e7998786b33183c758ac6d4711`、`InputHandlerTests_Cases5.swift` `18a1fa44fb6bd758268cbc15bce1c67b`。

## 未驗

- **真機 GUI 目視**：未在實際 IMK 客體中開啟設定、勾選新選項並目視「提示不再出現」——本 phase 之驗證止於 LibVanguard 之 state 層（`testSession.state.tooltip` 空／非空）與 macOS 側之組裝／宿主整合靶。
- **設定窗內新控件之版面**（標題行與描述行的折行）未經目視。
- **CI（linux／darwin／WinNT）**：本 phase 止於本機複驗；跨平台須推送後由 CI 判定。

## 附註

- **預設值取 `.bool(false)`＝照常顯示＝維持舊行為**：新選項是 opt-in 抑制，不動既有使用者的既有體驗；與 P222「新偏好預設＝舊行為」同規。
- **「改給聲音提示」未採納為本 phase 範圍**：工單樓主之〈功能說明〉另提議「或可像提供蜂鳴聲一樣給一個小型的聲音提示就好，不一定需要提供可能會遮擋住文字的內文提示」。本 phase 只做「關閉提示」這一維度（UserDef case 名 `kSuppressTooltipForIntonationKeyOverrideEvents` 已由事主定名為「抑制」語義）；既有的 TTS 朗讀（Phase 49 起）與蜂鳴偏好（`kBeepSoundPreference` 家族）本即有獨立的回饋管道，未因本偏好受影響或受新增。
- **未動之相鄰物**：`kSpecifyIntonationKeyBehavior` 本身（0／1／2 之語意與其值域）；`handleStandaloneIntonation` 之 `IntonationMarkInstruction` 提示；`.noLexiconRecord`／`.failedToFinalize` 兩支以 `errorCallback` 報錯的路徑；`tooltipForStandaloneIntonationMark`。
- **macOS 倉除鏡像與設定／i18n 之外零額外改動之查核**：全倉實查 `kSpecifyIntonationKeyBehavior` 之引用唯有兩個設定面板（＋巢狀 LibVanguard 本體），皆已對位；`UserDef` 之列舉遍歷（`allCases` 之 `resetAll`／`Snapshot`／JSON 匯出匯入）與 `dumpShellScriptBackup` 之 Mirror 走訪皆自動涵蓋新鍵，無須逐項登錄；`LegacyZone`（僅 InfoPlist.strings）不動。
- **建置環境（供日後取用）**：本工作區之 SwiftPM 需 `--disable-sandbox`（否則撞上巢狀沙盒 `sandbox-exec: sandbox_apply: Operation not permitted`）。
- **未 commit**：兩倉之變更留待事主 finalize。
- Swift 原始碼內未出現任何 Phase 編號。
